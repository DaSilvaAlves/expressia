id: mj-handoff-rgpd1-ready-for-push-20260616
from_agent: orchestrator (/sdc)
to_agent: devops
created: "2026-06-16T19:30:00Z"
status: consumed
consumed: true
consumed_at: "2026-06-16T19:50:00Z"
consumed_by: devops (Gage)

project: meu-jarvis (Expressia)
project_prefix: mj
priority: medium
type: ready-for-push

summary: >
  Story RGPD-1 (documentar processo manual de eliminação de conta — RGPD Art. 17,
  follow-up [AGENTE] do soft-launch) COMPLETA pelo ciclo SDC e com gate PASS
  9,0/10. Falta APENAS o push pelo @devops. É documentação PURA: 1 runbook novo +
  story + gate file, ZERO código/migrations/schema. Working tree tem 3 ficheiros
  untracked (todos em docs/). Billing CONGELADO; SEC-8 em HOLD. Este handoff migra
  o contexto para um terminal fresco (low-context) para fechar o push.

context:
  story_id: RGPD-1
  story_path: docs/stories/active/RGPD-1.account-deletion-purge-process.story.md
  branch: main
  current_task: >
    Fazer `@devops *push RGPD-1`. Ciclo SDC completo nesta sessão: @sm (Draft, 8
    ACs) → @po (GO 9,0/10, 5 PO-FIX) → @dev (runbook) → @qa (CONCERNS 8,5/10 →
    DOC-001 resolvido → PASS 9,0/10). Story em Status: Done, mas ainda em
    docs/stories/active/ — mover para completed/ faz parte do push.

  files_to_commit:
    - "docs/runbooks/rgpd-account-deletion.md — NOVO. Runbook RGPD Art. 17 (processo manual via getServiceDb, mapa CASCADE, resíduos auth.users/Storage, decisão self-service DEFERIDO, registo audit_log, 309 linhas)."
    - "docs/stories/active/RGPD-1.account-deletion-purge-process.story.md — NOVO. Mover para docs/stories/completed/ no push (Status: Done)."
    - "docs/qa/gates/RGPD-1-qa-gate.md — NOVO. Gate @qa PASS 9,0/10."

  push_instructions:
    - "Correr os quality gates pre-push conforme a autoridade @devops. Como é doc-only (zero touch em apps/, packages/db/src/, packages/db/migrations/), a regressão de código é impossível — lint/typecheck/test/build serão FULL TURBO/cache. JÁ verificados nesta sessão: check:rls EXIT 0 (NFR5 intacta, todas as tabelas com policies) + typecheck 10/10 cache."
    - "SEM db:migrate (zero migrations — 104 policies intactas)."
    - "Mover docs/stories/active/RGPD-1.account-deletion-purge-process.story.md → docs/stories/completed/ antes do commit (ou no mesmo commit)."
    - "Conventional commit + trailers (constraint/confidence/scope-risk). Sugestão de subject: `docs(rgpd): RGPD-1 — runbook de eliminação manual de conta (Art. 17) + decisão self-service deferido`."
    - "Push fast-forward, SEM --force, SEM --no-verify. CodeRabbit SKIP (precedente série SEC-1→10 + doc-only)."
    - "Actualizar docs/HANDOFF-INDEX.md: remover esta linha de Pending, adicionar em Archived; marcar este YAML consumed: true + consumed_at + consumed_by + status: consumed, e mover para docs/handoffs/archive/."

  decisions_made:
    - "Self-service de eliminação DEFERIDO para pós-soft-launch (decisão @po ratificada, documentada no runbook §6). Schema já preparado (account_deletion_jobs + accountDeletionStatusEnum + enums audit_action existem em audit.ts); falta só o fluxo (rota /api/account/delete + função Inngest gdpr-purge, NÃO registada em route.ts). Processo manual é proporcional para soft-launch (<15 min, dentro do prazo Art. 12 de 1 mês)."
    - "5 PO-FIX aplicados no runbook: (1) ordem multi-household no SET NULL — §2.5, com descoberta de que households.owner_user_id é RESTRICT (tenancy.ts:55), o que IMPÕE a ordem households→auth.users; (2) correcção factual .env.example (só comentário, não var 'gdpr') — §6; (3) canal+prazo do pedido RGPD (Art. 12) — §1.5; (4) aviso billing/fiscal (invoices.nif_customer retenção fiscal vs apagamento; cancelamento Stripe manual fora de âmbito) — §2.4; (5) account_deletion_canceled citado — §5/§6."
    - "DOC-001 (finding MEDIUM do @qa): tabela fantasma `projects` listada em §2.1 do runbook + Dev Notes da story. Reconfirmada independentemente (grep pgTable tasks.ts → 4 tabelas: tasks/task_recurrences/tags/task_tags; `projects` não existe). REMOVIDA de ambos os ficheiros. @qa pré-autorizou subida a PASS sem nova revisão."
    - "DOC-002 (LOW): clarificação opcional da resolução da connection string service_role em §1.3 — WAIVED, não bloqueante."

  gates_evidence:
    - "check:rls EXIT 0 — todas as tabelas com cobertura de policies (NFR5 intacta, zero touch DB)."
    - "typecheck 10/10 (FULL TURBO cache — nenhum código alterado)."
    - "git status --porcelain → apenas os 3 ficheiros docs/ acima (AC7/AC8 satisfeitos)."
    - "Suite completa (test/build) NÃO re-corrida nesta sessão por ser doc-only — @devops re-corre os gates pre-push standard (serão cache hits)."

  blockers: []

  caveats_de_processo:
    - "Dois subagentes (SM, DEV) caíram por ERRO DE SERVIDOR (HTTP 500 / 529 Overloaded), não por falha de trabalho. SendMessage indisponível neste harness → agente DEV morto não pôde ser retomado. O orquestrador do /sdc completou o bookkeeping MECÂNICO da story (checkboxes, Dev Agent Record, Status) + correu os gates de zero-regressão + aplicou a correcção trivial do DOC-001 (1 linha). O trabalho substantivo (o runbook) estava completo e verificado antes do crash. A revisão (gate @qa) correu em lane separada, intacta."

next_action: >
  Num terminal fresco: `@devops *push RGPD-1`. Correr os gates pre-push standard
  (cache hits — doc-only), mover a story active/→completed/, commit conventional +
  trailers, push fast-forward sem --force/--no-verify, SEM db:migrate. Depois
  consumir e arquivar este handoff + actualizar o HANDOFF-INDEX. Push é EXCLUSIVO
  do @devops.

notes: |
  - Fecha o item [AGENTE] "RGPD purge / eliminação de conta (doc)" do catálogo
    mj-handoff-followups-soft-launch-20260615 (secção follow_ups_seguranca_e_conformidade).
  - Catálogo mj-handoff-followups-soft-launch-20260615 MANTÉM-SE pending (fonte de
    verdade). Após este push, restam [AGENTE]: 2 alertas Grafana (config no dashboard
    é [EURICO]), OBS-3 Lighthouse (precisa de sessão browser), multi-household (deferido),
    INVITE-E2E-SMOKE. NÃO draftar SEC-8 (HOLD). Billing CONGELADO.
  - Caminho crítico [EURICO] inalterado: DNS-001 → Supabase Site URL → SITE_URL Vercel
    → SENTRY_DSN → SMTP/Resend + Rate Limits → smoke final.
  - Conformidade legal (placeholders das páginas /privacidade /termos + validação
    jurídica) continua bloqueante antes de divulgar a estranhos — e o runbook RGPD-1
    referencia o email de privacidade dessas páginas (§1.5), ainda placeholder.

related:
  - docs/runbooks/rgpd-account-deletion.md
  - docs/stories/active/RGPD-1.account-deletion-purge-process.story.md
  - docs/qa/gates/RGPD-1-qa-gate.md
  - docs/handoffs/mj-handoff-followups-soft-launch-20260615.yaml
  - docs/handoffs/mj-handoff-sec10-done-next-followups-20260616.yaml
