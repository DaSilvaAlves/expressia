id: mj-handoff-5.0-hotfix-po-to-dev-20260529
from_agent: po (Pax)
to_agent: dev
created: '2026-05-29T02:15:00Z'
status: consumed
consumed: true
consumed_at: '2026-05-29T03:00:00Z'
consumed_by: dev (Dex)

project: meu-jarvis (Expressia)
project_prefix: mj
story_id: '5.0-hotfix'
last_command: '@po *validate-story-draft 5.0-hotfix.middleware-auth-gate-tarefas-financas'
priority: high

summary: |
  @po (Pax) validou o draft `5.0-hotfix.middleware-auth-gate-tarefas-financas`
  → GO (Approved), 10/10 readiness, confiança HIGH. Anti-hallucination PASS:
  todos os claims confirmados byte-a-byte contra o código real. Status Draft v1.0
  → Ready v1.1. Story endereça o bypass NFR8 [SEC-MW-F1] HIGH: `middleware.ts:36`
  `APP_PATH_PREFIXES` omite `/tarefas` e `/financas`. Pronta para implementação @dev.

next_action: |
  `@dev *develop 5.0-hotfix.middleware-auth-gate-tarefas-financas`
  Fix funcional = 1 linha (adicionar '/tarefas'+'/financas' ao array). O grosso é o
  teste de cobertura auto-mantido (AC3). Após 5/5 gates GREEN → `@qa *qa-gate`
  (security gate; @qa fecha AC4 runtime data-leak + ratifica DP-HF.A e, se trivial,
  decide DP-HF.B). Depois `@devops *push`. Recomendação @po: este hotfix ANTES da 5.8.

context: |
  **Story:** docs/stories/active/5.0-hotfix.middleware-auth-gate-tarefas-financas.story.md (Ready v1.1)

  **Verificação anti-hallucination independente do @po (Article IV — PASS byte-a-byte):**
    - `middleware.ts:36` — `const APP_PATH_PREFIXES = ['/visao', '/jarvis', '/conta'] as const;` ✓ (omite /tarefas + /financas)
    - `middleware.ts:70` — `isAppPath = APP_PATH_PREFIXES.some((p) => pathname.startsWith(p))` ✓
    - `middleware.ts:71-76` — redirect 307 `/entrar?next=` só se `!user && isAppPath` ✓
    - `middleware.ts:8` — docstring afirma `/(app)/**` "(visão, tarefas, finanças, perfil…)" requer auth ✓ (contradiz array)
    - `middleware.ts:90-102` — matcher JÁ apanha /tarefas+/financas (exclui só _next, favicon, api/billing, api/cron, estáticos) → defeito SÓ na lógica isAppPath, NÃO no matcher ✓
    - `(app)/layout.tsx:23/28-30` — Server Component fino `<AppShell>{children}</AppShell>`, linha 23 afirma "auth gate intacto", SEM getUser() próprio ✓
    - `__tests__/middleware.test.ts` — EXISTE (Story 2.7), `@vitest-environment node` (linha 1), mock `@supabase/ssr` + `mocks.getUserMock` hoisted, `makeRequest(pathname)`, 6 casos (visao/jarvis/conta redirect + visao com sessão + /entrar + /api/me passam) ✓ — molde para casos novos
    - Estrutura `(app)/`: 5 segmentos de topo `conta/financas/jarvis/tarefas/visao` (+ layout.tsx + logout-action.ts ficheiros) vs 3 prefixos → 2 em falta ✓
    - Sub-rotas reais confirmadas: `/tarefas/{calendario,kanban}` + `/financas/{este-mes,variaveis,recorrentes,cartoes,patrimonio}` ✓ (AC2 cases válidos)
    - `core-config.yaml` SEM secção `coderabbit_integration` → CodeRabbit Disabled (precedente 3.1-5.7) ✓

  **DECISÕES PO (vinculativas para o @dev):**
    - DP-HF.A → **rec. A RATIFICADA**: teste de cobertura auto-mantido (lê `app/(app)/*/` via fs,
      assert cada segmento de topo coberto por `APP_PATH_PREFIXES`). É o entregável CENTRAL —
      o array já reincidiu 2× (1.5 só /visao → 2.7 +/jarvis+/conta → agora +/tarefas+/financas).
      Sem este teste, reincide pela 4ª vez. [DEV-DECISION de impl: exportar `APP_PATH_PREFIXES`
      do middleware.ts é a opção limpa para o teste importar — confirmar que NÃO quebra o bundle
      Edge. @qa ratifica no gate que o mecanismo REALMENTE parte com uma rota não coberta.]
    - DP-HF.B → **ÂMBITO FIXADO FORA do obrigatório** (opção B / follow-up LOW). NFR8 fecha-se
      com AC1+AC2+AC3; a guarda de layout é redundância, não correcção. **T4 permanece CONDICIONAL**:
      o @dev NÃO implementa T4 por iniciativa própria — só se o @architect a ratificar trivial no
      gate (via @qa). Caso contrário → follow-up LOW em STORY-BACKLOG.md. Mantém o hotfix mínimo/S.
    - DP-HF.C → **rec. A**: adicionar exactamente `'/tarefas'` + `'/financas'` (startsWith cobre sub-rotas;
      mesmo padrão de `/conta` cobre `/conta/preferencias`). NÃO adicionar sub-rotas individuais.
    - AC4 (runtime data-leak) → **confirmado como verificação @qa**, NÃO código @dev. O @dev entrega
      ao @qa a nota antes/depois do fix (T5.2). Fronteira aprovada pelo @po.

  **Âmbito (NÃO faz):** não toca tokens/dark mode (5.8), responsive/branding (5.10),
    RLS/schema/migrations, nem o matcher (middleware.ts:90-102). check:rls EXIT 0 28 tabelas INALTERADO.

  **Quality gates (5 root):** pnpm lint · typecheck · --filter @meu-jarvis/web test · build · check:rls.
    Baseline apps/web 879/116 (pós-5.7). Target net ≥ +6 (AC2 ≥5 casos + AC3 ≥1). Zero regressões nos
    6 casos existentes do middleware.test.ts. REQ-INLINE-1: de @meu-jarvis/db só `import type` (só aplicável se T4).

  **Pipeline:** @sm *draft → @po *validate ✓ → **@dev *develop** → @qa *qa-gate (security) → @devops *push.
    Apenas @devops faz push. Recomendação @po reafirmada: hotfix ANTES da 5.8.

  **Pós-fecho:** remover o achado de segurança da Story 5.10 (AC6/FUP-5.3.C) — passa a estar resolvido isoladamente.

  **Housekeeping pendente (não-bloqueante):** esta story + 5.8/5.9/5.10 + STORY-BACKLOG.md + handoffs
    estão untracked. Commit = @dev; push = @devops. O _INDEX.md do Epic 5 não lista as stories novas.

files_modified:
  # Modificado nesta sessão @po:
  - docs/stories/active/5.0-hotfix.middleware-auth-gate-tarefas-financas.story.md  # Status→Ready v1.1, DP-HF.B âmbito fixado, Change Log v1.1
  - docs/handoffs/mj-handoff-5.0-hotfix-po-to-dev-20260529.yaml  # este
  # Consumido + arquivado nesta sessão:
  - docs/handoffs/archive/mj-handoff-5.0-hotfix-sm-to-po-20260529.yaml
  # A modificar pela implementação @dev (referência, ainda não tocado):
  # - apps/web/src/middleware.ts (linha 36 array + comentário; possivelmente export APP_PATH_PREFIXES)
  # - apps/web/src/__tests__/middleware.test.ts (casos AC2 + cobertura AC3, ou ficheiro irmão middleware.coverage.test.ts)

blockers: []
