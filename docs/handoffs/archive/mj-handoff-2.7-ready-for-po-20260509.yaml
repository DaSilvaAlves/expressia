id: mj-handoff-2.7-ready-for-po-20260509
from_agent: "sm (River) — drafted by Orion (@aiox-master) per Constitution Article II"
to_agent: po
created: "2026-05-09T16:30:00+01:00"
status: pending
priority: medium
project_prefix: mj

summary: |
  Story 2.7 Draft v1.0 criada — `@po *validate-story-draft 2.7` (10-point checklist).
  Scope expandido per @aiox-master (Orion) delegated @sm authority (precedente 2.2):
  backend (schema `user_prefs` + endpoint /api/conta/preferencias + refactor /api/agent/prompt
  para gate always_preview) + frontend (UI mínima /jarvis chat + PreviewCard + toggle
  /conta/preferencias). Scope-shift M → L documentado em [AUTO-DECISION D28] @architect
  ratifica no gate. Decisão estratégica de Eurico pós-Story 2.6 push: "ver o cérebro
  a funcionar" requer UI chat real — gap funcional do Epic 2 (não há story explícita
  para UI chat principal). 14 ACs, 14 Tasks, 7 [AUTO-DECISIONS] D28-D34. Sem bloqueadores
  externos (mockable-only padrão 2.2-2.6).

context:
  story_id: "2.7"
  story_path: "docs/stories/active/2.7.preview-then-confirm-flow-ui.md"
  story_status: "Draft"
  story_version: "1.0"
  epic: "Epic 2 — Cérebro AI Multi-Intent"
  epic_progress: "6/11 stories Done (post-2.6 push) + 1 Draft (2.7)"
  branch: main

  scope_shift_rationale: |
    EPIC-2-EXECUTION.yaml linhas 220-232 originalmente definem Story 2.7 como M
    (backend toggle apenas):
      - Schema `users_prefs.always_preview boolean default false`
      - Endpoint POST /api/agent/confirm-preview/{token} (HMAC 5min)
      - Logic: gerar plan + previewToken se confidence<0.70 OU pref user

    PORÉM, Story 2.6 (Done v1.4) já entregou:
      - Endpoint confirm via nested REST `/api/agent/prompt/[runId]/confirm` (D21+DOC-FIX-001)
      - TTL 5min via `confirm_expires_at` row-level em `agent_runs`
      - Status `pending_preview` (Story 2.1 enum)

    O que resta na 2.7 backend = ~30% do scope original (apenas schema `user_prefs`
    + read no endpoint + endpoint /api/conta/preferencias). Para "ver funcionar"
    Eurico precisa de UI chat — gap do Epic 2 (não está em nenhuma das 11 stories).

    Decisão de Eurico em sessão 2026-05-09 (4-option AskUserQuestion):
      A) Story 2.7 expandida = backend + UI chat (Recommended) ← ESCOLHIDA
      B) @pm split em 2 stories (overhead SDC duplicado)
      C) Story 2.7 backend + UI demo throwaway (dívida técnica)
      D) Pivot mini-Epic UI Phase 1 (3-4 stories, adia 2.7/2.8)

    Implicação:
      - Estimate M → L (D28)
      - @architect ratifica scope-shift no gate (cross-domain backend+frontend)
      - @pm normalmente owns scope-shift mas Eurico autorizou directamente
      - Sem split em stories separadas (KISS, evita 2× ciclo SDC)

  decisions_made_at_draft:
    - "D28 scope-shift M → L (backend + UI chat mínima)"
    - "D29 schema user_prefs 1:1 user (não user×household composite PK)"
    - "D30 run_id-based confirm (sem HMAC adicional — leverage Story 2.6 D21+DOC-FIX-001)"
    - "D31 UI components inline em `app/(app)/jarvis/_components/` (sem `components/` directory geral — KISS para MVP)"
    - "D32 lazy-init `user_prefs` row no GET (UPSERT idempotente, sem seed migration)"
    - "D33 CodeRabbit pre-commit/push skipped consistente com 2.2-2.6"
    - "D34 tests UI Vitest+@testing-library/react+jsdom (sem Playwright E2E)"

  acceptance_criteria_summary:
    backend: "AC1-AC5 (schema migration + RLS + endpoint /api/conta/preferencias GET/PATCH + refactor /api/agent/prompt always_preview gate)"
    frontend: "AC6-AC10 (page /jarvis + PreviewCard + /conta/preferencias toggle + error handling PT-PT)"
    testing: "AC11-AC14 (Vitest backend + UI components, ≥45 testes novos, 5/5 quality gates GREEN, check:rls 28/112)"

  tasks_summary:
    backend: "T1 migration 0007 + T2 schema Drizzle + T3 endpoint conta/preferencias + T4 refactor /api/agent/prompt"
    frontend: "T5 page /jarvis + T6 jarvis-chat client + T7 chat-input + T8 preview-card + T9 result-message + T10 page /conta/preferencias + T11 prefs-toggle"
    testing: "T12 tests backend ≥10 conta/preferencias + ≥3 always_preview + T13 tests UI ≥18 components/pages + T14 quality gates final 5/5"

  recommended_execution_order: |
    Para @dev YOLO mockable-only:
    1. T1 (migration) → T2 (Drizzle) ANTES de qualquer endpoint test (RLS gate primeiro)
    2. T3 (endpoint conta/preferencias) com tests T12 (≥10 testes)
    3. T4 (refactor /api/agent/prompt) com tests T12 expand (+3 testes always_preview)
    4. T5-T9 (UI /jarvis components) em paralelo com tests T13 (TDD opcional)
    5. T10-T11 (UI /conta/preferencias) com tests T13
    6. T14 quality gates final (typecheck → lint → test → check:rls → build)

  critical_dev_notes:
    - "DN1 mockable-only: zero call real LLM/Anthropic/OpenAI. Mocks Vitest precedente 2.6."
    - "DN2 imports cross-package: `./` em packages internos source files (D27 Story 2.6 directive); `@/*` apenas em apps/web internas e tests internos packages."
    - "DN3 RLS check: `pnpm check:rls` deve reportar 28/112 (subindo de 27/108) — `user_prefs` adiciona 4 policies idempotentes via DO block."
    - "DN4 endpoint conta/preferencias usa `getDb()` (RLS via JWT), NÃO `getServiceDb()`."
    - "DN5 lazy-init UPSERT: `INSERT ... ON CONFLICT (user_id) DO NOTHING` no GET; `INSERT ... ON CONFLICT (user_id) DO UPDATE SET always_preview = $1, updated_at = now()` no PATCH."
    - "DN6 layout (app)/ tem header com link `Expressia` → `/visao` + botão Sair. Adicionar links `Jarvis` → `/jarvis` e `Conta` → `/conta/preferencias`."
    - "DN7 components em `_components/` (underscore prefix Next.js — não treats as route)."

  blockers_external:
    - "EB1 ANTHROPIC_API_KEY DPA UE PENDING — NÃO bloqueia (mockable-only)."
    - "EB2 OPENAI_API_KEY DPA UE PENDING — NÃO bloqueia (mockable-only)."
    - "EB3 Upstash Redis PENDING — NÃO bloqueia (cache não aplicado nesta story; Story 2.9 endereça)."

  predecessor_handoff: "mj-handoff-2.6-ready-for-devops-close-20260509 (consumed/archived 2026-05-09 16:00 — Story 2.6 Done v1.4 push completo)"

next_action: |
  @po *validate-story-draft 2.7

  Sequência:
  1. Ler `docs/stories/active/2.7.preview-then-confirm-flow-ui.md` v1.0 completo.
  2. Executar 10-point checklist:
     - Title clear + descriptive ✓
     - Description user story format ✓
     - AC numerados + testáveis (14 ACs verificar)
     - Scope sized (L confirmado per D28)
     - Dependencies (2.6 ✓ Done; sem bloqueadores externos)
     - Estimate L scope-shift ratification (verificar D28 justification + risk SR1)
     - Business value clear (FR4 + visibilidade Eurico)
     - Risks documented (7 SRs)
     - DoD inferível (14 ACs + 5/5 gates)
     - Alignment com Architecture (§4.4 + §4.5 + §5.1 + §8.1) + PRD FR4 + EPIC-2 §5/§8 DP2
  3. Cross-confirm anti-hallucination contra schema real:
     - `auth.users` em `packages/db/src/schema/auth.ts` (referência mínima Supabase Auth)
     - `households` em `packages/db/src/schema/tenancy.ts` (FK target)
     - `current_household_id()` helper (Story 1.3 migrations)
     - Pattern RLS DO block idempotente (Story 2.6 0001_rls_policies.sql)
     - Endpoint paths nested REST (Story 2.6 D21+DOC-FIX-001 architecture.md §1.3+§4.5)
     - `apps/web/src/app/(app)/` route group + middleware auth gate (Story 1.5)
     - `createServerSupabaseClient` em `@meu-jarvis/auth/server` (Story 1.5)
     - `withSpan`/Pino logger em `@meu-jarvis/observability` (Story 1.7)
  4. Review 7 [AUTO-DECISIONS] D28-D34:
     - D28 scope-shift M→L: PASS / CONCERNS-ACCEPTABLE / FAIL?
     - D29 schema 1:1 user: PASS verificar consistência com `auth.users` cardinalidade
     - D30 run_id-based: PASS verificar leverage 2.6 D21+DOC-FIX-001
     - D31 components inline: PASS verificar zero `components/` directory existente
     - D32 lazy-init: PASS verificar UPSERT idempotency padrão Supabase
     - D33 CodeRabbit skip: PASS consistência 2.2-2.6
     - D34 tests UI Vitest+jsdom: PASS verificar `apps/web/vitest.config.ts` setup
  5. Veredicto: GO (≥7/10) ou NO-GO (<7/10 + required fixes listados).
  6. Se GO: status Draft → Ready, story v1.0 → v1.1 com PO Validation block + Change Log.
  7. Se PO_FIX_INLINE necessários: aplicar inline (precedente 2.6 v1.0 → v1.1, 5 fixes).
  8. Update `_INDEX.md` row 2.7 Draft → Ready.
  9. Criar handoff sucessor `mj-handoff-2.7-ready-for-dev-20260509` (po → dev YOLO mockable-only).
  10. Update HANDOFF-INDEX.md (consume + archive este; pending row sucessor).

review_focus_areas:
  - "AC11+T12 RLS isolation cross-household (regression): garantir que test inclui cenário user A em household X tenta GET/PATCH user B em household Y → 0 rows."
  - "D28 scope-shift ratification: @po pode aceitar com nota CONCERNS-ACCEPTABLE OU exigir @pm formal scope-shift via [PO_FIX_INLINE]. Recomendação: aceitar dado precedente 2.2 (Orion executou *draft directamente) + decisão directa Eurico."
  - "D29 schema 1:1 user: edge case user em 2 households com prefs diferentes — explicitar como deferred DP no Dev Notes (já está)?"
  - "D31 inline components: pode @po preferir já criar `apps/web/src/components/` directory antecipando Story 2.8? — sugerir aceitar D31 KISS, refactor mecânico futuro se padrão repetir."
  - "AC8 PreviewCard countdown timer: validar UX edge case timer expira durante user a clicar Confirmar (race) — server-side check `confirm_expires_at > now()` já existe Story 2.6, UI mostra disabled state."

consumed: true
consumed_at: "2026-05-09T17:30:00+01:00"
consumed_by: "po (Pax)"
status: consumed

consumption_record: |
  Consumido por Pax (@po) 2026-05-09 17:30 ao executar `*validate-story-draft 2.7`.

  **Veredicto: GO 9.0/10 HIGH confidence**.

  10/10 critérios PASS no checklist (title clear, user story format, 14 ACs
  testáveis, scope L confirmado per D28, dependencies 2.6 ✓ + sem externos,
  estimate L razoável para 22 ficheiros tocados + ≥45 tests, business value FR4
  + Eurico "ver o cérebro a funcionar", 7 SRs documentados, DoD inferível 14 ACs
  + 5/5 gates, alignment Architecture §4.4 + §4.5 + §5.1 + §8.1 + PRD FR4 +
  EPIC-2 §5/§8 DP2). Score raw 10/10 → ajuste 9.0/10 reflecte 4 PO_FIX_INLINE
  necessários (não erros bloqueantes mas anti-hallucinations vs codebase real).

  Cross-confirm anti-hallucination contra 9 fontes reais:
    - packages/db/src/schema/auth.ts:26-31 (authUsers PASS)
    - packages/db/src/schema/tenancy.ts:46-71 (households FK PASS)
    - packages/db/migrations/0000:34-45 (current_household_id helper PASS)
    - packages/db/migrations/0001:692-708 (DO block pattern Story 2.6 PASS)
    - apps/web/src/app/api/agent/prompt/route.ts:293-300+400-407 (response shape REAL — HALLUCINATION DETECTED + FIXED)
    - apps/web/src/app/(app)/visao/page.tsx (Server Component pattern PASS)
    - apps/web/src/middleware.ts:24+57 (APP_PATH_PREFIX literal — HALLUCINATION DETECTED + FIXED)
    - packages/auth/src/server.ts:55 (createServerSupabaseClient PASS)
    - packages/observability/src/index.ts:12-22 (withSpan, Pino, Sentry PASS)
    - apps/web/vitest.config.ts:18 (jsdom + react plugin PASS — D34 OK)

  7 [AUTO-DECISIONS] D28-D34 classification:
    - D28 scope-shift M→L: PASS (precedente 2.2 + Eurico authorized)
    - D29 schema 1:1 user: CONCERNS-ACCEPTABLE (edge case deferred DP)
    - D30 run_id-based confirm: PASS (alinhado Story 2.6 D21+DOC-FIX-001)
    - D31 components inline: PASS (verified zero apps/web/src/components/)
    - D32 lazy-init UPSERT: PASS (Supabase canonical pattern)
    - D33 CodeRabbit skip: PASS (consistent 2.2-2.6)
    - D34 tests UI Vitest+jsdom: PASS (vitest.config.ts:18 already configured)

  4 [PO_FIX_INLINE v1.1] aplicados:
    1. Endpoint response shape (AC3+AC7+AC8+Dev Notes): `status: 'success'|'pending_preview'`
       → shape REAL Story 2.6 `mode: 'executed'|'preview'` + `results` (AtomicOutcome) +
       `expires_at` response key. Coluna DB `confirm_expires_at` mantida inalterada.
    2. AC2+T1+Dev Notes RLS placement: 4 policies migradas de 0007 → `0001_rls_policies.sql`
       via DO block condicional (espelha pattern Story 2.6 D17 :692-708).
       check-rls-coverage.ts:33 lê APENAS 0001.
    3. AC2 RLS predicate: `auth.uid() = user_id AND current_household_id()` →
       `is_household_member(household_id) AND auth.uid() = user_id` — combina
       cross-tenancy isolation com user-scoped constraint específico.
    4. AC6+T5 middleware auth gate: `APP_PATH_PREFIX = '/visao'` LITERAL — route groups
       Next.js são virtuais; T5 sub-task added para refactor `APP_PATH_PREFIXES` array
       `['/visao', '/jarvis', '/conta']`. File List atualizada (+`middleware.ts` +
       `0001_rls_policies.sql` modificados → 14 novos + 8 modificados ≈ 22 tocados).

  Status Draft → Ready (autoridade @po per ~/.claude/rules/story-lifecycle.md).
  Story v1.0 → v1.1 com PO Validation block + Change Log @po + 4 PO_FIX_INLINE markers.
  _INDEX.md row 2.7 actualizada Draft → Ready + Epic 2 header note.

  Concerns para @architect no gate:
    (a) Ratificar D28 scope-shift M→L cross-domain (backend+frontend numa única story).
    (b) Ratificar predicate RLS combo `is_household_member + auth.uid()` (divergência
        defensável vs precedent puro — user_prefs é genuinamente user-scoped).
    (c) Confirmar middleware refactor `APP_PATH_PREFIXES` array não causa regression
        em existing `/visao` route — recomenda-se test unitário.

  Sucessor: `mj-handoff-2.7-ready-for-dev-20260509` (po → dev unblocked YOLO mockable-only).
