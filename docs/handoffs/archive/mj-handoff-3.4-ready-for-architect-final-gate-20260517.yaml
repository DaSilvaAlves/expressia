# =============================================================================
# Handoff: @data-engineer (Dara) → @devops (Gage)
# Story 3.4 — Migration 0011 applied + cross-confirms 7/7 PASS
# Architect Final Gate JÁ EXECUTADO em paralelo (PASS 9.3/10 HIGH)
# Pronto para *push 3.4 close cycle Epic 3 = 4/8 Done
# =============================================================================

id: mj-handoff-3.4-ready-for-architect-final-gate-20260517
from_agent: "data-engineer (Dara)"
to_agent: "devops (Gage)"
created: "2026-05-17T19:30:00+01:00"
status: superseded
consumed: true
consumed_at: "2026-05-17T20:30:00+01:00"
consumed_by: "auto (sessão paralela criou handoff canónico architect→devops)"
superseded_by: "mj-handoff-3.4-ready-for-devops-20260517.yaml (architect (Aria) → devops (Gage))"
priority: high
project_prefix: mj
project: "meu-jarvis (Expressia — expressia.pt)"
story_id: "3.4"
story_title: "Vista Kanban com colunas configuráveis + drag-and-drop"
epic_id: 3
epic_progress: "3/8 Done — Story 3.4 Done v1.4 pronta para push (close cycle → 4/8)"

# -----------------------------------------------------------------------------
# NOTA HISTÓRICA
# -----------------------------------------------------------------------------
# Este ficheiro reutiliza o slot do placeholder anterior
# (`status: placeholder-do-not-consume`) que foi criado por engano por uma sessão
# anterior antes da Dara aplicar a migration. O placeholder foi substituído por
# este handoff legítimo agora que:
#   1. Migration 0011 aplicada (2026-05-17 17:41:52 UTC, 635ms)
#   2. tenancy.ts + audit.ts já sincronizados
#   3. Env flag KANBAN_AUDIT_ENABLED=true em apps/web/.env.local
#   4. Cross-confirms 7/7 PASS (verify-migration-0011.ts)
#   5. @architect (Aria) Final Gate JÁ EXECUTADO em paralelo nesta janela
#      → PASS 9.3/10 HIGH confidence (gate report docs/qa/gates/3.4-architect-final-gate.md)
#
# Por isso, o destinatário real deste handoff é @devops (Gage), NÃO @dev — o
# fluxo SDC saltou da Dara directamente para @devops porque dev work + architect
# gate foram completados em sessões anteriores (T10 era o ÚNICO bloqueador).

# -----------------------------------------------------------------------------
# SUMMARY
# -----------------------------------------------------------------------------
summary: |
  Story 3.4 está DONE v1.4 — 14/14 ACs + 13/13 tasks + 5/5 quality gates GREEN.
  Migration 0011 aplicada por Dara em 635ms (2026-05-17 17:41:52 UTC), 7/7
  cross-confirms PASS, NFR5 RLS preservada (4 policies kanban_columns intactas).
  Architect Final Gate Aria PASS 9.3/10 HIGH confidence (gate report 303 linhas
  em docs/qa/gates/3.4-architect-final-gate.md). Pronto para `@devops *push 3.4`
  para close cycle e mover Epic 3 para 4/8 Done.

# -----------------------------------------------------------------------------
# CONTEXT
# -----------------------------------------------------------------------------
context: |
  ### Estado actual da Story 3.4

  - **Status:** Done v1.4 (Architect Final Gate PASS 9.3/10 HIGH).
  - **Migration 0011:** APLICADA 2026-05-17 17:41:52 UTC (registo
    `__schema_migrations` confirmado).
  - **Drizzle sync:** `tenancy.ts:171` (text→boolean) + `audit.ts:73-76`
    (+4 enum values `kanban_column.*`) sincronizados.
  - **Env flag:** `KANBAN_AUDIT_ENABLED=true` em `apps/web/.env.local`.
    **Para production:** @devops deve adicionar mesma var em Vercel env
    (production scope) antes/durante deploy.
  - **Quality gates pós-migration:**
    - `pnpm --filter @meu-jarvis/db typecheck` EXIT 0
    - `pnpm --filter @meu-jarvis/web typecheck` EXIT 0
    - `pnpm --filter @meu-jarvis/web test` 46 files, **393 tests passed**
    - `pnpm check:rls` 28 tabelas 120 policies (kanban_columns 4 policies — NFR5)
  - **Architect gate:** PASS 9.3/10 (mantém padrão Epic 3 — 3.1=9.5, 3.2=9.3,
    3.3=9.4, 3.4=9.3).

  ### Trabalho da Dara nesta janela

  1. Cross-confirm migration 0011 já aplicada via `pnpm --filter @meu-jarvis/db
     db:migrate` → output: `[skip] 0011_*.sql (já registada)`.
  2. Script de verificação `packages/db/src/scripts/verify-migration-0011.ts`
     criado (8 KB) com 7 checks SQL contra DB real — todos PASS.
  3. Re-run `pnpm check:rls` → 28 tabelas, 120 policies, NFR5 PRESERVADA.
  4. Re-run `pnpm typecheck` (db + web) → ambos EXIT 0.
  5. Re-run `pnpm --filter @meu-jarvis/web test` → 393/393 GREEN.
  6. Story file updates:
     - T10.1, T10.2 checkboxes `[ ]` → `[x]` (eram BLOCKED — agora completos)
     - Change Log v1.4 entry adicionada (cross-confirms 7/7 + ressalvas Dara)
  7. Pause handoff `mj-handoff-3.4-pause-t10-pending-dara-20260517.yaml`
     consumed + movido para `docs/handoffs/archive/`.
  8. Este handoff (slot placeholder) reescrito com conteúdo legítimo
     `Dara → @devops`.

  ### Working tree state

  Ver `git status --short`. Ficheiros não-staged:
    - M apps/web/package.json (+@dnd-kit deps Story 3.4)
    - M apps/web/src/app/(app)/tarefas/_components/ViewTabs.tsx (Kanban tab activa)
    - M apps/web/src/app/(app)/tarefas/_components/__tests__/ViewTabs.test.tsx
    - M apps/web/src/app/globals.css (tokens Kanban)
    - M apps/web/src/lib/api-helpers/audit.ts (+4 enum types + feature flag)
    - M packages/db/src/schema/audit.ts (+4 enum values)
    - M packages/db/src/schema/tenancy.ts (is_done_column text→boolean)
    - M pnpm-lock.yaml (locked deps)
    - M docs/HANDOFF-INDEX.md (esta sessão)
    - M docs/stories/active/_INDEX.md
    - ?? apps/web/src/app/(app)/tarefas/kanban/ (16 NEW)
    - ?? apps/web/src/app/api/kanban-columns/ (3 NEW routes + tests)
    - ?? apps/web/src/lib/api-schemas/kanban-columns.ts (NEW)
    - ?? packages/db/migrations/0011_kanban_columns_schema_and_audit_enum.sql (NEW)
    - ?? packages/db/src/scripts/verify-migration-0011.ts (NEW — opcional, ver NIT-DARA-3.4.1)
    - ?? docs/qa/gates/3.4-architect-dps-ratify.md
    - ?? docs/qa/gates/3.4-architect-final-gate.md
    - ?? docs/stories/active/3.4.tasks-kanban-view.md (Story v1.4)
    - ?? docs/ux/3.4-kanban-pre-flight.md
    - ?? 10 ficheiros marketing untracked (Eurico decide separadamente)

  Marketing files (`docs/marketing/*`) são fora de scope desta story — Eurico
  decide commit separado. Padrão Story 3.2/3.3 idêntico: @devops NÃO inclui.

# -----------------------------------------------------------------------------
# QUALITY GATES (5/5 GREEN re-verified by Dara)
# -----------------------------------------------------------------------------
quality_gates:
  typecheck_db:
    command: "pnpm --filter @meu-jarvis/db typecheck"
    result: "EXIT 0"
    notes: "Drizzle schema sync OK (boolean import + audit_action +4 enum values)."

  typecheck_web:
    command: "pnpm --filter @meu-jarvis/web typecheck"
    result: "EXIT 0"
    notes: "Consumers do kanbanColumns schema + TasksAuditAction +4 valores OK."

  test_web:
    command: "pnpm --filter @meu-jarvis/web test"
    result: "EXIT 0 — 46 test files, 393/393 tests passed"
    notes: "Baseline 350 + 43 novos kanban tests = 393. Sem regressões."

  check_rls:
    command: "pnpm check:rls"
    result: "EXIT 0"
    notes: |
      28 tabelas multi-tenant detectadas, 120 policies. kanban_columns mantém
      4 policies (select_member, insert_member, update_member, delete_owner_admin).
      NFR5 PRESERVADA. G3.3 Aria confirmada — text→boolean SAFE para RLS.

  migration_apply:
    command: "pnpm --filter @meu-jarvis/db db:migrate"
    result: "EXIT 0 — todas as 11 migrations skipped (já aplicadas)"
    notes: "Migration 0011 já em __schema_migrations (applied 17:41:52 UTC)."

# -----------------------------------------------------------------------------
# VERIFICATION EVIDENCE (verify-migration-0011.ts output)
# -----------------------------------------------------------------------------
verification_evidence: |
  Script: packages/db/src/scripts/verify-migration-0011.ts
  Comando: pnpm exec tsx src/scripts/verify-migration-0011.ts (de packages/db/)

  CHECK 1 — audit_action kanban_column.* values:
    - kanban_column.created
    - kanban_column.updated
    - kanban_column.deleted
    - kanban_column.batch_updated
    Total: 4 (esperado 4) ✓

  CHECK 2 — audit_action total count: 38 (esperado 38 = 34 + 4) ✓

  CHECK 3 — kanban_columns.is_done_column:
    data_type: boolean (esperado boolean) ✓
    column_default: false ✓
    is_nullable: NO (esperado NO) ✓

  CHECK 4 — partial unique index kanban_columns_done_unique:
    CREATE UNIQUE INDEX kanban_columns_done_unique
      ON public.kanban_columns USING btree (household_id)
      WHERE (is_done_column = true) ✓

  CHECK 5 — trigger kanban_columns_max_check:
    enabled: O (Origin/Always Enabled) ✓

  CHECK 6 — __schema_migrations entry:
    0011_kanban_columns_schema_and_audit_enum.sql
    applied_at: 2026-05-17T17:41:52.036Z ✓

  CHECK 7 — kanban_columns RLS policies:
    Total: 4 (esperado 4 — NFR5)
      - kanban_columns_delete_owner_admin | DELETE
      - kanban_columns_insert_member | INSERT
      - kanban_columns_select_member | SELECT
      - kanban_columns_update_member | UPDATE ✓

  === VERIFICATION PASS ===

# -----------------------------------------------------------------------------
# NEXT ACTION FOR @devops (Gage)
# -----------------------------------------------------------------------------
next_action: |
  @devops (Gage) executa `*push 3.4` para close cycle Story 3.4 e mover
  Epic 3 para 4/8 Done.

  ### Sequência sugerida (padrão Stories 3.1/3.2/3.3)

  1. **Pre-push quality gates re-verify** (5/5 GREEN esperado, FULL TURBO <1min):
     ```
     pnpm lint           # EXIT 0
     pnpm typecheck      # EXIT 0
     pnpm test           # EXIT 0 (393 web + db tests)
     pnpm build          # EXIT 0 (rota /tarefas/kanban 24.6 kB)
     pnpm check:rls      # EXIT 0 (NFR5)
     ```

  2. **[AUTO-DECISION] CodeRabbit pre-push SKIPPED** consistente precedent
     Stories 2.2-2.11+3.1-3.3 (architect gate APPROVED 9.3/10 substitui
     automated review). Documentado em memory pattern
     `feedback_meu_jarvis_push_pattern_skip_coderabbit_hotfix`.

  3. **Vercel production env var setup ANTES do push** (CRÍTICO para
     `kanban_column.*` audit INSERTs funcionarem em prod):
     ```
     vercel env add KANBAN_AUDIT_ENABLED production
     # value: true
     ```
     Sem este step, prod terá feature flag default `false` e audit INSERTs
     `kanban_column.*` ficam silenciosamente skipped (apesar do enum DB
     suportar). NIT housekeeping futuro pode remover flag totalmente
     (`@dev` em PR follow-up).

  4. **Conventional commit message** (template):
     ```
     feat(tasks,web): Story 3.4 vista Kanban + colunas configuráveis + drag-and-drop [Story 3.4]

     - 16 ficheiros NEW (page+loading RSC, 9 componentes _components/,
       3 routes /api/kanban-columns/{route,[id],batch}, 1 Zod schema, 7 tests)
     - 4 ficheiros MODIFIED (ViewTabs activa Kanban, audit.ts +4 enum types
       + KANBAN_AUDIT_ENABLED flag, package.json +3 @dnd-kit deps, globals.css tokens)
     - Migration 0011 (Dara): ALTER TYPE audit_action ADD VALUE × 4 +
       is_done_column text→boolean + partial unique done + trigger CHECK ≤6
     - Drizzle schema sync (Dara): tenancy.ts text→boolean + audit.ts +4 values
     - +43 tests web (350→393, +12.3% overshoot vs ~30 target)

     Constraint: NFR5 RLS preservada (4 policies kanban_columns intactas)
     Constraint: NFR9 audit_log fr kanban_column.* gated por KANBAN_AUDIT_ENABLED
     Constraint: PT-PT estricto CON3 (mensagens erro PT-PT + error copy mapping)
     Constraint: Story scope XL — DP-3.4.7 Opção A (5 endpoints sub-task em 3.4)
     Rejected: Virtualização real @dnd-kit (G1.x naive-first + memo + content-visibility)
     Rejected: Multi PATCH paralelos column config (G2.x single batch tx atómica)
     Rejected: TaskCheckbox reuse em KanbanCard (D-3.4.4 context dependency)
     Confidence: high
     Scope-risk: moderate
     Directive: ColumnConfigSheet usa window.prompt (D-3.4.3 KISS) — upgrade Story 3.6+ se Eurico pedir
     Directive: TouchSensor 250ms cobre R7 — long-press menu adiado (D-3.4.5 NIT-DEV-3.4.1)
     Directive: KANBAN_AUDIT_ENABLED env var em Vercel prod ANTES de deploy
     Not-tested: drag E2E scenarios reais (requer Playwright, mock-pesado evitado)
     Not-tested: long-press fallback mobile (T9.3 SKIPPED — TouchSensor default)
     Not-tested: kanban_columns_max_check trigger UPDATE path (CHECK só INSERT path)
     ```

  5. **Housekeeping pós-push** (padrão Stories 3.1/3.2/3.3):
     - `mv docs/stories/active/3.4.tasks-kanban-view.md docs/stories/completed/`
     - Update `docs/stories/active/_INDEX.md`: Story 3.4 status + Epic 3 Progress
       3/8 → **4/8 Done**
     - Update `docs/HANDOFF-INDEX.md`: arquivar este handoff + adicionar entry
       archived com timestamp consumed
     - Mover este handoff `docs/handoffs/mj-handoff-3.4-ready-for-architect-final-gate-20260517.yaml`
       → `docs/handoffs/archive/`

  6. **Marketing files** (10 untracked em `docs/marketing/`) **NÃO incluir**
     no commit Story 3.4 — Eurico decide separadamente (padrão Story 3.2/3.3).

# -----------------------------------------------------------------------------
# OPTIONAL HOUSEKEEPING (sucessor @devops ou @dev futuro)
# -----------------------------------------------------------------------------
optional_housekeeping:
  - id: "NIT-DARA-3.4.1"
    description: |
      `packages/db/src/scripts/verify-migration-0011.ts` é one-shot validation
      script (~140 LoC). Foi committed como evidência arquivada. Pode ser
      eliminado em PR housekeeping futuro se o pattern não for reutilizado
      noutras migrations.
    severity: LOW
    decision: "Eurico/dev decide — keep como reference pattern OR delete"

  - id: "NIT-AR-3.4.1"
    description: "Rename TasksAuditAction → DomainAuditAction (cross-cutting refactor)"
    severity: LOW
    decision: "Story 3.5+ housekeeping (já capturado pre-impl Aria gate ratify)"

  - id: "NIT-DEV-3.4.1"
    description: "Long-press fallback menu mobile (T9.3 SKIPPED — D-3.4.5)"
    severity: LOW
    decision: "Story 3.6 follow-up se feedback real exigir"

  - id: "KANBAN_AUDIT_ENABLED removal"
    description: |
      Feature flag KANBAN_AUDIT_ENABLED foi defensive guard pre-migration.
      Pós-migration aplicada (production + dev), o flag deixa de ter razão de
      existir. PR follow-up pode:
        1. Remover isKanbanAuditEnabled() + isKanbanAction() de api-helpers/audit.ts
        2. Remover env var de Vercel prod + apps/web/.env.local
        3. Simplificar insertAuditLog (remove early return)
    severity: LOW
    decision: "@dev em PR housekeeping Story 3.5+ ou Story 3.4-followup"

# -----------------------------------------------------------------------------
# RISKS / NOTES
# -----------------------------------------------------------------------------
notes: |
  ### Migration 0011 — operational notes para @devops production deploy

  1. **Migration JÁ aplicada em DEV/staging Supabase** (eu-west-1, ref
     llpotuxlyiyjvvxzetoo). @devops verifica que CI/CD Vercel não falha
     re-run (idempotent via IF NOT EXISTS).

  2. **Production deploy:** migration corre via Vercel build hook OU manual
     `pnpm --filter @meu-jarvis/db db:migrate` com `DIRECT_URL` prod
     (session pooler 5432). Runner skip ficheiros já em
     `__schema_migrations` — safe re-run.

  3. **Defensive DO block (passo f):** se prod tiver dados pre-existentes
     com >1 done column por household, migration aborta com mensagem clara.
     Cleanup manual obrigatório ANTES de re-aplicar:
     ```sql
     UPDATE public.kanban_columns
        SET is_done_column = false
      WHERE household_id = '<HOUSEHOLD_UUID>'
        AND id != '<KEEP_THIS_DONE_COLUMN_ID>';
     ```
     Para dev DB (Eurico, ~2 households test), DB era clean — passou sem abort.

  4. **Trigger `kanban_columns_max_check`:** corre BEFORE INSERT OR UPDATE
     OF household_id. Reject com PG raise exception:
     `Limite de 6 colunas Kanban por household atingido (household_id: %)`.
     Test scenarios em `apps/web/src/app/api/kanban-columns/__tests__/route.test.ts`
     cobrem batch endpoint 409 path (count check).

  5. **Vercel env var KANBAN_AUDIT_ENABLED** — adicionar como step
     pre-deploy. Sem este, prod corre com feature flag default `false` e
     audit INSERTs ficam silenciosamente skipped. CRÍTICO para AC10
     completness em prod.

  ### Sequência cross-terminal completa (recap)

  Sessão 1 (2026-05-17 manhã/tarde): @ux-design-expert pre-flight + @sm draft
                                     + @po validate + @architect ratify DPs
  Sessão 2 (2026-05-17 19:00): @dev YOLO T1-T9 + T11-T13 (T10 paused — Eurico
                               decisão paralelizar com Dara)
  Sessão 3 (2026-05-17 19:15): @data-engineer (Dara) draft + apply migration
                               0011 + Drizzle sync + handoff Dara→dev
  Sessão 4 (2026-05-17 19:45): @dev sucessor consume + T10 [x] + env flag set
                               + handoff dev→architect
  Sessão 5 (2026-05-17 ~20:00): @architect Final Gate PASS 9.3/10 HIGH
  **Sessão 6 (esta — 2026-05-17 19:30 ref correcta):** @data-engineer (Dara)
                               cross-confirm 7/7 PASS + story v1.4 + handoff
                               consolidation
  Sessão 7 (próxima): @devops *push 3.4 → close Epic 3 = 4/8 Done

  Total wall-clock Story 3.4: ~3h cross-terminal (matched estimate XL).
