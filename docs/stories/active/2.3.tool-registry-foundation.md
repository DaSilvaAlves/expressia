# Story 2.3 — Tool Registry Foundation

**Status:** InProgress (v1.2) — @dev *develop em curso
**Epic:** Epic 2 — Cérebro AI Multi-Intent
**Owner:** @dev (impl) → @architect (gate)
**Estimate:** L
**Version:** 1.1
**Created:** 2026-05-08
**Updated:** 2026-05-08

---

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["pnpm typecheck", "pnpm lint", "pnpm test", "pnpm build", "pnpm check:rls"]
```

**Justificação do quality gate:** Story define o contrato `ToolDefinition<I,O>` e o `executeAtomic` que todas as stories Epic 2 vão consumir para criar entidades em Postgres (2.4 Classifier lê tools, 2.5 Planner+Executor executa tools, 2.6 endpoint expõe o resultado). A semântica transaccional de `executeAtomic` e a estrutura de `ReverseOp` são decisões arquitecturais. `@architect` (Aria) tem autoridade sobre estas decisões. `@qa` entra em stories funcionais com outputs end-to-end testáveis (2.5+, 2.10).

---

## Bloqueadores Externos

| ID | Requisito | Estado | Impacto nesta story |
|----|-----------|--------|---------------------|
| **EB1** | `ANTHROPIC_API_KEY` DPA UE | PENDING | **NÃO bloqueia.** Bloqueia 2.5/2.10. |
| **EB2** | `OPENAI_API_KEY` DPA UE | PENDING | **NÃO bloqueia.** Bloqueia 2.4/2.10. |

Story 2.3 é **100% mockável** — sem calls LLM reais, sem chamada a provider em produção.

---

## User Story

**As a** developer da equipa Expressia construindo o pipeline 3-stage do Cérebro AI,
**I want** um package `@meu-jarvis/tools` com a interface tipada `ToolDefinition<I,O>`, o `ToolRegistry` que regista e recupera tools, o helper `executeAtomic` que envolve múltiplas execuções de tools numa única transacção Drizzle com persistência de `ReverseOp` em `agent_reverse_ops`, e a taxonomia de erros específica de tools,
**so that** as Stories 2.5 (Planner+Executor) e 2.6 (endpoint agent/prompt) possam orquestrar tool calls multi-intent em transacção atómica com suporte a undo de 30s (FR6), sem duplicar lógica de atomicidade, sem fugas de PII para logs, e com OTel tracing consistente com o package `@meu-jarvis/agent` entregue em 2.2.

---

## Dependências

- **DEPENDE de Story 2.2 Done** (package `@meu-jarvis/agent` disponível: `ProviderInterface`, error hierarchy, `withProviderSpan`, `redactProviderPayload` — reutilizados aqui).
- **DEPENDE de Story 1.3 Done** (schema `agent_reverse_ops` aplicado: migration `0005`, coluna `expires_at DEFAULT now() + interval '30 seconds'`, `household_id NOT NULL`).
- **DEPENDE de Story 1.7 Done** (package `@meu-jarvis/observability`: `logger`, `withSpan`, `annotateSpan`, `recordSpanError`, `hashForCorrelation`).
- **Opcional (não bloqueia):** Story 1.4 Done (Testcontainers RLS suite — disponível para testes de integração bónus; testes obrigatórios usam mocks Vitest).
- **Stories que dependem desta:**
  - Story 2.5 (Planner + Executor) — executa tools via `executeAtomic`
  - Story 2.6 (Endpoint POST /api/agent/prompt) — usa tool definitions para compor tool_calls do Sonnet

---

## Acceptance Criteria

### AC1 — Estrutura do package

Package `@meu-jarvis/tools` criado em `packages/tools/` com:
- `package.json` source-only (sem build step, segue padrão `@meu-jarvis/agent` e `@meu-jarvis/db`); deps: `zod ^3.23.8`, `@meu-jarvis/db: workspace:*`, `@meu-jarvis/observability: workspace:*`, `@opentelemetry/api`; devDeps: `vitest`, `typescript`, `@types/node`.
- `tsconfig.json` extends `tsconfig.base.json` com `paths: { "@/*": ["./src/*"] }`.
- Estrutura de directórios: `src/{contracts.ts,registry.ts,atomic.ts,errors.ts,tracing.ts,index.ts,__fixtures__/,__tests__/}`.
- `vitest.config.ts` com env `node`, globals `true`, alias `@`.
- Adicionado a `apps/web/next.config.ts` `transpilePackages` array.
- Workspace dep `"@meu-jarvis/tools": "workspace:*"` adicionado a `apps/web/package.json`.

### AC2 — Contrato `ToolDefinition<I, O>` tipado com Zod

`packages/tools/src/contracts.ts` exporta:

```ts
export interface ToolDefinition<I, O> {
  readonly name: string;               // ex: 'create_task' — snake_case, lowercase
  readonly domain: ToolDomain;         // 'tasks' | 'finance' | 'query' | 'system'
  readonly description: string;        // PT-PT; descreve QUANDO usar (passado ao LLM)
  readonly inputSchema: z.ZodType<I>;  // valida args vindos do LLM
  readonly outputSchema: z.ZodType<O>; // valida resultado de execute()
  readonly requiredPlan?: PlanTier;    // 'pessoal' | 'familia' | 'pro' — optional; ausência = free+
  readonly estimatedTokens?: number;   // dica para cost router (Story 2.9)
  preview(input: I, ctx: ToolExecutionContext): string;                         // texto PT-PT para preview card (FR4)
  execute(input: I, ctx: ToolExecutionContext): Promise<O>;                     // execução dentro de transacção (ver executeAtomic)
  reverse(output: O, ctx: ToolExecutionContext): Promise<ReverseOpPayload>;     // produz reverse_op declarativo para undo (FR6)
}
```

Exporta também:
- `ToolDomain` — `z.enum(['tasks', 'finance', 'query', 'system'])`, tipo derivado `ToolDomain`.
- `PlanTier` — `z.enum(['free', 'pessoal', 'familia', 'pro'])`, tipo derivado `PlanTier`.
- `ToolExecutionContext` — interface com `{ householdId: string; userId: string; db: DrizzleDbClient; traceId: string; runId: string }`. `db` é o cliente Drizzle com RLS activa (retornado por `getDb()` de `@meu-jarvis/db`). `DrizzleDbClient` é o tipo inferido de `getDb()`.
- `ReverseOpPayload` — `z.discriminatedUnion('kind', [...])` com variantes:
  - `{ kind: 'delete_row'; table: string; id: string }` — insert revertido com delete
  - `{ kind: 'restore_row'; table: string; id: string; snapshot: Record<string, unknown> }` — update revertido com snapshot prévio
  - `{ kind: 'composite'; ops: ReverseOpPayload[] }` — lista de ops (máximo 10 — guard para prevenir payloads descontrolados)

Restrições:
- Nenhum campo de `ToolExecutionContext` inclui prompt text ou content de mensagens (PII).
- `ToolDefinition` é interface, não classe — a implementação concreta fica nas stories de domínio (2.6, 2.7).

### AC3 — `ToolRegistry` com API idempotente

`packages/tools/src/registry.ts` exporta `ToolRegistry` (class ou factory singleton):

```ts
class ToolRegistry {
  register<I, O>(tool: ToolDefinition<I, O>): void;            // lança DuplicateToolError se name+domain já registado
  get(name: string): ToolDefinition<unknown, unknown>;          // lança ToolNotFoundError se ausente
  list(): ToolDefinition<unknown, unknown>[];                   // todos os tools registados
  getByDomain(domain: ToolDomain): ToolDefinition<unknown, unknown>[];  // filtra por domain
  getAnthropicToolDefinitions(): AnthropicToolDefinition[];     // formato { name, description, input_schema: {} } para passar ao Sonnet
  clear(): void;                                                // apenas para uso em testes (marcado @internal)
}

export const toolRegistry: ToolRegistry;  // singleton exportado
```

- `register()` é **idempotente se chamado com exactamente o mesmo objecto** (identidade por referência); lança `DuplicateToolError` se `name` colidir com tool diferente já registado.
- `get()` tem complexidade **O(1)** via Map interno.
- `getAnthropicToolDefinitions()` serializa `inputSchema` para JSON Schema via `zodToJsonSchema` de `zod-to-json-schema` (a adicionar como dep); retorna array no formato esperado por Anthropic SDK.

### AC4 — `serializeReverseOp` / `deserializeReverseOp`

`packages/tools/src/contracts.ts` exporta:
- `serializeReverseOp(op: ReverseOpPayload): string` — JSON.stringify com validação Zod prévia. Lança `ToolValidationError` se payload inválido.
- `deserializeReverseOp(raw: string): ReverseOpPayload` — JSON.parse + validação Zod. Lança `ToolValidationError` se inválido ou não parseável.
- Round-trip garantido: `deserializeReverseOp(serializeReverseOp(op))` produz objecto deep-equal ao original para todas as variantes (`delete_row`, `restore_row`, `composite`).

### AC5 — `executeAtomic` — atomicidade transaccional Drizzle

`packages/tools/src/atomic.ts` exporta:

```ts
export async function executeAtomic(
  tools: Array<{ definition: ToolDefinition<unknown, unknown>; input: unknown }>,
  ctx: ToolExecutionContext,
): Promise<AtomicResult>
```

Comportamento obrigatório:
- Abre uma transacção Drizzle (`ctx.db.transaction(async (tx) => { ... })`).
- Dentro da transacção, executa **cada tool sequencialmente** (não em paralelo — evitar race conditions em estado partilhado Postgres na mesma transacção).
- Para cada tool: valida `input` contra `inputSchema` (Zod parse), executa `tool.execute(input, ctxWithTx)`, obtém `output`, chama `tool.reverse(output, ctxWithTx)` para obter `ReverseOpPayload`.
- Persiste cada `ReverseOpPayload` em `agent_reverse_ops` com `expires_at = now() + interval '30 seconds'`, `household_id = ctx.householdId`, `agent_run_id = ctx.runId` — tudo dentro da mesma transacção.
- **Se qualquer tool falhar** (input inválido, execute lança excepção, output não valida): a transacção faz **ROLLBACK completo** — nenhuma entidade é criada, nenhuma `agent_reverse_ops` row persiste.
- Retorna `AtomicResult`:
  ```ts
  interface AtomicResult {
    success: true;
    results: Array<{ toolName: string; output: unknown; reverseOpId: string }>;
  }
  // ou em caso de falha parcial (não lançada — apenas quando o rollback ocorreu de forma controlada):
  interface AtomicFailure {
    success: false;
    failedToolName: string;
    error: ToolError;
    rolledBack: true;
  }
  ```
- `ctxWithTx` é um `ToolExecutionContext` com `db` substituído pelo cliente da transacção Drizzle (para que as tools escrevam dentro da mesma transacção).

### AC6 — Persistência `agent_reverse_ops` com `expires_at = now() + 30s`

Dentro de `executeAtomic`, cada `ReverseOpPayload` é persistido na tabela `agent_reverse_ops` (schema real de `@meu-jarvis/db`, migration `0005`) com:
- `id`: `gen_random_uuid()` (gerado antes do insert, incluído em `reverseOpId` do resultado).
- `agent_run_id`: `ctx.runId`.
- `household_id`: `ctx.householdId`.
- `reverse_op`: `serializeReverseOp(payload)` (JSONB).
- `expires_at`: `sql\`now() + interval '30 seconds'\`` via Drizzle raw SQL — não calculado em JavaScript para evitar drift de clock.
- `reverted_at`: `null`.

Insert usa o cliente Drizzle da transacção (`tx`) para garantir atomicidade com as tools.

### AC7 — RLS: `executeAtomic` respeita `household_id` (NFR5)

- `ToolExecutionContext.db` é sempre o cliente `getDb()` (com RLS activa via JWT) — **nunca** `getServiceDb()`.
- A transacção Drizzle corre com o mesmo role `authenticated` com claims do household.
- O campo `household_id` em `agent_reverse_ops` é obrigatório e populado com `ctx.householdId` — Postgres rejeitará qualquer tentativa de insert sem este campo.
- Teste unitário verifica que `executeAtomic` usa o `db` do contexto (nunca cria o seu próprio cliente).

### AC8 — Taxonomia de erros específica de tools

`packages/tools/src/errors.ts` exporta hierarquia:

```
ToolError (abstract base)
 ├─ ToolValidationError(toolName, field, message)     — input/output Zod inválido; NÃO retryable
 ├─ ToolExecutionError(toolName, cause)               — execute() lançou excepção; NÃO retryable (causa determinística)
 ├─ ToolTransactionError(cause)                       — falha da transacção Drizzle/Postgres; retryable
 ├─ ToolNotFoundError(name)                           — tool não registado; NÃO retryable
 ├─ DuplicateToolError(name, domain)                  — conflito no register(); NÃO retryable
 └─ ToolPlanGateError(toolName, requiredPlan, actualPlan)  — plano insuficiente; NÃO retryable
```

- Todos os erros têm `message` técnico (logs/Sentry) e `userMessage` PT-PT (mostrável ao utilizador em Story 2.6).
- `message` **nunca inclui** tool input content (PII).
- `ToolExecutionError.cause` guarda a excepção original mas é redactada de PII antes de qualquer log.

### AC9 — OTel tracing whitelist para tool execution (zero payload em spans)

`packages/tools/src/tracing.ts` exporta:
- `withToolSpan(toolName, domain, fn)` — cria span `agent.tool.call` com atributos whitelist:
  - `tool.name` (string)
  - `tool.domain` (string)
  - `tool.duration_ms` (number, post-call)
  - `tool.success` (boolean)
  - `tool.household_hash` (sha256 do householdId — via `hashForCorrelation`)
  - `tool.trace_id` (string)
- `withAtomicSpan(runId, toolCount, fn)` — span `agent.tool.atomic` para o `executeAtomic` completo com:
  - `tool.atomic.tool_count` (number)
  - `tool.atomic.run_id` (string)
  - `tool.atomic.success` (boolean)
  - `tool.atomic.rolled_back` (boolean — true em falha parcial)
- **NUNCA** incluir: tool inputs, tool outputs, prompt content, snapshot data de `restore_row`.

### AC10 — PII redaction nos tool inputs antes de log

`packages/tools/src/atomic.ts` e `src/registry.ts` utilizam `redactToolInputForLog(toolName, input)`:
- Helper interno que retorna `{ toolName, inputRedacted: true }` — nunca serializa o input em logs.
- Logs de debug usam apenas: `toolName`, `domain`, `householdHash`, `traceId`, `durationMs`, `success`.
- Compatible com `PII_REDACT_PATHS` de `@meu-jarvis/observability` (não redefinir, reutilizar onde aplicável).

### AC11 — Cobertura de testes ≥ 30 (mocks Vitest, sem DB real)

`packages/tools/src/__tests__/` cobre:

| Ficheiro | Mínimo | Cobertura |
|---------|--------|-----------|
| `contracts.test.ts` | 6 | ReverseOpPayload variantes + serializeReverseOp round-trip (delete_row, restore_row, composite) + guard composite.ops.length > 10 |
| `registry.test.ts` | 8 | register/get/list/getByDomain/getAnthropicToolDefinitions happy-path + DuplicateToolError colisão nome + ToolNotFoundError get ausente + clear() reset |
| `atomic.test.ts` | 12 | happy-path 1 tool + happy-path 3 tools + rollback quando tool 2 falha (tx.rollback chamado) + rollback quando input inválido + reverse_op insert correctamente (mock Drizzle tx) + expires_at usa SQL (não JS Date) + ToolExecutionContext.db nunca substituído internamente + AtomicResult structure + ToolPlanGateError passa de ToolError para callers |
| `errors.test.ts` | 6 | instanciação cada classe + retryable flags + PT-PT userMessage em todas + message não contém content de input |
| `tracing.test.ts` | 5 | withToolSpan atributos whitelist + withAtomicSpan atributos + prompt/input NÃO em atributos + tool.household_hash não é reversível + span status ERROR em falha |
| `integration/atomic-rls.test.ts` | opcional (+10 se 1.4 disponível) | Testcontainers Postgres efémero — executeAtomic persiste agent_reverse_ops com household_id correcto + cross-household blocked por RLS |

- **NENHUM teste faz call real** a Anthropic, OpenAI ou Supabase production.
- Mock Drizzle: a transacção é mockada com `vi.fn()` que expõe `tx.insert`, `tx.select`, `tx.rollback`.

### AC12 — JSDoc + README do package

- Todas as interfaces e funções públicas em `src/contracts.ts`, `src/registry.ts`, `src/atomic.ts` têm JSDoc com:
  - `@param`, `@returns` quando não óbvio.
  - `@throws` para cada tipo de erro que pode ser lançado.
  - `Trace:` indicando o FR/NFR/AC de rastreabilidade.
- `packages/tools/README.md` (ou `PACKAGE.md`) com: propósito, exports principais, como usar `executeAtomic`, exemplo mínimo de mock tool para testes.
- Gates verdes em CI:
  - `pnpm typecheck` passa (todos os packages, incluindo `@meu-jarvis/tools`).
  - `pnpm lint` passa com `--max-warnings=0`.
  - `pnpm test` passa (todos os packages; novos tests ≥ 30 do tools package).
  - `pnpm build` passa (Next.js build inclui `@meu-jarvis/tools` via `transpilePackages`).
  - `pnpm check:rls` mantém-se exit 0 (invariante NFR5 preservada — esta story não cria tabelas; usa `agent_reverse_ops` que já existe).

---

## Trace (Article IV — No Invention)

| AC | Rastreabilidade |
|----|----------------|
| AC1 | Architecture §4.3 (`packages/agent/src/tools/registry.ts` referenciado); padrão source-only confirmado em Story 2.2 AC1 |
| AC2 | Architecture §4.3 (`ToolDefinition<I,O>` — interface completa definida na architecture); FR4 (preview), FR6 (reverse/undo) |
| AC3 | Architecture §4.3 (`toolRegistry` como single source of truth, `getAnthropicToolDefinitions` implícito no tool_calls pattern); NFR11 (O(1) lookup) |
| AC4 | Architecture §4.5 (`reverse_op jsonb` — serialização/deserialização do payload declarativo); FR6 (undo 30s) |
| AC5 | Architecture §4.3 ("`execute` corre dentro de transacção Pg"); FR2 (atomicidade multi-intent); FR6 (reverse_op por execute) |
| AC6 | Architecture §4.5 (`expires_at = now() + interval '30 seconds'`); db-schema.md §4.4 (`agent_reverse_ops.expires_at DEFAULT now() + interval '30 seconds'`) |
| AC7 | NFR5 (RLS activa em TODAS as tabelas `household_id`); Architecture §3.2 (`getDb()` — cliente authenticated com RLS) |
| AC8 | Story 2.2 AC7 (pattern de hierarquia tipada com `userMessage` PT-PT); NFR12 (logs sem PII) |
| AC9 | Architecture §9.3 (OTel tracing, PII redaction); Story 2.2 AC8 (pattern `withProviderSpan` e whitelist attributes) |
| AC10 | NFR12 (logs sem PII); Architecture §9.3 (`PII_REDACT_PATHS`, redactProviderPayload pattern) |
| AC11 | Story 2.2 AC10 (pattern de cobertura ≥30 com mocks); NFR16 (cobertura de testes); Story 1.4 (Testcontainers disponível para bonus) |
| AC12 | PRD NFR19 (qualidade de código); Story 2.2 AC11-12 (COMPLIANCE.md + gates CI como padrão) |

---

## Tasks / Subtasks

- [x] **Task 1 — Setup do package** (AC1)
  - [x] 1.1 Criar `packages/tools/package.json` source-only; deps: `zod ^3.23.8`, `zod-to-json-schema ^3.23.x`, `@meu-jarvis/db: workspace:*`, `@meu-jarvis/observability: workspace:*`, `@opentelemetry/api`; devDeps: `vitest`, `typescript`, `@types/node`
  - [x] 1.2 Criar `packages/tools/tsconfig.json` extends `tsconfig.base.json` + `paths: { "@/*": ["./src/*"] }`
  - [x] 1.3 Criar estrutura de directórios `src/` completa (contracts.ts, registry.ts, atomic.ts, errors.ts, tracing.ts, index.ts, `__fixtures__/`, `__tests__/`)
  - [x] 1.4 Criar `vitest.config.ts` com `env: 'node'`, `globals: true`, alias `@`
  - [x] 1.5 Adicionar `"@meu-jarvis/tools": "workspace:*"` a `apps/web/package.json`
  - [x] 1.6 Adicionar `'@meu-jarvis/tools'` ao array `transpilePackages` em `apps/web/next.config.ts`
  - [x] 1.7 Executar `pnpm install` — validar sem erros

- [ ] **Task 2 — Contratos e tipos** (AC2, AC4)
  - [ ] 2.1 `src/contracts.ts`: definir `ToolDomain` enum + `PlanTier` enum + `ToolExecutionContext` interface (importar `DrizzleDbClient` de `@meu-jarvis/db`)
  - [ ] 2.2 `src/contracts.ts`: definir `ReverseOpPayload` discriminated union (3 variantes: `delete_row`, `restore_row`, `composite` com guard max 10 ops)
  - [ ] 2.3 `src/contracts.ts`: definir interface `ToolDefinition<I, O>` com todos os campos obrigatórios e opcionais (AC2)
  - [ ] 2.4 `src/contracts.ts`: implementar `serializeReverseOp(op)` + `deserializeReverseOp(raw)` com validação Zod
  - [ ] 2.5 `src/contracts.ts`: exportar `AtomicResult` e `AtomicFailure` interfaces
  - [ ] 2.6 `__tests__/contracts.test.ts` (≥6 testes): round-trip das 3 variantes + guard composite.ops.length > 10 lança ToolValidationError + deserialize de JSON inválido lança ToolValidationError

- [ ] **Task 3 — Errors hierarchy** (AC8)
  - [ ] 3.1 `src/errors.ts`: classe abstracta `ToolError extends Error` com `toolName?: string`, `userMessage: string` (PT-PT), `retryable: boolean`
  - [ ] 3.2 `src/errors.ts`: 6 subclasses — `ToolValidationError`, `ToolExecutionError`, `ToolTransactionError`, `ToolNotFoundError`, `DuplicateToolError`, `ToolPlanGateError`
  - [ ] 3.3 Garantir `message` técnico nunca inclui input content; `userMessage` sempre PT-PT, neutro de implementação
  - [ ] 3.4 `__tests__/errors.test.ts` (≥6 testes): instanciação + retryable flags + PT-PT userMessage em todas + message não contém simulação de conteúdo de input

- [ ] **Task 4 — Tracing helper** (AC9)
  - [ ] 4.1 `src/tracing.ts`: importar `withSpan`, `annotateSpan`, `recordSpanError`, `hashForCorrelation` de `@meu-jarvis/observability`
  - [ ] 4.2 `src/tracing.ts`: implementar `withToolSpan(toolName, domain, ctx, fn)` com 6 atributos whitelist (AC9)
  - [ ] 4.3 `src/tracing.ts`: implementar `withAtomicSpan(runId, toolCount, fn)` com 4 atributos whitelist (AC9)
  - [ ] 4.4 Definir e exportar `TOOL_SPAN_ATTRIBUTE_KEYS` (lista imutável de keys permitidas)
  - [ ] 4.5 `__tests__/tracing.test.ts` (≥5 testes): atributos whitelist correctos + inputs/outputs NÃO em atributos + household_hash usa hashForCorrelation (não expõe householdId raw) + span status ERROR em falha

- [ ] **Task 5 — ToolRegistry** (AC3)
  - [ ] 5.1 `src/registry.ts`: implementar `ToolRegistry` class com Map interno `name → ToolDefinition`
  - [ ] 5.2 Implementar `register()` com guard idempotência por referência + `DuplicateToolError` em colisão de nome
  - [ ] 5.3 Implementar `get()` — O(1) via Map — + `ToolNotFoundError` se ausente
  - [ ] 5.4 Implementar `list()` e `getByDomain()` (filter sobre Map.values())
  - [ ] 5.5 Implementar `getAnthropicToolDefinitions()` — usa `zod-to-json-schema` para serializar `inputSchema`
  - [ ] 5.6 Implementar `clear()` marcado com `/** @internal — apenas para testes */`
  - [ ] 5.7 Exportar singleton `toolRegistry = new ToolRegistry()`
  - [ ] 5.8 `__tests__/registry.test.ts` (≥8 testes): register/get/list/getByDomain happy-path + DuplicateToolError + ToolNotFoundError + getAnthropicToolDefinitions formato correcto + clear() reseta o state

- [ ] **Task 6 — Mock tools para testes** (AC11 prep)
  - [ ] 6.1 `src/__fixtures__/mock-tools.ts`: definir 3 tools de teste internas (nunca exportadas pelo barrel principal):
    - `echoTool`: aceita `{ text: string }`, devolve `{ echoed: string }`, reverse: `delete_row`
    - `failTool`: aceita `{ shouldFail: true }`, sempre lança `ToolExecutionError`, reverse não chega a ser chamada
    - `slowTool`: aceita `{ delayMs: number }`, devolve `{ done: true }` após mock delay, reverse: `delete_row`
  - [ ] 6.2 Confirmar que `src/__fixtures__/` **não** é re-exportado por `src/index.ts`

- [ ] **Task 7 — `executeAtomic`** (AC5, AC6, AC7)
  - [ ] 7.1 `src/atomic.ts`: implementar `executeAtomic(tools, ctx)` com abertura de transacção Drizzle (`ctx.db.transaction`)
  - [ ] 7.2 Dentro da transacção: para cada tool — validar input (Zod parse, lança ToolValidationError), criar `ctxWithTx` com db = tx, chamar `execute(input, ctxWithTx)`, validar output (outputSchema), chamar `reverse(output, ctxWithTx)` para obter ReverseOpPayload
  - [ ] 7.3 Persistir cada `ReverseOpPayload` em `agent_reverse_ops` dentro da transacção com `expires_at = sql\`now() + interval '30 seconds'\`` (SQL puro, não JS Date)
  - [ ] 7.4 Em caso de falha de qualquer tool: a transacção faz rollback automático (propriedade do Drizzle `transaction()`) + retornar `AtomicFailure`
  - [ ] 7.5 Em caso de sucesso de todos: retornar `AtomicResult` com resultados individuais
  - [ ] 7.6 Envolver `executeAtomic` em `withAtomicSpan` (Task 4)
  - [ ] 7.7 `__tests__/atomic.test.ts` (≥12 testes): todos os cenários do AC11 (ver tabela)

- [ ] **Task 8 — Barrel público** (AC1, AC2, AC3)
  - [ ] 8.1 `src/index.ts`: exportar contratos públicos (`ToolDefinition`, `ToolDomain`, `PlanTier`, `ToolExecutionContext`, `ReverseOpPayload`, `AtomicResult`, `AtomicFailure`, `serializeReverseOp`, `deserializeReverseOp`)
  - [ ] 8.2 Exportar `toolRegistry` singleton + tipo `ToolRegistry`
  - [ ] 8.3 Exportar `executeAtomic`
  - [ ] 8.4 Exportar hierarquia de erros (`ToolError` + 6 subclasses)
  - [ ] 8.5 Exportar `TOOL_SPAN_ATTRIBUTE_KEYS` (para Story 2.5 configurar Grafana dashboards)
  - [ ] 8.6 **NÃO exportar**: `withToolSpan`, `withAtomicSpan`, `redactToolInputForLog`, `__fixtures__/*`, `clear()` (internos ao package)

- [ ] **Task 9 — Documentação** (AC12)
  - [ ] 9.1 JSDoc completo em todas as interfaces e funções públicas (`@param`, `@returns`, `@throws`, `Trace:`) em `contracts.ts`, `registry.ts`, `atomic.ts`
  - [ ] 9.2 `packages/tools/README.md`: propósito, exports principais, exemplo mínimo de mock tool para testes de consumers, nota sobre `__fixtures__/` ser interno

- [ ] **Task 10 — Validação final** (AC12)
  - [ ] 10.1 `pnpm install` → sem erros
  - [ ] 10.2 `pnpm typecheck` → todos os packages exit 0
  - [ ] 10.3 `pnpm lint` → todos os packages exit 0 (`--max-warnings=0`)
  - [ ] 10.4 `pnpm test` → exit 0; confirmar ≥30 novos testes em `packages/tools`
  - [ ] 10.5 `pnpm build` → exit 0 (Next.js inclui `@meu-jarvis/tools` via transpilePackages)
  - [ ] 10.6 `pnpm check:rls` → exit 0 (invariante: mesmas 26 tabelas / 104 policies — esta story não cria tabelas)

---

## Dev Notes

### Estrutura do monorepo relevante

```
packages/
├── agent/                       # @meu-jarvis/agent (Story 2.2 Done)
│   └── src/
│       ├── contracts.ts         # MinimalToolDefinition — placeholder substituído por ToolDefinition<I,O> desta story
│       ├── errors.ts            # ProviderError hierarchy — padrão a replicar em ToolError
│       ├── tracing.ts           # withProviderSpan — padrão a replicar em withToolSpan
│       └── redaction.ts        # redactProviderPayload — padrão a reutilizar
├── db/                          # @meu-jarvis/db
│   └── src/schema/
│       └── agent.ts             # agent_reverse_ops table (migration 0005 aplicada)
├── observability/               # @meu-jarvis/observability (Story 1.7 Done)
│   └── src/index.ts             # logger, withSpan, annotateSpan, hashForCorrelation
└── tools/                       # @meu-jarvis/tools — A CRIAR NESTA STORY
    └── src/
        ├── contracts.ts         # ToolDefinition, ReverseOpPayload, ToolExecutionContext
        ├── registry.ts          # ToolRegistry singleton
        ├── atomic.ts            # executeAtomic
        ├── errors.ts            # ToolError hierarchy
        ├── tracing.ts           # withToolSpan, withAtomicSpan
        ├── index.ts             # barrel público
        ├── __fixtures__/        # mock tools (apenas para testes)
        └── __tests__/           # testes unitários
```

### Schema real (agent_reverse_ops)

A tabela já existe em produção desde migration `0005` (Story 2.1). O @dev **não deve criar** nova migration — apenas referenciar a tabela existente via Drizzle.

Campos relevantes para `executeAtomic`:

```ts
// packages/db/src/schema/agent.ts — já existente
agentReverseOps: {
  id: uuid (pk, gen_random_uuid),
  agentRunId: uuid (fk agent_runs.id),
  householdId: uuid (fk households.id, NOT NULL),
  reverseOp: jsonb (NOT NULL),  // ReverseOpPayload serializado
  expiresAt: timestamptz (NOT NULL, DEFAULT now() + interval '30 seconds'),
  revertedAt: timestamptz (NULL),
  createdAt: timestamptz (DEFAULT now()),
}
```

### Transacção Drizzle — padrão correcto

```ts
// Uso correcto dentro de executeAtomic:
await ctx.db.transaction(async (tx) => {
  const ctxWithTx = { ...ctx, db: tx as typeof ctx.db };
  // ... executa tools e inserts dentro de tx
});
```

**NUNCA** usar `getServiceDb()` dentro de `executeAtomic` — tools escrevem com RLS activa (`authenticated`).

### `expires_at` com SQL puro (AC6 — crítico)

```ts
// CORRECTO — evita drift de clock JS vs Postgres:
await tx.insert(agentReverseOps).values({
  expiresAt: sql`now() + interval '30 seconds'`,
  // ...
});

// ERRADO — clock JS pode divergir do Postgres:
expiresAt: new Date(Date.now() + 30_000),
```

### `zod-to-json-schema` — serialização para Anthropic

A função `getAnthropicToolDefinitions()` usa a lib `zod-to-json-schema`:

```ts
import { zodToJsonSchema } from 'zod-to-json-schema';

getAnthropicToolDefinitions() {
  return Array.from(this.#tools.values()).map((tool) => ({
    name: tool.name,
    description: tool.description,
    input_schema: zodToJsonSchema(tool.inputSchema, { target: 'jsonSchema7' }),
  }));
}
```

O output é o formato que `AnthropicProvider.complete()` já aceita via `MinimalToolDefinition` (Story 2.2 AC2).

### `MinimalToolDefinition` em `@meu-jarvis/agent` (carry-over 2.2)

A Story 2.2 definiu `MinimalToolDefinitionSchema` como placeholder explícito: `{ name, description, input_schema: Record<string,unknown> }`. Após esta story, o `ToolDefinition<I,O>` desta story é a forma definitiva. A Story 2.5 (Planner+Executor) substituirá o uso de `MinimalToolDefinition` por `ToolDefinition` via `getAnthropicToolDefinitions()`. Esta story **não modifica** o package `@meu-jarvis/agent`.

### Carry-over naming de 2.1 (Article IV)

As tabelas do schema agent usam os nomes reais:
- `intent_classifications` — **NÃO** `agent_intents`
- `agent_runs.tool_calls` (JSONB) — **NÃO** tabela separada
- `agent_reverse_ops` — nome real da tabela de undo

### Imports absolutos obrigatórios (Article VI / NFR19)

```ts
// Correcto:
import { agentReverseOps } from '@meu-jarvis/db';
import { withSpan } from '@meu-jarvis/observability';
import { ToolError } from '@/errors';

// ERRADO:
import { agentReverseOps } from '../../db/src/schema/agent';
import { ToolError } from '../errors';
```

### Padrão de erro com `userMessage` PT-PT (carry-over 2.2)

```ts
// Seguir o mesmo padrão de errors.ts em @meu-jarvis/agent:
export class ToolValidationError extends ToolError {
  constructor(toolName: string, field: string, detail: string) {
    super(
      `Tool '${toolName}' validation failed on field '${field}': ${detail}`,  // técnico
      toolName,
      'Os dados fornecidos ao agente são inválidos. Tenta formular o pedido de forma diferente.',  // PT-PT para utilizador
      false,  // retryable
    );
  }
}
```

---

## Testing

### Framework e ambiente

- **Vitest** com `env: 'node'`, `globals: true` — consistente com `packages/agent` e `packages/db`.
- **Mocks**: Drizzle `db.transaction` mockado com `vi.fn(async (fn) => fn(mockTx))` onde `mockTx` expõe `insert`, `select`.
- **Sem calls reais** a Anthropic, OpenAI, Supabase production.

### Testes obrigatórios (≥30 total)

| Ficheiro | Mínimo | Cenários chave |
|---------|--------|----------------|
| `contracts.test.ts` | 6 | Round-trip 3 variantes ReverseOpPayload; guard composite.ops.length > 10; deserialize JSON inválido |
| `registry.test.ts` | 8 | register/get/list/getByDomain; DuplicateToolError; ToolNotFoundError; getAnthropicToolDefinitions formato; clear() |
| `atomic.test.ts` | 12 | 1 tool sucesso; 3 tools sucesso; rollback quando tool 2 falha; rollback input inválido; insert agent_reverse_ops com dados correctos; expires_at é SQL (não JS Date); ctxWithTx.db é tx (não ctx.db original); AtomicResult structure; ToolPlanGateError propagado |
| `errors.test.ts` | 6 | Cada classe instanciada; retryable correcto; userMessage PT-PT; message sem content |
| `tracing.test.ts` | 5 | Whitelist atributos; inputs não em spans; household_hash via hashForCorrelation; span ERROR em falha |

### Testes bónus (opcional, se Story 1.4 disponível)

`__tests__/integration/atomic-rls.test.ts` com Testcontainers Postgres efémero:
- `executeAtomic` persiste `agent_reverse_ops` com `household_id` correcto.
- Attempt de acesso cross-household bloqueado por RLS (query com household_id diferente retorna empty).
- Confirmar `expires_at` está ~30s no futuro (±5s de tolerância de clock).

---

## Risks & Mitigations

| Risco | Impacto | Mitigação |
|-------|---------|-----------|
| `executeAtomic` com reverse_ops órfãos (rollback da transacção não apaga rows que ainda não existem — elas não são criadas) | Alto | Rollback Drizzle é automático — todos os inserts (entidades + agent_reverse_ops) desfazem juntos. Coberto por teste `atomic.test.ts` "rollback apaga tudo". |
| Tool input PII vazar para logs de erro | Alto | `ToolExecutionError.cause` é redactado antes de log; `redactToolInputForLog` retorna apenas toolName+redacted flag. Coberto por `errors.test.ts`. |
| `register()` aceitar tool duplicado silenciosamente | Médio | `DuplicateToolError` lançado sempre que colide por nome (mesmo domain diferente). `clear()` só em testes. |
| `expires_at` calculado em JS diverge do relógio Postgres | Médio | Uso obrigatório de `sql\`now() + interval '30 seconds'\``. Coberto por `atomic.test.ts` "expires_at é SQL". |
| `getDb()` interno (`getServiceDb()`) substituir `ctx.db` por engano | Alto | `executeAtomic` recebe `ctx.db` e nunca cria novo cliente. Coberto por teste "ctxWithTx.db é tx". |
| Mock tools vazar para produção via barrel `index.ts` | Médio | `__fixtures__/` não é re-exportado por `index.ts`. Coberto por inspecção do barrel + lint (sem import de `__fixtures__` fora de `__tests__`). |
| Cleanup de `agent_reverse_ops` expirados não implementado | Baixo (esta story) | Out-of-scope — job Inngest a implementar em story futura. TODO documentado em README e como comentário em `atomic.ts`. |

---

## Constitution Compliance Notes

| Artigo | Principio | Aplicação nesta story |
|--------|-----------|----------------------|
| Article I — CLI First | Toda execução via CLI | `executeAtomic` é lógica pura no package — invocada via CLI/endpoint, nunca pressupõe UI |
| Article IV — No Invention | Todo AC rastreável | Ver coluna Trace na secção "Trace (Article IV)" — 12/12 ACs rastreados |
| Article V — Quality First | Gates CI obrigatórios | `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm build`, `pnpm check:rls` — todos AC12 |
| Article VI — Absolute Imports | Sem imports relativos | `@meu-jarvis/db`, `@meu-jarvis/observability`, `@/...` — nunca `../../` |
| NFR5 (RLS) | `household_id` em agent_reverse_ops | `executeAtomic` usa `ctx.db` (getDb, authenticated, RLS activa); `household_id` obrigatório no insert |
| NFR11 (Performance) | `get()` O(1), overhead documentado | Map interno; `executeAtomic` overhead = 1 transacção Drizzle + N inserts (documentado em README) |
| NFR12 (PII) | Zero PII em logs/spans | `redactToolInputForLog`, whitelist OTel attributes, `userMessage` separado de `message` |
| NFR19 (Code Quality) | TypeScript strict, PT-PT comments | `noUncheckedIndexedAccess`, sem `any`, comments e userMessages em PT-PT |

---

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-05-08 | 1.0 | Draft inicial — Tool Registry Foundation | River (@sm) |
| 2026-05-08 | 1.1 | `@po` validation GO 9.7/10 HIGH (10/10 pontos: 9 a 1.0, 1 a 0.7 por ambiguidade não-blocking de `ToolPlanGateError`); 3 AUTO-DECISIONS aceites; 3 suggested improvements non-blocking documentados; status Draft → Ready; @dev *develop 2.3 unblocked | Pax (@po) |

---

## Dev Agent Record

*(Preenchido pelo @dev durante implementação)*

### Agent Model Used

—

### Debug Log References

—

### Completion Notes

—

### File List

*(Criados/modificados durante implementação)*

---

## PO Validation Results

**Validador:** @aiox-po (Pax)
**Data:** 2026-05-08
**Verdict:** GO
**Score:** 9.7/10
**Confidence:** HIGH

### Score breakdown

| # | Ponto | Score | Notas |
|---|-------|-------|-------|
| 1 | Story format compliance | 1/1 | Segue o template oficial AIOX (status, narrativa As/I/So, ACs numerados, tasks com checkboxes, dev notes, testing, change log, dev agent record vazio, QA results placeholder). Estrutura idêntica à 2.2 (gold standard 9.4/10). |
| 2 | Acceptance Criteria specificity | 1/1 | 12 ACs, todos testáveis e verificáveis. Cada AC define artefactos concretos (ficheiros, exports, comportamento esperado). PT-PT em todo o texto. AC11 inclui tabela explícita de mínimos por ficheiro de teste. |
| 3 | Tasks granularity | 1/1 | 10 tasks com ~37 subtasks accionáveis. Cada subtask ≤2h estimadas. Mapeamento explícito Task → AC documentado. Task 6 (mock tools fixtures) é genuinamente útil para Tasks 7 (executeAtomic) e Story 2.5 futura. |
| 4 | Architecture references traceability | 1/1 | Secção dedicada "Trace (Article IV)" com 12/12 ACs rastreados a Architecture §3.2/§4.3/§4.5/§9.3, db-schema §4.4, PRD FR4/FR6/NFR5/NFR11/NFR12/NFR16/NFR19, e Story 2.2 ACs (carry-over de pattern). Verificado contra `docs/architecture.md` linhas 385-405 (reverse_op spec) e `packages/db/src/schema/agent.ts` linhas 188-218 (tabela real). Article IV cumprido. |
| 5 | Testing strategy completeness | 1/1 | Target ≥30 explícito, divisão obrigatórios vs opcionais (Testcontainers bonus +10 se 1.4 disponível). Distinção unit (mocks) vs integration (Testcontainers). Cenários críticos enumerados (rollback, expires_at SQL, ctxWithTx.db = tx, span attributes whitelist). Mock strategy explícita (`vi.mock`, `mockTx.insert/select/rollback`). |
| 6 | Dependencies & prerequisites listed | 1/1 | Secção "Dependências" lista 3 stories Done (2.2, 1.3, 1.7), 1 opcional não-bloqueante (1.4), 2 stories downstream (2.5, 2.6). Tech deps listadas (zod, zod-to-json-schema, @opentelemetry/api). Bloqueadores externos EB1/EB2 explicitamente classificados como NÃO-bloqueantes (story 100% mockável). |
| 7 | Risks & mitigations identified | 1/1 | 7 riscos com mitigations específicas (não 3 mínimos exigidos). Mitigations rastreadas a testes obrigatórios. Risco "cleanup `agent_reverse_ops` expirados não implementado" honestamente classificado out-of-scope com TODO documentado. |
| 8 | Constitution compliance | 1/1 | Secção "Constitution Compliance Notes" cobre Articles I, IV, V, VI + NFR5/NFR11/NFR12/NFR19. Article IV (No Invention): nomes reais de tabelas (`intent_classifications`, `agent_reverse_ops`) confirmados. Article VI (absolute imports): exemplos correctos vs errados em Dev Notes. NFR5: `ctx.db` = `getDb()` (authenticated, RLS activa) explícito em AC7 + teste obrigatório. |
| 9 | Scope discipline | 0.7/1 | Foundation puramente — não inclui tools reais (correctamente deferido a 2.6/2.7). Out-of-scope explícito: cleanup job Inngest. **Pequena ambiguidade detectada:** AC8 lista `ToolPlanGateError` na taxonomia mas a story não define onde/quando este erro é lançado (gate de plano não está coberto por nenhum AC nem teste explícito além de "passa de ToolError para callers" em atomic.test.ts). Ver suggested improvement #1. |
| 10 | PT-PT consistency | 1/1 | Zero ocorrências de PT-BR (`grep` sobre `\busar\b\|\bdeletar\b\|\bsetar\b` retorna 0 matches). Comments, error messages, AC text, task descriptions todos em PT-PT. Padrão `userMessage` PT-PT vs `message` técnico explícito (AC8 + Dev Notes). |

**Subtotal pontos 1-10:** 9.7/10
**Score total:** **9.7/10** (GO threshold ≥7)

### AUTO-DECISIONS review

- **AD1** Package name `@meu-jarvis/tools` (não `@meu-jarvis/tool-registry`): **ACCEPT** — Consistente com convenção de nomenclatura do monorepo (`@meu-jarvis/agent`, `@meu-jarvis/db`, `@meu-jarvis/observability` — substantivos curtos por domínio). Mais curto e o conteúdo do package vai além do registry (inclui `executeAtomic`, errors, tracing). Decisão sólida.
- **AD2** `composite` ops máximo 10 (guard payload-level): **ACCEPT** — Não conflitua com o limite de 5 intents do Epic 2 (esse é por *prompt*, este é por *payload nested ops*). Um `composite` reverse_op pode legitimamente ter ~3-5 ops (ex: `restore_row` parent + `delete_row` child entities), e 10 é guarda razoável contra abuse. Mitiga o risco de payload jsonb descontrolado em `agent_reverse_ops`. Coberto por teste obrigatório em `contracts.test.ts` (≥6 testes incluem o guard). Bem fundamentado.
- **AD3** `zod-to-json-schema ^3.23.x` como dep: **ACCEPT** — Lib é o standard ecosystem para conversão Zod→JSON Schema 7 (formato que Anthropic SDK aceita em `tools[].input_schema`). Alternativas consideradas: (a) escrever conversor manual — viola KISS e duplica esforço; (b) `@anthropic-ai/sdk` já tem helper interno — não, o SDK aceita JSON Schema mas não converte de Zod. `zod-to-json-schema` é maintained pela comunidade Zod e usada por outros frameworks (tRPC, etc.). Bundle size ~30KB é aceitável (package source-only, sem build). Decisão correcta.

### Required fixes (blocking)

Nenhum.

### Suggested improvements (non-blocking)

1. **`ToolPlanGateError` — definir locus de invocação.** AC8 lista o erro na taxonomia mas nenhum AC define explicitamente *quando* `executeAtomic` (ou outra função desta story) deve lançá-lo. Se o gate de `requiredPlan` é responsabilidade do *consumidor* (Story 2.5 Planner+Executor antes de chamar `executeAtomic`), então a definição da classe nesta story é correcta mas a story deveria mencionar isso explicitamente em Dev Notes (linha tipo: *"`ToolPlanGateError` é definido aqui mas lançado pelo Planner em 2.5 — esta story só fornece a classe e teste de instanciação"*). Alternativa: adicionar AC opcional para um helper `assertPlanAllowed(tool, plan)` em `registry.ts` que o Planner use. **Não bloqueia a story** — `@dev` pode resolver no Dev Notes durante implementação ou esclarecer com `@architect` no gate. Não-blocking porque a hierarquia tipada está completa e o uso fica óbvio.

2. **Deserialize JSON inválido — variante `composite` aninhado profundo.** AC4 garante round-trip mas o teste `contracts.test.ts` ≥6 não menciona explicitamente o caso `composite` aninhado dentro de `composite` (recursão). O guard de `max 10 ops` aplica-se a *cada nível* ou só ao topo? Sugestão: clarificar em Dev Notes ou subtask 2.6 que o guard é *aplicado em cada nível recursivamente* (ou *apenas top-level* — qualquer escolha é defensável, mas precisa estar documentada para `@architect` validar no gate). Suggested test adicional: `composite` com 1 nested `composite` válido (sucesso) + `composite` com nested de 11 ops (falha). **Não-blocking** — pode emergir como nit no gate `@architect` similar ao TEST-001-NB de 2.2.

3. **Idempotência de `register()` por referência — clarificar semântica em testes.** AC3 diz *"`register()` é idempotente se chamado com exactamente o mesmo objecto (identidade por referência)"*. Mas o teste `registry.test.ts` ≥8 não enumera explicitamente esse caso (foco está em "DuplicateToolError em colisão de nome"). Sugestão: adicionar 1 teste em `registry.test.ts` cobrindo `registry.register(toolA); registry.register(toolA);` (mesma referência) → não lança. Permite cobertura completa do contrato AC3. **Não-blocking** — `@dev` pode adicionar durante implementação para chegar aos ≥8 testes mínimos.

### Final verdict

**GO.** Story 2.3 atinge 9.7/10 e está pronta para `@dev *develop`. O draft do River (@sm) demonstra qualidade equivalente ou superior à Story 2.2 (que entregou 9.4/10 no gate `@architect`) — todos os 12 ACs são testáveis com mocks Vitest, todas as 3 AUTO-DECISIONS são sólidas e justificadas, a rastreabilidade Article IV é completa (12/12), e a story respeita disciplina de scope (foundation, sem invenção de tools de domínio). Os 3 suggested improvements são refinamentos para `@dev`/`@architect` decidirem durante implementação/gate, não fixes blocking. Story 100% mockável (independente de EB1/EB2). Próxima acção: `@dev *develop 2.3`.

---

## QA Results

*(Preenchido pelo @architect no gate)*

—
