# Story 1.5 — Supabase Auth + RLS Integration + custom_access_token_hook

**Status:** Ready
**Epic:** Epic 1 — Foundation & Multi-Tenant Core
**Owner:** @dev
**Estimate:** L
**Created:** 2026-05-04
**Updated:** 2026-05-04

---

## BLOQUEADORES EXTERNOS

> **AVISO:** Esta story tem duas dependências externas:
>
> 1. **Story 1.3** Done (Supabase com schema + RLS migrations aplicadas).
> 2. **Story 1.4** Done (suite de testes RLS validada).
> 3. **Supabase Dashboard — Auth Hook:** O `custom_access_token_hook` precisa de ser registado manualmente no Supabase Dashboard → Auth → Hooks. Não é possível fazer via Drizzle ou SQL — requer acesso ao Dashboard.
> 4. **Providers OAuth (opcional MVP):** Se Google/Apple OAuth for activado, precisa de `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET` (e equivalentes Apple). PRD FR24 menciona social login como opção — flagged para decisão do Eurico. Esta story apenas configura email+password; OAuth é extensão futura.

---

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["vitest", "typecheck", "e2e-playwright"]
```

---

## User Story

**As a** utilizador da plataforma Expressia,
**I want** conseguir registar-me com email+password, confirmar o email e receber um JWT que contém o meu `household_id` como custom claim,
**so that** todas as queries à base de dados são automaticamente filtradas pelo meu household via RLS, sem necessidade de passar `household_id` explicitamente em cada query.

---

## Dependências

- **DEPENDE de Story 1.1** (monorepo com Next.js 15 funcional).
- **DEPENDE de Story 1.3** (schema Supabase com `household_members` e helpers RLS aplicados).
- **DEPENDE de Story 1.4** (suite RLS validada — confirmar que Auth + RLS funciona correctamente).
- Story 1.6 (endpoint `/api/me`) depende desta story.

---

## Acceptance Criteria

- [ ] **AC1:** Package `packages/auth/` existe e exporta `createServerSupabaseClient()` e `createBrowserSupabaseClient()` usando `@supabase/ssr`.
- [ ] **AC2:** `apps/web/src/middleware.ts` implementa auth gate via Supabase: rotas sob `/(app)/*` requerem sessão válida; redireccionam para `/entrar` se não autenticado.
- [ ] **AC3:** Function SQL `custom_access_token_hook` está criada no schema (já incluída em `packages/db/migrations/`), registada no Supabase Dashboard → Auth → Hooks, e ao fazer login, o JWT resultante contém o campo `household_id` com o `household_id` default do utilizador.
- [ ] **AC4:** Ao registar novo utilizador (email+password), é criado automaticamente um household default e o utilizador fica como `owner` na tabela `household_members`. Isto deve acontecer via Supabase Database Webhook ou trigger SQL after user insert.
- [ ] **AC5:** `pnpm lint` e `pnpm typecheck` em `packages/auth/` e em `apps/web/` passam sem erros.
- [ ] **AC6:** Teste de integração em `packages/db-test/` verifica que um JWT com `household_id` correcto permite SELECT na tabela `tasks` do household correspondente, e um JWT com `household_id` errado é bloqueado (RLS).
- [ ] **AC7:** Variáveis de ambiente necessárias documentadas e verificadas: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`.
- [ ] **AC8:** A lógica de refresh de sessão está implementada em middleware (Supabase SSR pattern — `updateSession()`).

---

## Trace (Article IV — No Invention)

| AC | Rastreabilidade |
|----|----------------|
| AC1 | Architecture §5.1 (Supabase Auth, ADR-002), `@supabase/ssr` |
| AC2 | Architecture §8.1 (route group `(app)/` auth-required), §7.2 (edge middleware) |
| AC3 | Architecture §5.2 (`custom_access_token_hook` SQL function), db-schema.md (migrations) |
| AC4 | PRD FR24 (registo + criação household), FR25 (utilizador pertence a household), Architecture §5.3 (household creation flow) |
| AC5 | PRD NFR17-19, CON5 |
| AC6 | Architecture §10.2 (RLS testing com JWT), NFR5 |
| AC7 | PRD NFR8 (secrets nunca em código) |
| AC8 | Architecture §5.1 (sessão JWT 1h, refresh 30d), `@supabase/ssr` pattern |

---

## Tasks

- [ ] **Task 1 — Criar package `packages/auth/` (AC1)**
  - [ ] 1.1 Criar `packages/auth/package.json` com nome `@meu-jarvis/auth`
  - [ ] 1.2 Instalar `@supabase/ssr`, `@supabase/supabase-js`
  - [ ] 1.3 Criar `packages/auth/src/server.ts` — `createServerSupabaseClient()` para Server Components e Route Handlers
  - [ ] 1.4 Criar `packages/auth/src/browser.ts` — `createBrowserSupabaseClient()` para Client Components
  - [ ] 1.5 Criar `packages/auth/src/index.ts` — barrel exports
  - [ ] 1.6 Criar `packages/auth/tsconfig.json` com `strict: true`

- [ ] **Task 2 — Middleware de auth em `apps/web/` (AC2, AC8)**
  - [ ] 2.1 Criar `apps/web/src/middleware.ts` com Supabase auth middleware
  - [ ] 2.2 Implementar `updateSession()` para refresh automático de sessão
  - [ ] 2.3 Configurar `matcher` para rotas protegidas: `/(app)/:path*`
  - [ ] 2.4 Redireccionamento para `/entrar` se sessão inválida ou expirada

- [ ] **Task 3 — `custom_access_token_hook` (AC3) [REQUER SUPABASE DASHBOARD]**
  - [x] 3.1 Verificar que a function SQL `public.custom_access_token_hook` existe nas migrations aplicadas — **CUMPRIDO PRE-EMPTIVELY (2026-05-06)**: gap descoberto durante prep da story (função era assumida mas nunca tinha sido materializada). Dara (@data-engineer) criou `packages/db/migrations/0002_auth_hook.sql` com a spec do architecture §5.2 (corrigida — `created_at` → `joined_at`) + grants `supabase_auth_admin`; aplicada na Supabase real (eu-west-1) em 495ms; smoke test verde. Verify script em `packages/db/src/scripts/verify-0002.ts`. Commit `d2cbf1f`.
  - [x] 3.2 Aceder a Supabase Dashboard → Authentication → Hooks — feito por Eurico
  - [x] 3.3 Registar `custom_access_token_hook` como "Customize Access Token (JWT) Claims" hook — **ENABLED** no Dashboard (schema `public`, function `custom_access_token_hook`, type Postgres function), confirmado por screenshot 2026-05-06
  - [ ] 3.4 Fazer login com utilizador de teste e verificar que o JWT contém `household_id` (via jwt.io ou endpoint `/api/me` da Story 1.6) — pendente para @dev (precisa de utilizador real com household membership; provavelmente integrado com Task 4 e 5)
  - [ ] 3.5 Documentar passos em `docs/runbooks/supabase-auth-setup.md` — pendente para @dev

- [ ] **Task 4 — Auto-criação de household no registo (AC4)**
  - [ ] 4.1 Criar trigger SQL ou Supabase Database Webhook que executa após `auth.users` insert
  - [ ] 4.2 A função deve: `INSERT INTO households (name, owner_id, currency, locale) VALUES ('Household de {username}', NEW.id, 'EUR', 'pt-PT')`
  - [ ] 4.3 Seguido de: `INSERT INTO household_members (household_id, user_id, role) VALUES (new_household_id, NEW.id, 'owner')`
  - [ ] 4.4 Seguido de: `INSERT INTO subscriptions (household_id, plan, status, trial_end) VALUES (new_household_id, 'familia', 'trialing', now() + 14 days)` (Architecture §6.4)
  - [ ] 4.5 Verificar que a trigger está activa e funciona com utilizador de teste

- [ ] **Task 5 — Páginas de auth em `apps/web/` (scaffolding básico) (AC2)**
  - [ ] 5.1 Criar `apps/web/src/app/(auth)/entrar/page.tsx` — formulário email+password PT-PT
  - [ ] 5.2 Criar `apps/web/src/app/(auth)/registar/page.tsx` — formulário registo PT-PT
  - [ ] 5.3 Criar `apps/web/src/app/(auth)/recuperar/page.tsx` — recuperação de password PT-PT
  - [ ] 5.4 Criar Server Actions para submit (não Route Handlers — conforme Architecture §7.1)
  - [ ] **Nota:** Esta story cria apenas scaffolding funcional — UX completa é Epic 6

- [ ] **Task 6 — Teste de integração JWT + RLS (AC6)**
  - [ ] 6.1 Adicionar teste em `packages/db-test/` que simula JWT com `household_id` correcto
  - [ ] 6.2 Verificar SELECT retorna dados do household correcto
  - [ ] 6.3 Simular JWT com `household_id` errado → SELECT retorna 0 rows

- [ ] **Task 7 — Verificação final (AC5, AC7)**
  - [ ] 7.1 `pnpm lint` sem erros
  - [ ] 7.2 `pnpm typecheck` sem erros
  - [ ] 7.3 Verificar variáveis de ambiente no `.env.local`

---

## File List

_(Preenchido pelo @dev durante implementação)_

**Ficheiros a criar:**
- `packages/auth/package.json`
- `packages/auth/tsconfig.json`
- `packages/auth/src/server.ts`
- `packages/auth/src/browser.ts`
- `packages/auth/src/index.ts`
- `apps/web/src/middleware.ts`
- `apps/web/src/app/(auth)/entrar/page.tsx`
- `apps/web/src/app/(auth)/registar/page.tsx`
- `apps/web/src/app/(auth)/recuperar/page.tsx`
- `docs/runbooks/supabase-auth-setup.md`

**Ficheiros a actualizar:**
- `apps/web/package.json` (adicionar `@meu-jarvis/auth`)
- `package.json` raiz (workspace `packages/auth`)

---

## Dev Notes

### Supabase SSR pattern — `createServerSupabaseClient` (Architecture §5.1, ADR-002)

```typescript
// packages/auth/src/server.ts
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'

export function createServerSupabaseClient() {
  const cookieStore = cookies()
  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() { return cookieStore.getAll() },
        setAll(cookiesToSet) {
          cookiesToSet.forEach(({ name, value, options }) =>
            cookieStore.set(name, value, options)
          )
        },
      },
    }
  )
}
```

### Middleware pattern (Architecture §7.2)

```typescript
// apps/web/src/middleware.ts
import { NextResponse } from 'next/server'
import type { NextRequest } from 'next/server'
import { createServerClient } from '@supabase/ssr'

export async function middleware(request: NextRequest) {
  let supabaseResponse = NextResponse.next({ request })

  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() { return request.cookies.getAll() },
        setAll(cookiesToSet) {
          cookiesToSet.forEach(({ name, value }) =>
            request.cookies.set(name, value)
          )
          supabaseResponse = NextResponse.next({ request })
          cookiesToSet.forEach(({ name, value, options }) =>
            supabaseResponse.cookies.set(name, value, options)
          )
        },
      },
    }
  )

  const { data: { user } } = await supabase.auth.getUser()

  if (!user && request.nextUrl.pathname.startsWith('/app')) {
    return NextResponse.redirect(new URL('/entrar', request.url))
  }

  return supabaseResponse
}

export const config = {
  matcher: ['/(app)/:path*', '/api/((?!billing|cron).*)'],
}
```

### `custom_access_token_hook` SQL (Architecture §5.2)

A function já existe em `packages/db/migrations/` (aplicada em Story 1.3). Apenas precisa de ser registada no Dashboard.

```sql
-- Verificar que existe:
SELECT routine_name FROM information_schema.routines
WHERE routine_schema = 'public'
  AND routine_name = 'custom_access_token_hook';
```

### Auto-criação de household (Architecture §6.4, PRD FR24, FR30)

O trigger é criado em SQL (adicionar a uma nova migration ou incluir numa existente):

```sql
-- packages/db/migrations/0002_auth_trigger.sql (nova migration)
create or replace function public.handle_new_user()
returns trigger language plpgsql security definer
set search_path = public
as $$
declare
  new_household_id uuid;
begin
  -- Criar household default
  insert into public.households (name, owner_id, currency, locale)
  values (
    'Casa de ' || split_part(new.email, '@', 1),
    new.id,
    'EUR',
    'pt-PT'
  )
  returning id into new_household_id;

  -- Adicionar user como owner
  insert into public.household_members (household_id, user_id, role)
  values (new_household_id, new.id, 'owner');

  -- Activar trial 14 dias (Architecture §6.4) — plano familia durante trial
  insert into public.subscriptions (
    household_id, plan, status,
    trial_end, current_period_end
  )
  values (
    new_household_id, 'familia', 'trialing',
    now() + interval '14 days',
    now() + interval '14 days'
  );

  return new;
end;
$$;

create trigger on_auth_user_created
  after insert on auth.users
  for each row execute procedure public.handle_new_user();
```

**Atenção:** Esta migration acede ao schema `auth` (Supabase managed). Deve ser testada em staging antes de aplicar a produção.

### Trial de 14 dias — plano família (Architecture §6.4)

A Architecture decide que o trial dá plano `familia` (não `pessoal`) para melhor UX durante os 14 dias. Eurico confirmou este ponto como pendente de validação — implementar como `familia` por default até indicação em contrário.

### Sessão JWT (Architecture §5.4)

| Item | Valor |
|------|-------|
| JWT TTL | 1 hora |
| Refresh token | 30 dias com rotation |
| MFA | TOTP opt-in (Supabase nativo, não configurar nesta story) |
| Hashing passwords | bcrypt cost 12 (Supabase Auth default — NFR6 cumprida) |

### Páginas de auth — copy PT-PT obrigatória (CON3)

Exemplos de copy:
- Botão submit: "Entrar", "Registar", "Recuperar palavra-passe"
- Labels: "Endereço de email", "Palavra-passe"
- Mensagens de erro: "Email ou palavra-passe incorrectos", "Email inválido"
- NUNCA: "Password", "Login", "Sign in", "Sign up"

---

## Testing Notes

- **Unit:** `pnpm typecheck` em `packages/auth/`.
- **Integration:** Teste em `packages/db-test/` simulando JWT com `household_id` correcto vs errado.
- **E2E (smoke):** Fluxo básico registo → login → acesso a rota protegida → redirect se não autenticado. (Playwright em Story 1.6 cobre mais detalhes.)
- **Manual:** Verificar no Supabase Dashboard que o hook está registado e testar com utilizador real.

---

## Definition of Done

- [ ] `packages/auth/` existe com `createServerSupabaseClient()` e `createBrowserSupabaseClient()`
- [ ] Middleware em `apps/web/src/middleware.ts` protege rotas `/(app)/*`
- [ ] `custom_access_token_hook` registado no Supabase Dashboard e JWT contém `household_id`
- [ ] Auto-criação de household + trial ao registar novo utilizador
- [ ] Trigger SQL ou webhook documentado e aplicado
- [ ] Teste de integração JWT + RLS passa
- [ ] `pnpm lint` e `pnpm typecheck` passam (exit 0)
- [ ] `docs/runbooks/supabase-auth-setup.md` criado
- [ ] @qa gate APPROVED
- [ ] PR ready para @devops merge

---

## CodeRabbit Integration

> **CodeRabbit Integration**: Desactivado em `core-config.yaml`. Revisão manual pelo @qa.

---

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-05-04 | 1.0 | Story criada; BLOQUEADORES EXTERNOS documentados | River (@sm) |
| 2026-05-06 | 1.1 | **Bloqueador B2 desbloqueado.** Tasks 3.1-3.3 cumpridas pre-emptively: migration `0002_auth_hook.sql` criada + aplicada Supabase real (Dara), Auth Hook registado e ENABLED no Dashboard (Eurico). Bug spec architecture §5.2 corrigido (`created_at` → `joined_at`). Story pronta para `@po *validate-story-draft` (Draft → Ready). | Orion (@aiox-master) + Dara (@data-engineer) |
| 2026-05-06 | 1.2 | **@po validation: GO (9/10).** Status Draft → Ready. 10-point checklist: 8 ✅ + 2 ⚠️ (Business Value e Risks sem secções formais mas conteúdo distribuído inline — não bloqueia). Trace Article IV sólido, AC testáveis, dependências mapeadas, bloqueador Auth Hook resolvido. Story pronta para `@dev *develop 1.5`. | Pax (@po) |
