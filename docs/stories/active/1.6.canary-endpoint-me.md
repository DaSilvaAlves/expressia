# Story 1.6 — Endpoint Canary `/api/me` + Teste E2E RLS Cross-Household

**Status:** InProgress
**Epic:** Epic 1 — Foundation & Multi-Tenant Core
**Owner:** @dev
**Estimate:** M
**Created:** 2026-05-04
**Updated:** 2026-05-06

---

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["vitest", "playwright", "typecheck"]
```

---

## User Story

**As a** developer da equipa Expressia,
**I want** um endpoint `/api/me` autenticado que devolva o household e role do utilizador, acompanhado de um teste E2E que prove que o RLS bloqueia acesso cross-household,
**so that** a Epic 1 fica provada fim-a-fim: auth → JWT com household_id → RLS activa → acesso correcto e bloqueio cross-household verificado automaticamente.

---

## Dependências

- **DEPENDE de Story 1.1** (monorepo Next.js funcional).
- **DEPENDE de Story 1.5** (Supabase Auth + middleware + JWT com `household_id`).
- Story 1.7 (observability) pode correr em paralelo com esta story.

---

## Acceptance Criteria

- [ ] **AC1:** `GET /api/me` com JWT válido retorna `200` com corpo JSON: `{ user: { id, email }, household: { id, name, plan }, role: 'owner'|'admin'|'member' }`.
- [ ] **AC2:** `GET /api/me` sem JWT retorna `401` com mensagem PT-PT.
- [ ] **AC3:** `GET /api/me` com JWT de utilizador sem household retorna `404` com mensagem PT-PT.
- [ ] **AC4:** Latência p95 do endpoint < 200ms em Vercel fra1 (PRD Epic 1 AC3).
- [ ] **AC5:** Teste E2E Playwright prova que User B não consegue ver dados do Household A via `/api/me` (JWT de B tem `household_id` de B, não A).
- [ ] **AC6:** Teste E2E Playwright prova que inserção directa com `household_id` de outro household é bloqueada pelo RLS (erro 403 ou RLS violation).
- [ ] **AC7:** Endpoint usa Drizzle + RLS — query é feita no contexto do JWT do utilizador (não service role).
- [ ] **AC8:** Todos os erros do endpoint retornam no formato `ApiError` padronizado (Architecture §7.3): `{ error: { code, message, timestamp, requestId } }`. Mensagens em PT-PT.
- [ ] **AC9:** `pnpm lint` e `pnpm typecheck` passam sem erros.
- [ ] **AC10:** Teste E2E está integrado no job `e2e` do CI (`.github/workflows/ci.yaml`) — corre em PRs via Vercel preview deploy.

---

## Trace (Article IV — No Invention)

| AC | Rastreabilidade |
|----|----------------|
| AC1 | PRD Epic 1 AC2 (canary endpoint), Architecture §7.1 (Route Handlers REST) |
| AC2 | Architecture §7.3 (error handling padronizado), PRD NFR5 (auth gate) |
| AC3 | Architecture §7.3 (error handling), edge case sem household |
| AC4 | PRD Epic 1 AC3 ("canary endpoint responde com latência p95 < 200ms") |
| AC5-AC6 | PRD Epic 1 AC2 ("Cross-household access bloqueado — verificado em teste automatizado"), Architecture §10.4 ("Convite household + RLS isolation") |
| AC7 | Architecture §5.1 (RLS via JWT), ADR-002 |
| AC8 | Architecture §7.3 (`ApiError` type, mensagens PT-PT) |
| AC9 | PRD NFR17-19, CON5 |
| AC10 | Architecture §11.4 (job `e2e` Playwright) |

---

## Tasks

- [x] **Task 1 — Criar endpoint `GET /api/me` (AC1-AC3, AC7, AC8)**
  - [x] 1.1 Criar `apps/web/src/app/api/me/route.ts`
  - [x] 1.2 Obter sessão via `createServerSupabaseClient()` do package `@meu-jarvis/auth`
  - [x] 1.3 Se não autenticado, retornar `ApiError` 401 com mensagem PT-PT: "Sessão inválida ou expirada. Por favor inicie sessão novamente."
  - [x] 1.4 ~~Query Drizzle~~ → Query via `supabase.from('household_members').select('role, households (id, name, plan)')` (PostgREST com RLS-via-JWT — decisão C6 Opção A). `households.plan` denormalizado evita JOIN com `subscriptions`.
  - [x] 1.5 Se sem household, retornar `ApiError` 404 com mensagem PT-PT: "Household não encontrado. Por favor complete o registo."
  - [x] 1.6 Retornar `200` com `{ user, household, role }`
  - [x] 1.7 `requestId` UUID v4 incluído em todas as respostas de erro (preparação OTel Story 1.7)

- [x] **Task 2 — Criar tipo `ApiError` (AC8)**
  - [x] 2.1 Criado `apps/web/src/lib/errors.ts` (decisão C1 — KISS, sem `packages/shared/`)
  - [x] 2.2 Exportar tipo `ApiErrorBody` conforme Architecture §7.3
  - [x] 2.3 Helper `apiError(code, message, status, details?)` retorna `NextResponse<ApiErrorBody>` formatado

- [x] **Task 3 — Testes Vitest para o endpoint (AC1-AC3)**
  - [x] 3.1 Criado `apps/web/src/app/api/me/__tests__/route.test.ts` (8 testes, environment override `node`)
  - [x] 3.2 Mock de Supabase client via `vi.mock('@meu-jarvis/auth/server')` (decisão C2 — Testcontainers fica para Story 1.4)
  - [x] 3.3 Teste: JWT válido → 200 com household correcto + variante `email: null`
  - [x] 3.4 Teste: sem JWT → 401 + variante com erro de auth (JWT expired)
  - [x] 3.5 Teste: JWT sem household → 404 + variante com `households` relação null + defensive 500 + verificação requestId UUID & timestamp ISO

- [ ] **Task 4 — Testes E2E Playwright (AC5, AC6, AC10)** ⏸ PAUSED — awaits @devops C4 (CI secrets) + C5 (Vercel project linkage)
  - [ ] 4.1 Criar `apps/web/e2e/` se não existe
  - [ ] 4.2 Criar `apps/web/playwright.config.ts`
  - [ ] 4.3 Criar `apps/web/e2e/rls-isolation.spec.ts`:
    - Setup: criar 2 utilizadores com 2 households distintos
    - Verificar `/api/me` com JWT de User A retorna household A
    - Verificar `/api/me` com JWT de User B retorna household B (não A)
    - Verificar que tentativa de acesso directo a dados do household A com token de User B falha
  - [ ] 4.4 Actualizar `.github/workflows/ci.yaml` — job `e2e` usa Vercel preview URL
  - [ ] 4.5 _(NEW por decisão C3)_ Criar `apps/web/e2e/helpers/test-users.ts` com `supabase.auth.admin.createUser()` (Service role APENAS em test setup)

- [ ] **Task 5 — Verificar latência (AC4)** ⏸ PAUSED — awaits @devops C5 (Vercel project linkage)
  - [ ] 5.1 Deploy em Vercel preview (ou staging)
  - [ ] 5.2 Medir latência do endpoint com `curl -w "%{time_total}" ...` 10 vezes
  - [ ] 5.3 Documentar resultado em PR description

- [x] **Task 6 — Verificação final (AC9)**
  - [x] 6.1 `pnpm --filter @meu-jarvis/web lint` — ✔ No ESLint warnings or errors
  - [x] 6.2 `pnpm --filter @meu-jarvis/web typecheck` — ✔ exit 0, zero erros
  - [x] 6.3 _(extra)_ `pnpm --filter @meu-jarvis/web test` — ✔ 9/9 testes passam (incluindo 8 novos)

---

## File List

_(Preenchido pelo @dev durante implementação)_

**Ficheiros a criar:**
- `apps/web/src/app/api/me/route.ts`
- `apps/web/src/app/api/me/__tests__/route.test.ts`
- `apps/web/src/lib/errors.ts` (ou `packages/shared/src/errors.ts`)
- `apps/web/playwright.config.ts`
- `apps/web/e2e/rls-isolation.spec.ts`

**Ficheiros a actualizar:**
- `.github/workflows/ci.yaml` (job `e2e`)

---

## Dev Notes

### Estrutura do endpoint (Architecture §7.1, §7.3)

```typescript
// apps/web/src/app/api/me/route.ts
import { NextResponse } from 'next/server'
import { createServerSupabaseClient } from '@meu-jarvis/auth'
import { db } from '@meu-jarvis/db'
import { householdMembers, households, subscriptions } from '@meu-jarvis/db/schema'
import { eq } from 'drizzle-orm'
import { apiError } from '@/lib/errors'

export async function GET() {
  const supabase = createServerSupabaseClient()
  const { data: { user }, error: authError } = await supabase.auth.getUser()

  if (authError || !user) {
    return apiError('AUTH_REQUIRED', 'Sessão inválida ou expirada. Por favor inicie sessão novamente.', 401)
  }

  // Query com RLS activo (user context via JWT)
  const membership = await db
    .select({
      household: households,
      role: householdMembers.role,
      plan: subscriptions.plan,
    })
    .from(householdMembers)
    .innerJoin(households, eq(householdMembers.household_id, households.id))
    .leftJoin(subscriptions, eq(subscriptions.household_id, households.id))
    .where(eq(householdMembers.user_id, user.id))
    .limit(1)

  if (!membership.length) {
    return apiError('HOUSEHOLD_NOT_FOUND', 'Household não encontrado. Por favor complete o registo.', 404)
  }

  return NextResponse.json({
    user: { id: user.id, email: user.email },
    household: {
      id: membership[0].household.id,
      name: membership[0].household.name,
      plan: membership[0].plan,
    },
    role: membership[0].role,
  })
}
```

### `ApiError` type (Architecture §7.3)

```typescript
// apps/web/src/lib/errors.ts
import { NextResponse } from 'next/server'
import { randomUUID } from 'crypto'

export type ApiError = {
  error: {
    code: string
    message: string        // PT-PT user-facing
    details?: Record<string, unknown>
    timestamp: string      // ISO-8601
    requestId: string
  }
}

export function apiError(
  code: string,
  message: string,
  status: number,
  details?: Record<string, unknown>
): NextResponse<ApiError> {
  return NextResponse.json(
    {
      error: {
        code,
        message,
        details,
        timestamp: new Date().toISOString(),
        requestId: randomUUID(),
      }
    },
    { status }
  )
}
```

**Nota:** Nunca expor stack traces ou mensagens internas ao cliente. Nunca usar `details` para PII.

### RLS no endpoint — CRITICAL (Architecture §5.1, ADR-002)

O endpoint usa Drizzle **com o cliente Supabase do utilizador** (não service role). Isto significa que o RLS está activo automaticamente — a query filtra apenas o household do JWT.

**NÃO usar `SUPABASE_SERVICE_ROLE_KEY`** no endpoint `/api/me` — service role bypassa RLS.

### Teste E2E — RLS cross-household (Architecture §10.4, PRD Epic 1 AC2)

```typescript
// apps/web/e2e/rls-isolation.spec.ts
import { test, expect } from '@playwright/test'

test('RLS bloqueia acesso cross-household', async ({ request }) => {
  // Criar utilizador A + B (via Supabase Admin API em test setup)
  const tokenA = await getTestToken('user-a@test.com')
  const tokenB = await getTestToken('user-b@test.com')

  // User A vê o seu próprio household
  const resA = await request.get('/api/me', {
    headers: { Authorization: `Bearer ${tokenA}` }
  })
  expect(resA.status()).toBe(200)
  const bodyA = await resA.json()
  expect(bodyA.household).toBeDefined()

  // User B vê o seu próprio household (diferente de A)
  const resB = await request.get('/api/me', {
    headers: { Authorization: `Bearer ${tokenB}` }
  })
  expect(resB.status()).toBe(200)
  const bodyB = await resB.json()

  // Os dois households são diferentes
  expect(bodyA.household.id).not.toBe(bodyB.household.id)
})
```

### Configuração Playwright (Architecture §10.1)

```typescript
// apps/web/playwright.config.ts
import { defineConfig } from '@playwright/test'

export default defineConfig({
  testDir: './e2e',
  use: {
    baseURL: process.env.PLAYWRIGHT_BASE_URL || 'http://localhost:3000',
  },
  webServer: process.env.CI ? undefined : {
    command: 'pnpm dev',
    url: 'http://localhost:3000',
    reuseExistingServer: true,
  },
})
```

Em CI, usa `PLAYWRIGHT_BASE_URL` apontando para o Vercel preview deploy.

### Resposta esperada `GET /api/me`

```json
{
  "user": {
    "id": "uuid-do-user",
    "email": "utilizador@exemplo.pt"
  },
  "household": {
    "id": "uuid-do-household",
    "name": "Casa do João",
    "plan": "familia"
  },
  "role": "owner"
}
```

---

## Testing Notes

- **Unit (Vitest):** Mocks do Supabase client — testar os 3 casos: 200, 401, 404.
- **Integration:** Query real ao Postgres (Testcontainers) com JWT simulado — verificar que RLS filtra correctamente.
- **E2E (Playwright):** Teste `rls-isolation.spec.ts` com 2 utilizadores reais em Vercel preview (ou localhost).
- **Performance:** Medir latência p95 < 200ms — documentar no PR.

---

## Definition of Done

- [ ] `GET /api/me` retorna 200, 401, 404 nos casos corretos
- [ ] Respostas de erro no formato `ApiError` em PT-PT
- [ ] Latência p95 < 200ms documentada
- [ ] Teste E2E Playwright de RLS cross-household a passar
- [ ] Testes Vitest para o endpoint passam
- [ ] CI job `e2e` configurado e a correr
- [ ] `pnpm lint` e `pnpm typecheck` passam (exit 0)
- [ ] @qa gate APPROVED
- [ ] PR ready para @devops merge

---

## CodeRabbit Integration

> **CodeRabbit Integration**: Desactivado em `core-config.yaml`. Revisão manual pelo @qa.

---

## Dev Agent Record

### Agent Model Used

`claude-opus-4-7[1m]` — Dex (@dev), session 2026-05-06.

### Debug Log References

- 2026-05-06 — Pre-implementation discovery: validar RLS-via-JWT mechanics vs `getDb()` direct postgres-js connection antes de tocar em código. Resultado: C6 raised (ver abaixo).

### Completion Notes List

#### Implementation Decisions (resolvidas com @po antes de implementação)

**C1 — `ApiError` helper location** ✅ RESOLVED
- **Decision:** `apps/web/src/lib/errors.ts`
- **Rationale:** KISS — único consumidor por agora é `apps/web`. Criar `packages/shared/` apenas com 2+ consumers (YAGNI). Confirmado por @po.

**C2 — Test strategy (Vitest unit tests)** ✅ RESOLVED
- **Decision:** Mock Supabase JS client (não Testcontainers para esta story)
- **Rationale:** Story 1.4 (Testcontainers + RLS suite) está Ready (não Done). Quando 1.4 ship-ar, considerar migrar testes Vitest deste endpoint para integração real. E2E continua com Playwright contra DB real em preview.

**C3 — `getTestToken()` test helper** ✅ RESOLVED
- **Decision:** Criar `apps/web/e2e/helpers/test-users.ts` com `supabase.auth.admin.createUser()` (Supabase Admin API)
- **Rationale:** `SUPABASE_SERVICE_ROLE_KEY` é usado APENAS em test setup, NUNCA em runtime do endpoint (que continua a usar JWT do utilizador). Adiciona implicitamente uma Task 4.5.

#### Pre-DevOps Coordination (sinalizar a Gage antes de Task 4)

**C4 — Vercel preview secrets para CI** ✅ APPROACH RESOLVED, ⏸ awaits secrets

- **Decisão Gage (@devops):** Opção B — **Vercel GitHub Integration nativa**, sem Vercel CLI no CI.
- **Razões:**
  - Zero CI minutos extra (Vercel deploya em paralelo, fora do GH Actions runner)
  - Standard pattern Next.js + Vercel (95% dos projectos)
  - Sem necessidade de `VERCEL_TOKEN` / `VERCEL_ORG_ID` / `VERCEL_PROJECT_ID` como secrets
  - Preview URL exposed via GitHub Deployment Status — Playwright apanha via action `bobheadxi/deployments@v1` (1k+ stars, mantida)
- **Job `e2e` proposto** (a adicionar a `.github/workflows/ci.yaml` em Task 4.4):
  ```yaml
  e2e:
    name: E2E Playwright (RLS isolation)
    runs-on: ubuntu-latest
    needs: [quality]
    if: github.event_name == 'pull_request'
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - id: deployment
        uses: bobheadxi/deployments@v1
        with:
          step: wait-for-deployment-status
          token: ${{ secrets.GITHUB_TOKEN }}
          env: Preview
          timeout: 600
      - uses: pnpm/action-setup@v4
        with: { version: '${{ env.PNPM_VERSION }}' }
      - uses: actions/setup-node@v4
        with: { node-version: '${{ env.NODE_VERSION }}', cache: pnpm }
      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @meu-jarvis/web exec playwright install --with-deps chromium
      - run: pnpm --filter @meu-jarvis/web exec playwright test
        env:
          PLAYWRIGHT_BASE_URL: ${{ steps.deployment.outputs.deployment_url }}
          SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
  ```
- **Secrets necessários no repo (`DaSilvaAlves/expressia`):**
  | Secret | Estado actual | Uso |
  |--------|---------------|-----|
  | `NEXT_PUBLIC_SUPABASE_URL` | ❌ NÃO definido | Build job actual + e2e test setup |
  | `NEXT_PUBLIC_SUPABASE_ANON_KEY` | ❌ NÃO definido | Build job actual + e2e |
  | `SUPABASE_SERVICE_ROLE_KEY` (NEW) | ❌ NÃO definido | **Apenas** test setup E2E (criar test users via Admin API) — NUNCA no runtime do endpoint |
- **Discovery lateral:** Os 3 CI runs anteriores em `main` passaram **sem nenhum secret configurado** porque (a) Next.js build não falha quando vars são vazias em build-time, (b) `getEnv()` em `packages/auth/src/server.ts` apenas lança erro em runtime. Vai partir assim que app for deployed em ambiente real.

**C5 — Vercel project linkage ao repo** 🛑 BLOCKED — awaits Eurico

- **Estado descoberto:**
  - `vercel projects ls` → 22 projects no team `euricojsalves-4744s-projects`, **nenhum** ligado a `DaSilvaAlves/expressia`
  - Sem `.vercel/project.json` em `apps/web/` ou raiz do monorepo
  - Vercel CLI 51.2.1 instalada e autenticada localmente
- **Acção do Eurico (não pode ser executada por Gage):**
  1. Abrir https://vercel.com/new
  2. Import Git Repository → seleccionar `DaSilvaAlves/expressia`
  3. Configurar:
     - **Root Directory:** `apps/web`
     - **Framework Preset:** Next.js (auto-detect)
     - **Region:** `fra1` (já em `apps/web/vercel.json` linha de `regions: ["fra1"]`)
     - **Environment Variables (Production + Preview + Development):**
       - `NEXT_PUBLIC_SUPABASE_URL`
       - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
       - (Outras vars Supabase necessárias: `DATABASE_URL`, `DATABASE_URL_DIRECT`, `SUPABASE_SERVICE_ROLE_KEY`)
  4. Confirmar GitHub Integration activa (deploy automático em PRs + push para main)
  5. Fornecer ao Gage:
     - `SUPABASE_SERVICE_ROLE_KEY` (para `gh secret set` no repo — apenas para CI)
     - Nome do Vercel project (para documentação)
- **Após Eurico desbloquear:** Gage corre `gh secret set` para os 3 secrets acima, depois `@dev` retoma Tasks 4-5.

#### Technical Blocker (awaits Eurico decision)

**C6 — RLS-via-JWT mechanics colidem com AC7 (Drizzle direct connection)** 🛑 BLOCKED

- **Discovery:** AC7 exige *"Endpoint usa Drizzle + RLS — query é feita no contexto do JWT do utilizador (não service role)"*. Investigação revela conflito real:
  - `getDb()` em `packages/db/src/client.ts:33-53` usa postgres-js direct connection com role `authenticated`, **sem injecção de JWT claims**
  - `current_household_id()` SQL function (migration `0000_initial_schema.sql:41-44`) lê de `request.jwt.claims.household_id` (apenas populado quando query passa por **PostgREST** via supabase-js) **OU** de `app.current_household_id` GUC (apenas populado por chamada manual a `setHouseholdContext()` — paradox para `/api/me` que não conhece `householdId` à priori)
  - Resultado: implementar literalmente como Dev Notes mostra (linhas 134-176) → `current_household_id()` retorna NULL → todas as policies RLS bloqueiam → endpoint devolve sempre 0 rows
- **Evidence in codebase:** O único exemplo de query autenticada existente — `apps/web/src/app/(app)/visao/page.tsx` (Story 1.5) — usa `supabase.from(...)` (PostgREST), não Drizzle direct. Não há nenhum exemplo funcional de Drizzle+RLS no codebase ainda.
- **Three options:**

  | # | Approach | Cumpre AC7? | Esforço | Trade-off |
  |---|----------|-------------|---------|-----------|
  | **A** | Usar `supabase.from(...).select(...)` (PostgREST com JWT auto-injectado) | Spirit ✅, letter ⚠️ | 0 (alinha com `visao/page.tsx`) | Re-interpretar AC7 como "RLS via JWT activa, sem service role" — supabase-js em vez de Drizzle ORM neste endpoint específico |
  | **B** | Estender `getDb()` para aceitar JWT/claims e fazer `select set_config('request.jwt.claims', ..., true)` antes de cada query | ✅ Cumpre letter | M (transações explícitas, edge cases, refactor de `getDb()`) | É trabalho de Story 1.4 (RLS test suite). Scope creep aqui. |
  | **C** | `getServiceDb()` (bypass RLS) + filter manual `user_id = auth.uid()` | ❌ Viola AC7 | 0 | Descartar — viola AC explicitamente |

- **Dex recommendation:** **Opção A**. Razões: (1) spirit de AC7 cumprido (RLS-via-JWT activa); (2) pattern já existe em `visao/page.tsx`; (3) Drizzle como ORM canónico fica para Story 1.4 onde Testcontainers+RLS test suite vai forçar a evolução de `getDb()` (Opção B); (4) tipos Drizzle continuam source-of-truth — apenas o cliente runtime muda.
- **Decision (Eurico, 2026-05-06):** ✅ **Opção A — Supabase JS client (PostgREST)**.
- **Next step:** `@po *amend-ac7` — suavizar wording de AC7 de *"Endpoint usa Drizzle + RLS"* para *"RLS via JWT activa, sem service role"*, com nota sobre Story 1.4 como evolução para Drizzle direct quando `getDb()` ganhar JWT injection. Após amend, `@dev` retoma Tasks 1-3+6 com Status: Ready → InProgress.
- **Schema impact:** Drizzle types/schemas continuam como source-of-truth. Apenas o **cliente runtime** muda — usar `supabase.from('household_members').select(...)` em vez de `db.select().from(householdMembers)...`. `households.plan` (denormalizado) evita JOIN com `subscriptions` para AC1.

### File List

**Created (Tasks 1-3):**
- `apps/web/src/lib/errors.ts` — Helper `apiError()` + tipo `ApiErrorBody` (Architecture §7.3)
- `apps/web/src/app/api/me/route.ts` — `GET /api/me` Route Handler (RLS-via-JWT via PostgREST)
- `apps/web/src/app/api/me/__tests__/route.test.ts` — 8 testes Vitest cobrindo AC1-AC3 + AC8 + defensive 500

**Pending (Tasks 4-5, paused):**
- `apps/web/playwright.config.ts` — Task 4.2
- `apps/web/e2e/rls-isolation.spec.ts` — Task 4.3
- `apps/web/e2e/helpers/test-users.ts` — Task 4.5 (NEW por C3)
- `.github/workflows/ci.yaml` — Task 4.4 (extender com job `e2e`)

**Verification commands run (Task 6):**
- `pnpm --filter @meu-jarvis/web lint` → exit 0, zero warnings
- `pnpm --filter @meu-jarvis/web typecheck` → exit 0, zero erros
- `pnpm --filter @meu-jarvis/web test` → 9/9 passed (1.45s)

---

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-05-04 | 1.0 | Story criada | River (@sm) |
| 2026-05-06 | 1.1 | @po validation: GO 9.5/10. 5 clarificações (C1-C5) anotadas para @dev resolver upfront. Status: Draft → Ready. | Pax (@po) |
| 2026-05-06 | 1.2 | @dev pre-implementation discovery: C1-C3 resolved; C4-C5 awaits @devops; **C6 raised** (RLS-via-JWT mechanics colidem com AC7 "Drizzle direct"). Implementação Tasks 1-N pausada à espera de Eurico decision (A/B/C). Zero ficheiros tocados. | Dex (@dev) |
| 2026-05-06 | 1.3 | **Eurico decidiu Opção A** (Supabase JS client / PostgREST). Próximo passo: @po *amend-ac7 para suavizar wording de AC7. Após amend, @dev retoma. | Dex (@dev) regista decisão |
| 2026-05-06 | 1.4 | @dev implementação parcial. Tasks 1-3+6 done: `errors.ts` helper, `route.ts` endpoint via PostgREST RLS-via-JWT, 8 testes Vitest passam (9/9 com smoke), lint+typecheck verde. Tasks 4-5 paused awaiting @devops C4+C5. Status: Ready → InProgress. | Dex (@dev) |
| 2026-05-06 | 1.5 | @devops Gage coordinated Vercel setup. **C4 RESOLVED** (approach: Opção B — Vercel GitHub Integration nativa, job `e2e` proposto via `bobheadxi/deployments@v1`). **C5 BLOCKED on Eurico** — Vercel project NÃO existe para `DaSilvaAlves/expressia`. Discovery lateral: 0 secrets no repo (CI runs anteriores passaram porque build Next.js não falha sem env vars). Handoff persistente criado em `docs/handoffs/`. | Gage (@devops) |
