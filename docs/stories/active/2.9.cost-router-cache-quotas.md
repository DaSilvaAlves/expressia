# Story 2.9 — Cost Router + Cache Upstash + Quotas (NFR20)

**Status:** Draft
**Epic:** Epic 2 — Cérebro AI Multi-Intent
**Owner:** @sm (draft) → @po (validate) → @dev (impl) → @architect (gate) → @devops (push)
**Estimate:** M
**Version:** 1.0
**Created:** 2026-05-12
**Author:** @sm (River)

---

## Executor Assignment

| Phase | Owner | Output |
|-------|-------|--------|
| Draft | @sm (River) | `2.9.cost-router-cache-quotas.md` v1.0 |
| Validate | @po (Pax) | GO/NO-GO + 10-point checklist + anti-hallucination cross-confirm |
| Implement | @dev (Dex) | Upstash cache + cost router + quota fixes + ≥14 testes novos |
| Quality Gate | @architect (Aria) | APPROVED / CONCERNS / FAIL (cross-domain: cost engineering + RLS + cache) |
| Push | @devops (Gage) | `git push origin main` |

**Type-to-executor consistency PASS:** scope M cross-domain (cache infra + routing logic + quota enforcement + RLS fix) → @dev (full-stack) com gate dedicado @architect. Rationale: decisões sobre cache key strategy, RLS serviceDb choice, quota tier alignment com Architecture §4.6 são arquitecturais → @architect (vs @qa que entra em benchmark E2E Story 2.10).

---

## Bloqueadores Externos

| ID | Requisito | Estado | Impacto nesta story |
|----|-----------|--------|---------------------|
| **EB3** | Upstash Redis EU Frankfurt — projecto `eu-west-1` | **PENDING** | **NÃO bloqueia** impl mockable-only. Cliente Upstash mockado via Vitest `vi.mock('@upstash/redis')` (padrão 2.2-2.8). Runbook T8 documenta provisão para Eurico. Integração real desbloqueada pre-push @devops. |
| **EB1** | `ANTHROPIC_API_KEY` DPA UE | PENDING | NÃO afecta 2.9 — pipeline continua mockable-only (padrão 2.2-2.8). |
| **EB2** | `OPENAI_API_KEY` DPA UE | PENDING | NÃO afecta 2.9. |

**NOTA:** `@dev *develop 2.9` pode arrancar imediatamente após validação @po. Zero calls reais a Upstash/Anthropic/OpenAI — todos os testes usam mocks Vitest (precedente 6 stories Done APPROVED 9.4/10 mockable-only).

---

## User Story

**Como** utilizador autenticado de um household Família que submete prompts recorrentes ao `/jarvis` ao longo do mês,
**Quero** que prompts repetidos sejam respondidos mais depressa (sem re-invocar o classificador LLM), que perguntas simples como "quantas tarefas tenho?" sejam resolvidas directamente na base de dados sem chamar o executor Sonnet, e que o sistema me avise claramente em PT-PT quando atingir o limite mensal de prompts antes de cobrar overrun,
**Para que** a Expressia mantenha custo LLM abaixo de 30% MRR (NFR20 / R3 mitigação) e eu possa usar o agente com confiança de que não haverá cobranças surpresa nem performance degradada por abuso.

---

## Dependências

**Internas (Done):**

- Story 2.1 ✓ Schema `agent_quotas` (tabela + 4 RLS policies em 0001) — JÁ EXISTE com colunas `household_id`, `plan`, `period_start`, `period_end`, `prompts_used`, `tokens_input_used`, `tokens_output_used`, `cost_eur_accumulated`, `updated_at`. Zero migration nova necessária.
- Story 2.2 ✓ Package `@meu-jarvis/agent` com `OpenAIClientLike` — padrão mockable-only reutilizável.
- Story 2.4 ✓ Classifier PT-PT — output `ClassificationResult.intents[]` + `overall_confidence` consumido pelo cost router.
- Story 2.6 ✓ Endpoint `POST /api/agent/prompt` — ponto de integração central das três features desta story. Funções `checkQuota`, `incrementQuota`, `checkRateLimit` já existem em `apps/web/src/lib/agent/rate-limiter.ts` e `audit-log.ts`.

**Externas:**

- **EB3 — Upstash Redis EU Frankfurt** — DEFERRED, não bloqueia. `.env.example:61-62` já tem `UPSTASH_REDIS_REST_URL` e `UPSTASH_REDIS_REST_TOKEN` (vazios). Eurico provisiona via runbook T8 pre-push @devops.

---

## Acceptance Criteria

### 1. Cache Upstash — lookup antes do classifier

- **AC1:** Criar `apps/web/src/lib/agent/cache.ts` com interface `UpstashClientLike` mockable-only e implementação `UpstashCache` que usa `@upstash/redis`:
  - Método `get(key: string): Promise<string | null>` — lookup de ClassificationResult cacheado.
  - Método `set(key: string, value: string, ttlSeconds: number): Promise<void>` — cache SET com TTL.
  - Método `del(key: string): Promise<void>` — invalidação manual (para testes).
  - Cliente inicializado lazy via `UPSTASH_REDIS_REST_URL` + `UPSTASH_REDIS_REST_TOKEN`; se variáveis ausentes, `UpstashCache` retorna sempre `null` em `get` (modo degradado — sem crash em dev/CI). Trace: Architecture §4.6, `.env.example:61-62`.

- **AC2:** Criar função `buildCacheKey(prompt: string, householdPlan: string): string` em `apps/web/src/lib/agent/cache.ts`:
  - Implementação: `sha256(normalize(prompt) + householdPlan)` usando `node:crypto.createHash('sha256').update(...).digest('hex')`.
  - `normalize(prompt)`: trim + lowercase + collapse whitespace (`prompt.trim().toLowerCase().replace(/\s+/g, ' ')`).
  - **NOTA anti-hallucination:** key usa `householdPlan` (ex: `'familia'`), NÃO `householdId` — conforme Architecture §4.6 literal (`sha256(normalize(prompt) + household_plan)`). Dois utilizadores do mesmo plano partilham cache de classificação (segurança: ClassificationResult não contém PII — apenas intents detectadas).
  - NUNCA logar o prompt em cleartext nos logs OTel (NFR12 PII redaction — precedente DN5 Story 2.6 redactPii). Cache key (hash) é seguro para logging.
  - Trace: Architecture §4.6 algoritmo de routing — linha `sha256(normalize(prompt) + household_plan)`.

- **AC3:** Integrar cache lookup em `apps/web/src/app/api/agent/prompt/route.ts` **antes** do classifier (step 6 actual):
  - Após rate limit + quota check (step 4) e INSERT initial agent_run (step 5), tentar `cacheClient.get(cacheKey)`.
  - **HIT:** parsear ClassificationResult cacheado, pular classifier LLM, registar `cache_hit: true` em OTel span (`annotateAgentPromptSpan`). Continuar para cost router (AC6) usando ClassificationResult do cache.
  - **MISS:** executar classifier normalmente. Após classificação bem-sucedida, fazer `cacheClient.set(cacheKey, JSON.stringify(result), CACHE_TTL_SECONDS)`.
  - `CACHE_TTL_SECONDS`: lido de `process.env.CACHE_TTL_SECONDS`; default `300` (5 minutos) se ausente ou inválido.
  - Cache NÃO é invocado no path idempotent replay (step 3) — idempotency check tem prioridade.
  - Trace: Architecture §4.6 fluxo "HIT (5min TTL) → return cached response [classifier-only path]".

### 2. Cost router — `consultar_dados` direct-DB

- **AC4:** Criar `apps/web/src/lib/agent/cost-router.ts` com função `isSingleConsultarDados(intents: ClassificationResult['intents']): boolean`:
  - Retorna `true` se e só se `intents.length === 1 && intents[0].intent === 'consultar_dados'`.
  - Multi-intent envolvendo `consultar_dados` (ex: `['consultar_dados', 'criar_tarefa']`) retorna `false` — mantém pipeline completo executor.
  - Trace: Architecture §4.6 linha "if intents == [consultar_dados] AND read-only → query DB direct, no executor". ADR-009 §14.9.

- **AC5:** Criar mapa de query templates em `apps/web/src/lib/agent/cost-router.ts` — `CONSULTAR_DADOS_TEMPLATES` (objeto read-only):
  - Templates MVP mínimos (scope desta story — catálogo expandido em Epic 3/4):
    - `'count_tasks'`: `SELECT count(*) FROM tasks WHERE household_id = $1 AND completed_at IS NULL` — "quantas tarefas tenho?".
    - `'count_finances'`: `SELECT count(*) FROM transactions WHERE household_id = $1` — "quantas transacções tenho?".
    - `'balance_summary'`: `SELECT coalesce(sum(amount_cents), 0) AS total_cents FROM transactions WHERE household_id = $1` — "qual é o meu saldo?".
  - Função `executeDirectQuery(intent: 'consultar_dados', rawSpan: string | undefined, householdId: string, db: Database): Promise<DirectQueryResult>`:
    - Usar `getDb()` (authenticated, RLS aplicada via JWT) — NUNCA `getServiceDb()`.
    - Heurística simples sobre `rawSpan` para escolher template (ex: contains 'tarefa' → count_tasks, contains 'saldo'/'finanç' → balance_summary, default → count_tasks).
    - Retornar `{ kind: 'direct_query', data: rows, template_used: string }`.
    - Se query falhar, propagar erro (NÃO silenciar — retorna 500 ao caller).
  - Trace: Architecture §4.6, Architecture §13.1 "poupa ~40% das chamadas", `agentIntentEnum` em `packages/db/src/schema/agent.ts:52`.

- **AC6:** Integrar cost router em `apps/web/src/app/api/agent/prompt/route.ts` **após** o classifier (step 6 actual) e **antes** do Planner+Executor (step 8 actual):
  - Se `isSingleConsultarDados(classification.intents)` → chamar `executeDirectQuery` → construir response sem invocar Planner+Executor (bypass completo).
  - Response shape do direct-DB path: `{ mode: 'executed', run_id, results: directResult, summary: '...' }` — compatível com shape existente (sem `undo_url`/`undo_expires_at` — operação read-only não é reversível).
  - Registar `cost_router_bypass: true` em OTel span.
  - `incrementQuota` chamado **igualmente** no path direct-DB (quota é por prompt, não por LLM call).
  - Trace: Architecture §4.6, precedente `route.ts` pattern updateAfterExecutor + incrementQuota.

### 3. Quotas — hard-stop 110% + tier alignment + RLS fix

- **AC7:** Corrigir valores de `QUOTA_BY_PLAN` em `apps/web/src/lib/agent/rate-limiter.ts` para alinhar com Architecture §4.6 literal:

  | Plano | Valor actual (errado) | Valor correcto (Architecture §4.6) |
  |-------|----------------------|------------------------------------|
  | `free` | 100 | 50 |
  | `pessoal` | 500 | 1500 |
  | `familia` | 2000 | 3000 |
  | `pro` | 10000 | 10000 (correcto) |

  **[AUTO-DECISION D48 cross-confirm anti-hallucination]:** Os valores actuais em `rate-limiter.ts:40-45` divergem de Architecture §4.6 tabela "Quotas por plano (NFR20)". Esta é uma discrepância real no codebase descoberta via leitura do ficheiro real. Story 2.9 corrige.

- **AC8:** Corrigir `checkQuota` em `apps/web/src/lib/agent/rate-limiter.ts` para implementar hard-stop real a 110%:
  - Condição actual: `if (used >= limit)` (bloqueia a 100%).
  - Condição correcta: `if (used >= Math.floor(limit * 1.1))` (bloqueia a 110% — Architecture §4.6 literal "Hard-stop a 110%").
  - `QuotaExceededError` mensagem PT-PT: `"Limite de prompts atingido. Próxima janela em N min."` onde N é calculado como minutos até `period_end` (lido de `agent_quotas.period_end`).
  - Para calcular N min: necessita de ler `period_end` adicionalmente — expandir SELECT em `checkQuota` para `select plan, prompts_used, period_end from agent_quotas`.
  - Resposta HTTP 429 continua com body `{ code: 'QUOTA_EXCEEDED', message: '...', plan, used, limit }` (sem breaking change no handler — só a mensagem muda).
  - Trace: Architecture §4.6 "Hard-stop a 110%"; DP7 Trial=Família quota=Família (confirmado Epic plan §8).

- **AC9:** Corrigir `incrementQuota` em `apps/web/src/lib/agent/audit-log.ts` para usar `getServiceDb()` em vez de `getDb()`:
  - **[AUTO-DECISION D50 anti-hallucination crítico]:** Verificação directa de `packages/db/migrations/0001_rls_policies.sql:353-362` confirma que INSERT e UPDATE em `agent_quotas` são BLOQUEADOS para `authenticated` (policies `agent_quotas_insert_blocked: with check (false)` + `agent_quotas_update_blocked: using (false)`). O UPSERT actual em `incrementQuota` usa `getDb()` (authenticated) — falha silenciosamente em produção com RLS real Supabase. Deve migrar para `getServiceDb()` (service_role ignora RLS), análogo ao `markRunReverted` da Story 2.8 DN3.
  - `incrementQuota` continua não-fatal (try/catch + Pino warn + não bloqueia response) — mesma semântica da Story 2.6.
  - Trace: `0001_rls_policies.sql:353-362`, `apps/web/src/lib/agent/audit-log.ts:182-197`, Story 2.8 DN3 (padrão `getServiceDb()` para operações sistémicas).

- **AC10:** Integrar headers HTTP 429 standard em resposta QUOTA_EXCEEDED no handler principal (`route.ts`):
  - Adicionar header `X-Quota-Reset: <period_end ISO-8601>` à response 429 QUOTA_EXCEEDED.
  - Adicionar header `Retry-After: <segundos até period_end>`.
  - `period_end` é propagado via `QuotaExceededError` (adicionar campo `periodEnd: Date` ao constructor).
  - Trace: RFC 7231 §6.5.29 Retry-After, Architecture §7.2 "quota mensal por household".

### 4. Mensagens PT-PT + validação Trial=Família

- **AC11:** Verificar e documentar mapeamento Trial=Família (DP7: A):
  - Confirmação: `QUOTA_BY_PLAN['familia']` é aplicada a households com `plan = 'trialing'` ou `plan = 'familia'`?
  - Verificar `packages/db/src/schema/tenancy.ts` ou `billing.ts` para `planTierEnum` — confirmar se 'trialing' é um valor distinto ou se é coberto por um plano base.
  - Se `planTierEnum` não tem `'trialing'` como enum value separado, o mapeamento é: `agent_quotas.plan` é sempre um dos valores base do plano (`free`, `pessoal`, `familia`, `pro`); households em trial têm `plan = 'familia'` per DP7 Architecture §6.4. Zero alteração necessária — só documentar no código.
  - Trace: Epic plan §8 DP7 "Trial=Família quota=Família (3000 prompts/mês)", Architecture §6.4.

- **AC12:** Mensagem PT-PT estrita em todas as respostas de quota/rate limit:
  - `QuotaExceededError.message`: `"Limite de prompts atingido. Próxima janela em N min."` (PT-PT — nunca "Quota exceeded" em inglês).
  - `RateLimitError.message` actual: `"Rate limit excedido: X/Y pedidos por minuto. Tenta novamente em Ns."` — manter forma PT-PT, não alterar (já correcto).
  - Cache miss logs: `'Cache miss — a classificar'`, cache hit logs: `'Cache hit — a reutilizar classificação'` (PT-PT, Pino structured).
  - Cost router bypass log: `'Consulta directa DB — executor bypassed'` (PT-PT).
  - Trace: CLAUDE.md convenções PT-PT + NFR12, precedente DN8 Story 2.6.

### 5. Testes Vitest (mockable-only)

- **AC13:** Tests Vitest para `apps/web/src/lib/agent/cache.ts` em `__tests__/cache.test.ts` (novo ficheiro):
  - **(i)** `buildCacheKey` é determinístico: mesmo prompt + plano → mesmo hash.
  - **(ii)** `buildCacheKey` normaliza: prompt com espaços extra / maiúsculas → mesma key que versão normalizada.
  - **(iii)** `buildCacheKey` com planos diferentes → keys diferentes (household_plan é parte do hash).
  - **(iv)** `UpstashCache.get` retorna `null` quando `UPSTASH_REDIS_REST_URL` não está definido (modo degradado sem crash).
  - **(v)** `UpstashCache.get` + `set` com mock Upstash: `vi.mock('@upstash/redis')` — simular HIT e MISS correctamente.
  - **(vi)** Cache não logga prompt cleartext (assert que logs Pino NÃO contêm o prompt string original — só hash).
  - Estimate ≥6 testes.

- **AC14:** Tests Vitest para `apps/web/src/lib/agent/cost-router.ts` em `__tests__/cost-router.test.ts` (novo ficheiro):
  - **(i)** `isSingleConsultarDados` retorna `true` para `[{intent: 'consultar_dados', ...}]`.
  - **(ii)** `isSingleConsultarDados` retorna `false` para array vazio, multi-intent, outras intents.
  - **(iii)** `executeDirectQuery` com `rawSpan` contendo 'tarefa' → executa `count_tasks` template, retorna count.
  - **(iv)** `executeDirectQuery` propaga erro DB (não swallows) — mock DB throw → assert throw propagado.
  - **(v)** `executeDirectQuery` usa `getDb()` (authenticated) — NÃO `getServiceDb()` — mock assert.
  - Estimate ≥5 testes.

- **AC15:** Tests Vitest para fixes em `rate-limiter.ts` + `audit-log.ts` em ficheiros existentes:
  - **`__tests__/rate-limiter.test.ts`** (expandir ou criar):
    - **(i)** `checkQuota` bloqueia a 110% da quota (ex: familia → bloqueia quando `used >= 3300`, não `3000`).
    - **(ii)** `checkQuota` passa quando `used = 3299` (familia — abaixo de 110%).
    - **(iii)** `QUOTA_BY_PLAN` valores correctos: `{free:50, pessoal:1500, familia:3000, pro:10000}`.
    - **(iv)** `QuotaExceededError.message` contém "Limite de prompts atingido" + "Próxima janela em".
  - **`__tests__/audit-log.test.ts`** (expandir ou criar):
    - **(i)** `incrementQuota` chama `getServiceDb()` — NÃO `getDb()` (mock assert via spy/stub).
  - Estimate ≥5 testes.

- **AC16:** Integração em `route.ts` — testes de contrato (expandir `__tests__/route.test.ts` ou `prompt.test.ts` existente):
  - **(i)** Request com cache HIT → classifier NÃO chamado (mock Upstash.get → retorna ClassificationResult JSON).
  - **(ii)** Request com intent `consultar_dados` singleton → Planner+Executor NÃO chamados (mock cost router bypass).
  - **(iii)** Request quando `prompts_used >= 110% * limit` → 429 com header `X-Quota-Reset` + `Retry-After`.
  - Estimate ≥3 testes.

- **AC17 — Quality gates 5/5 GREEN:**
  - `pnpm typecheck` — 9/9 packages PASS, 0 errors.
  - `pnpm lint` — 9/9 packages PASS, 0 warnings (`--max-warnings=0`).
  - `pnpm test` — ≥14 testes novos (AC13 6 + AC14 5 + AC15 5 parcial + AC16 3 = ≥14) + ≥153 preservados (baseline Story 2.8 Done).
  - `pnpm check:rls` — 28 tabelas / 120 policies NFR5 PRESERVADA (zero novas tabelas nesta story).
  - `pnpm build` — Next.js 14 routes FULL TURBO sem erros.

---

## [AUTO-DECISIONS] @sm

> Decisões arquitecturais documentadas pelo @sm durante drafting. Numeração contínua desde Story 2.8 D43. @architect ratifica no gate; @po cross-confirma anti-hallucination no validate.

### [AUTO-DECISION D44] UpstashClientLike mockable — zero call real em tests (precedente 2.2-2.8)

**Decisão:** Cliente Upstash Redis implementado via interface `UpstashClientLike` injectável (padrão D8/D13/D22 stories anteriores). Em tests, `vi.mock('@upstash/redis')` é a estratégia. Em modo degradado (env vars ausentes), `UpstashCache` retorna `null` em `get` sem throw.

**Justificação:** EB3 pending. Zero CI infra extra. Padrão estabelecido em 6 stories Done APPROVED 9.4/10. `UpstashClientLike` interface permite swap futuro para outros redis clients sem alterar o caller.

**Trace:** Stories 2.2-2.8 precedente mockable-only, Architecture §4.6 "Redis cache 5min".

### [AUTO-DECISION D45] SHA256 cache key via `node:crypto` (Edge-compatible)

**Decisão:** `node:crypto.createHash('sha256')` — não `crypto.subtle.digest` (async). `node:crypto` está disponível em Next.js Edge Runtime (Vercel `fra1`) e em Vitest `node` environment sem polyfill. `crypto.subtle` requer await + Buffer conversion adicional sem vantagem.

**Justificação:** Consistência com precedente `hashPrompt` em `apps/web/src/lib/agent/redaction.ts` que usa `node:crypto` (Story 2.6 D25). Re-usar mesmo módulo no mesmo codebase.

**Trace:** `apps/web/src/lib/agent/redaction.ts:hashPrompt` (Story 2.6), Next.js Edge Runtime docs `node:crypto` support.

### [AUTO-DECISION D46] Cache TTL 300s default — configurável via `CACHE_TTL_SECONDS`

**Decisão:** TTL de 5 minutos (`300s`) conforme Architecture §4.6. Configurável via env `CACHE_TTL_SECONDS` (fallback `300`) para facilitar override em staging/dev sem deploy.

**Justificação:** Architecture §4.6 literal "cache Upstash 5min". Env var override é padrão AIOX para configuração não-estrutural (precedente `AGENT_RATE_LIMIT_PER_MINUTE` em rate-limiter.ts).

**Trace:** Architecture §4.6, `rate-limiter.ts:28-31` (padrão env var override).

### [AUTO-DECISION D47] Cost router bypass APENAS para intent singleton `consultar_dados`

**Decisão:** `isSingleConsultarDados` retorna `true` APENAS quando `intents.length === 1 AND intent === 'consultar_dados'`. Multi-intent com `consultar_dados` mantém pipeline completo. "Consultar + criar" num mesmo prompt usa executor Sonnet.

**Justificação:** Architecture §4.6 literal "if intents == [consultar_dados] AND read-only → query DB direct, no executor". O "==" implica singleton — multi-intent não foi explicitamente incluído no design e o executor é necessário para os side-effects das outras intents.

**Trace:** Architecture §4.6 algoritmo de routing, ADR-009 §14.9.

### [AUTO-DECISION D48] Correcção `QUOTA_BY_PLAN` — valores actuais vs Architecture §4.6

**Decisão:** Corrigir `QUOTA_BY_PLAN` em `rate-limiter.ts` de `{free:100, pessoal:500, familia:2000, pro:10000}` para `{free:50, pessoal:1500, familia:3000, pro:10000}`.

**Justificação anti-hallucination:** Leitura directa de `apps/web/src/lib/agent/rate-limiter.ts:40-45` confirma valores actuais. Architecture §4.6 tabela "Quotas por plano (NFR20)" especifica `{Free:50, Pessoal:1500, Família:3000, Pro:10000}`. Divergência é real — Story 2.9 corrige. Esta é uma correcção de alinhamento com spec, não uma feature nova.

**Trace:** `rate-limiter.ts:40-45` (estado actual codebase), Architecture §4.6 (spec correcta).

### [AUTO-DECISION D49] Hard-stop 110% — `Math.floor(limit * 1.1)`

**Decisão:** Condição hard-stop: `used >= Math.floor(limit * 1.1)`. Exemplo família: `3000 * 1.1 = 3300`.

**Justificação:** Architecture §4.6 literal "Hard-stop a 110% para evitar abuso". `Math.floor` garante inteiro (prompts são inteiros). Actualmente `checkQuota` usa `>= limit` (100%) — correcção alinha com spec.

**Trace:** Architecture §4.6, `rate-limiter.ts:149` (condição actual `if (used >= limit)`).

### [AUTO-DECISION D50] `incrementQuota` deve usar `getServiceDb()` (fix RLS crítico)

**Decisão:** Migrar `incrementQuota` de `getDb()` para `getServiceDb()` (service_role) porque INSERT/UPDATE em `agent_quotas` são BLOQUEADOS para `authenticated` via RLS.

**Justificação anti-hallucination crítica:** Verificação directa de `packages/db/migrations/0001_rls_policies.sql:353-362` confirma:
- `agent_quotas_insert_blocked` on `agent_quotas` FOR INSERT to authenticated: `with check (false)` — bloqueia TODO insert por authenticated.
- `agent_quotas_update_blocked` on `agent_quotas` FOR UPDATE to authenticated: `using (false) with check (false)` — bloqueia todo update por authenticated.
- Comentário na migration: "Apenas service_role gere counters (atomicidade + race conditions)."

O UPSERT actual em `audit-log.ts:182-197` usa `getDb()` (authenticated) → falha silenciosamente em produção Supabase com RLS enforced. Classificado como bug latente descoberto via leitura real do codebase. Fix: `getServiceDb()` análogo a `markRunReverted` (Story 2.8 DN3).

**Trace:** `0001_rls_policies.sql:342-362`, `audit-log.ts:182-197`, Story 2.8 DN3 padrão getServiceDb para operações sistémicas.

### [AUTO-DECISION D51] CodeRabbit pre-commit/push skipped (consistente 2.2-2.8)

**Decisão:** Consistente com D12/D14/D22/D26/D33/D42. @architect gate substitui CodeRabbit (APPROVED 9.4/10 padrão).

**Justificação:** 2.9 é primariamente lógica de routing + cache client + quota fixes — padrões estabelecidos. CodeRabbit re-baseline agendada para Story 2.10 (benchmark + credentials reais Anthropic/OpenAI — focus area ideal).

### [AUTO-DECISION D52] Vitest mocks para Upstash — `vi.mock('@upstash/redis')`

**Decisão:** Mocks Vitest determinísticos via `vi.mock('@upstash/redis')` — não Upstash Dev Server local. `UpstashClientLike` interface permite mock via objeto simples com métodos `get`/`set`/`del`.

**Justificação:** EB3 pending. Padrão estabelecido em todas as stories 2.2-2.8 (LLM mocks, Inngest mocks, DB mocks). Zero CI infra extra. Tests deterministicos e rápidos.

**Trace:** Story 2.8 D43 (Vitest mocks Inngest), Story 2.4 D12 (Vitest mocks OpenAI).

### [AUTO-DECISION D53] Cache key usa `householdPlan` (não `householdId`) — correcção vs spawn prompt

**Decisão:** Cache key = `sha256(normalize(prompt) + householdPlan)` conforme Architecture §4.6. O spawn prompt desta story alegou `sha256(prompt + household_id)` — discrepância corrigida aqui.

**Justificação anti-hallucination:** Architecture §4.6 literal: `sha256(normalize(prompt) + household_plan)`. Usar `household_id` criaria uma cache per-household (muito granular, hit rate baixo). Usar `household_plan` partilha cache entre users do mesmo plano (classificação de intents não é PII-sensitive — é apenas a lista de intents detectadas). Correcto para maximizar hit rate (R7 mitigação).

**Trace:** Architecture §4.6 linha "sha256(normalize(prompt) + household_plan)".

### [AUTO-DECISION D54] `consultar_dados` direct-DB usa `getDb()` (authenticated) com RLS

**Decisão:** `executeDirectQuery` usa `getDb()` (authenticated, RLS aplicada) — NUNCA `getServiceDb()`. Queries são read-only SELECT com `household_id = $1` onde `$1` vem do JWT claim `current_household_id()`.

**Justificação:** `consultar_dados` é operação de leitura de dados do utilizador — RLS via authenticated é o mecanismo correcto (garante que utilizador só vê os próprios dados). `getServiceDb()` seria bypass de segurança inaceitável para reads de dados do utilizador. Análogo a `checkQuota` que usa `getDb()` para SELECT em `agent_quotas` (mas UPDATE/INSERT precisam de service_role — AC9 D50).

**Trace:** Architecture NFR5, CLAUDE.md "getDb() em todas as rotas/RSC/Server Actions com utilizador final", D50 distingue claramente reads (getDb) vs writes (getServiceDb) em agent_quotas.

---

## Tasks

### T1 — Cache Upstash client (`apps/web/src/lib/agent/cache.ts`)

- [ ] Criar `apps/web/src/lib/agent/cache.ts` (novo ficheiro):
  - [ ] Interface `UpstashClientLike` com métodos `get`, `set`, `del`
  - [ ] Classe `UpstashCache` implementando `UpstashClientLike` via `@upstash/redis`
  - [ ] Modo degradado: se env vars ausentes, `get` retorna `null` sem throw
  - [ ] `buildCacheKey(prompt, householdPlan): string` com `node:crypto` SHA256
  - [ ] `normalize(prompt): string` inline (trim + lowercase + collapse whitespace)
  - [ ] Export `const cacheClient: UpstashClientLike` — lazy singleton
  - [ ] `CACHE_TTL_SECONDS` constante com env override

### T2 — Cost router (`apps/web/src/lib/agent/cost-router.ts`)

- [ ] Criar `apps/web/src/lib/agent/cost-router.ts` (novo ficheiro):
  - [ ] `isSingleConsultarDados(intents): boolean` — singleton check
  - [ ] `CONSULTAR_DADOS_TEMPLATES` — 3 templates MVP (count_tasks, count_finances, balance_summary)
  - [ ] `selectTemplate(rawSpan: string | undefined): string` — heurística simples keyword matching
  - [ ] `executeDirectQuery(intent, rawSpan, householdId, db): Promise<DirectQueryResult>` — usa `getDb()`
  - [ ] Tipo `DirectQueryResult: { kind: 'direct_query'; data: unknown[]; template_used: string }`

### T3 — Fix `checkQuota` e `QUOTA_BY_PLAN` em `rate-limiter.ts`

- [ ] Corrigir `QUOTA_BY_PLAN` para `{free:50, pessoal:1500, familia:3000, pro:10000}` (AC7)
- [ ] Corrigir condição `checkQuota`: `>= Math.floor(limit * 1.1)` (AC8 — hard-stop 110%)
- [ ] Expandir SELECT em `checkQuota` para incluir `period_end` (para mensagem "Próxima janela em N min")
- [ ] Actualizar `QuotaExceededError` com campo `periodEnd: Date` + mensagem PT-PT correcta (AC8, AC12)
- [ ] Actualizar `route.ts` handler QUOTA_EXCEEDED para adicionar headers `X-Quota-Reset` + `Retry-After` (AC10)

### T4 — Fix `incrementQuota` em `audit-log.ts` (RLS crítico)

- [ ] Mudar `incrementQuota` de `getDb()` para `getServiceDb()` (AC9 / D50)
- [ ] Importar `getServiceDb` em `audit-log.ts` (verificar se já importado; se não, adicionar)
- [ ] Manter semântica não-fatal (try/catch + Pino warn — sem breaking change)

### T5 — Integração em `route.ts` (cache + cost router)

- [ ] Após step 5 (INSERT agent_run), calcular `householdPlan` (ler de `agent_quotas` ou `households` tabela)
- [ ] Calcular `cacheKey = buildCacheKey(body.prompt, householdPlan)`
- [ ] Step 5b (novo): cache lookup `cacheClient.get(cacheKey)` — HIT → bypass classifier, registar span
- [ ] Step 6 (existente): se MISS, executar classifier + `cacheClient.set(cacheKey, ...)`
- [ ] Step 6b (novo, após classifier): `isSingleConsultarDados` check → `executeDirectQuery` se true
- [ ] Response path direct-DB: construir response `mode: 'executed'` sem undo_url (read-only)
- [ ] Registar `cache_hit`, `cost_router_bypass` em OTel span
- [ ] `incrementQuota` chamado igualmente no path direct-DB

### T6 — Verificar e documentar Trial=Família (AC11)

- [ ] Ler `packages/db/src/schema/tenancy.ts` — verificar `planTierEnum` values e como trial é representado
- [ ] Documentar no código (comentário em `rate-limiter.ts:QUOTA_BY_PLAN`) o mapeamento Trial=Família
- [ ] Confirmar que `agent_quotas.plan` recebe `'familia'` para households em trial (Architecture §6.4 DP7)

### T7 — Tests Vitest (mockable-only)

- [ ] Criar `apps/web/src/lib/agent/__tests__/cache.test.ts` (≥6 testes — AC13)
- [ ] Criar `apps/web/src/lib/agent/__tests__/cost-router.test.ts` (≥5 testes — AC14)
- [ ] Expandir/criar `apps/web/src/lib/agent/__tests__/rate-limiter.test.ts` (≥4 testes — AC15 rate-limiter)
- [ ] Expandir/criar `apps/web/src/lib/agent/__tests__/audit-log.test.ts` (≥1 teste — AC15 audit-log)
- [ ] Expandir `apps/web/src/app/api/agent/prompt/__tests__/` (≥3 testes integração — AC16)

### T8 — Runbook Upstash EU Frankfurt (deferred EB3)

- [ ] Criar `docs/runbooks/upstash-setup.md` (novo ficheiro):
  - [ ] Provisionamento conta Upstash — criar database Redis em `eu-west-1` (Frankfurt)
  - [ ] Copiar `UPSTASH_REDIS_REST_URL` + `UPSTASH_REDIS_REST_TOKEN` do dashboard
  - [ ] Popular em `apps/web/.env.local` (dev) + Vercel env vars (prod)
  - [ ] Smoke test: `curl $UPSTASH_REDIS_REST_URL/set/test/hello -H "Authorization: Bearer $UPSTASH_REDIS_REST_TOKEN"`
  - [ ] Nota: database no Vercel `fra1` region para minimizar latência

### T9 — Instalar dependência `@upstash/redis`

- [ ] `pnpm --filter @meu-jarvis/web add @upstash/redis` (versão latest stable)
- [ ] Confirmar `apps/web/package.json` adiciona `@upstash/redis`
- [ ] `pnpm install` no root (workspace lockfile actualizado)

### T10 — Quality gates 5/5 GREEN (AC17)

- [ ] `pnpm typecheck` — 0 erros
- [ ] `pnpm lint` — 0 warnings (`--max-warnings=0`)
- [ ] `pnpm test` — ≥14 novos + ≥153 preservados passam
- [ ] `pnpm check:rls` — 28 tabelas / 120 policies NFR5 PRESERVADA
- [ ] `pnpm build` — 14 routes sem erros

---

## Critical Dev Notes

**DN1 — Mockable-only ZERO calls reais:** `UpstashClientLike` interface injectável em todos os testes. `vi.mock('@upstash/redis')` cobre o cliente real. Em CI (sem env vars Upstash), `UpstashCache` cai em modo degradado (sempre MISS) sem throw. Zero chamadas reais a Upstash em qualquer test.

**DN2 — Cache key é `household_plan`, NÃO `household_id`:** Architecture §4.6 literal. Dois utilizadores do mesmo plano partilham cache de ClassificationResult (intents — não PII). Maximiza hit rate. Ver D53.

**DN3 — SHA256 via `node:crypto` (precedente redaction.ts):** `import { createHash } from 'node:crypto'; createHash('sha256').update(input).digest('hex')`. Mesmo módulo usado em `redaction.ts` hashPrompt. NÃO usar `crypto.subtle` (async desnecessário).

**DN4 — NUNCA logar prompt cleartext em cache logs:** O `cacheKey` (hash) pode ser logado. O `body.prompt` original nunca aparece em logs Pino (NFR12 PII). Ver precedente DN5 Story 2.6.

**DN5 — `agent_quotas` INSERT/UPDATE requer `getServiceDb()`:** RLS bloqueia authenticated. Crítico fix em `incrementQuota`. Ver D50 + AC9. `getDb()` pode ser mantido para SELECT em `checkQuota` (read-only é permitido per `agent_quotas_select_member` policy).

**DN6 — `consultar_dados` direct-DB usa `getDb()` (authenticated + RLS):** Leitura de dados do utilizador DEVE usar authenticated com RLS. Nunca `getServiceDb()` para reads de domínio. Ver D54.

**DN7 — Hard-stop 110%:** `Math.floor(limit * 1.1)` — família: 3300. Testar limite exacto (3299 passa, 3300 bloqueia). Ver D49.

**DN8 — `QUOTA_BY_PLAN` correcto pós-fix:** `{free:50, pessoal:1500, familia:3000, pro:10000}`. Os valores actuais no codebase estão errados vs Architecture §4.6. Ver D48.

**DN9 — Cost router path sem `undo_url`/`undo_expires_at`:** Operação `consultar_dados` é read-only — não tem reverse_op, não tem undo. Response shape: `{ mode: 'executed', run_id, results: directResult, summary: '...' }` sem campos undo. Client (ResultMessage) já lida com `undoUrl` ausente (D40 Story 2.8).

**DN10 — `incrementQuota` chamado no path direct-DB:** Quota é por prompt processado (inclui direct-DB). O contador `prompts_used` deve incrementar independentemente de LLM ter sido chamado ou não.

**DN11 — `householdPlan` necessário para `cacheKey`:** Para calcular `cacheKey`, `route.ts` precisa do `plan` do household. Pode ser lido de `agent_quotas.plan` (se row existe) ou de `households.plan`. Preferir `households.plan` via uma query simples (fallback: `'free'` se não encontrado, para ser conservador).

**DN12 — `period_end` em `QuotaExceededError`:** Para mensagem "Próxima janela em N min", `checkQuota` precisa de ler `period_end` da `agent_quotas`. Se row não existe, usar `date_trunc('month', now()) + interval '1 month'` como estimativa. N min = `Math.ceil((period_end - now()) / 60000)`.

**DN13 — PT-PT estrito:** "Limite de prompts atingido" (não "Quota excedida"). "Próxima janela em" (não "Próximo período"). "Cache hit — a reutilizar classificação" (não "Cache hit — reusing"). Consultar vocabulário Stories 2.6/2.7/2.8 para consistência.

**DN14 — Templates `consultar_dados` são MVP mínimo:** 3 templates (count_tasks, count_finances, balance_summary) cobrem os casos mais frequentes. Catálogo expandido em Epic 3/4 quando tools de domínio completas estiverem implementadas. Story 2.9 NÃO inventa templates além destes 3.

**DN15 — Estrutura de ficheiros esperada (lib/agent/):**

```
apps/web/src/lib/agent/
├── audit-log.ts          [MODIFICADO — fix incrementQuota getServiceDb]
├── cache.ts              [NOVO — UpstashClientLike + buildCacheKey]
├── cost-router.ts        [NOVO — isSingleConsultarDados + executeDirectQuery]
├── db-shim.ts            [NÃO TOCADO]
├── idempotency.ts        [NÃO TOCADO]
├── rate-limiter.ts       [MODIFICADO — QUOTA_BY_PLAN fix + hard-stop 110%]
├── redaction.ts          [NÃO TOCADO]
├── tracing.ts            [NÃO TOCADO]
└── __tests__/
    ├── cache.test.ts           [NOVO — AC13]
    ├── cost-router.test.ts     [NOVO — AC14]
    ├── rate-limiter.test.ts    [NOVO ou EXPANDIDO — AC15]
    └── audit-log.test.ts       [NOVO ou EXPANDIDO — AC15]
```

---

## Referências

- `docs/architecture.md` §4.6 — algoritmo de routing, quotas por plano, cache key, ADR-009
- `docs/architecture.md` §13.1 — modelo de custo, `consultar_dados` poupa ~40% chamadas
- `docs/architecture.md` §14.9 ADR-009 — cost router decision summary
- `docs/prd.md` NFR20 — custo LLM ≤ 30% MRR Pessoal
- `docs/epics/epic-2-cerebro-ai-multi-intent.md` §5 row 2.9, §8 DP7, R3, R7
- `docs/epics/EPIC-2-EXECUTION.yaml` Story 2.9 (linhas 249-263)
- `apps/web/src/lib/agent/rate-limiter.ts` — `checkQuota`, `QUOTA_BY_PLAN`, `QuotaExceededError`
- `apps/web/src/lib/agent/audit-log.ts` — `incrementQuota` (fix D50)
- `apps/web/src/app/api/agent/prompt/route.ts` — ponto de integração das 3 features
- `packages/db/src/schema/agent.ts:240-265` — `agentQuotas` Drizzle schema (JÁ EXISTE)
- `packages/db/migrations/0001_rls_policies.sql:342-362` — RLS policies agent_quotas (INSERT/UPDATE BLOCKED authenticated)
- `.env.example:61-62` — `UPSTASH_REDIS_REST_URL` + `UPSTASH_REDIS_REST_TOKEN`
- `docs/stories/completed/2.8.undo-inngest-cleanup-ui.md` — precedente formato + D43 Vitest mocks

---

## Anti-Hallucination Cross-Confirm Report

| # | Alegação vs codebase | Resultado | Acção |
|---|----------------------|-----------|-------|
| H1 | Spawn prompt alega: "T1 necessita migration nova agent_quotas + 4 RLS policies" | **HALLUCINATION** — `agent_quotas` já existe em `packages/db/src/schema/agent.ts:240-265` + 4 RLS em `0001:342-362` | AC e Tasks ajustados: zero migration nova |
| H2 | Spawn prompt alega: `sha256(prompt + household_id)` como cache key | **HALLUCINATION** — Architecture §4.6 literal é `sha256(normalize(prompt) + household_plan)` | AC2 e D53 corrigem com trace |
| H3 | `incrementQuota` usa `getDb()` — OK porque RLS permite | **BUG LATENTE** — `0001:353-362` bloqueia INSERT/UPDATE em agent_quotas para authenticated. `getDb()` falha em produção. | AC9 + D50 fixam para getServiceDb() |
| H4 | `QUOTA_BY_PLAN` está correcto | **ERRADO** — valores actuais `{free:100, pessoal:500, familia:2000}` divergem de Architecture §4.6 `{free:50, pessoal:1500, familia:3000}` | AC7 + D48 corrigem |
| H5 | `checkQuota` implementa hard-stop 110% | **INCOMPLETO** — condição actual é `>= limit` (100%), não 110% | AC8 + D49 corrigem |
| H6 | Cache e cost router não existem no codebase | **CONFIRMADO** — glob e find em `apps/web/src/lib/agent/` confirmam ausência de `cache.ts` e `cost-router.ts` | Scope correcto — T1+T2 criam ficheiros novos |

---

## File List (antecipada)

**Novos:**
- `apps/web/src/lib/agent/cache.ts`
- `apps/web/src/lib/agent/cost-router.ts`
- `apps/web/src/lib/agent/__tests__/cache.test.ts`
- `apps/web/src/lib/agent/__tests__/cost-router.test.ts`
- `apps/web/src/lib/agent/__tests__/rate-limiter.test.ts` (pode ser novo se não existe)
- `apps/web/src/lib/agent/__tests__/audit-log.test.ts` (pode ser novo se não existe)
- `docs/runbooks/upstash-setup.md`

**Modificados:**
- `apps/web/src/lib/agent/rate-limiter.ts` (QUOTA_BY_PLAN + hard-stop 110% + period_end)
- `apps/web/src/lib/agent/audit-log.ts` (incrementQuota → getServiceDb)
- `apps/web/src/app/api/agent/prompt/route.ts` (cache + cost router integração + headers 429)
- `apps/web/src/app/api/agent/prompt/__tests__/` (expandir testes integração)
- `apps/web/package.json` (+ @upstash/redis)
- `pnpm-lock.yaml` (lockfile actualizado)

**Total estimado:** 7 novos + 6 modificados = 13 ficheiros

---

## Definition of Done

Inferível dos ACs — todos os critérios abaixo devem ser PASS para marcar Ready for Review:

1. `apps/web/src/lib/agent/cache.ts` existe com `UpstashClientLike`, `UpstashCache`, `buildCacheKey` — AC1, AC2
2. Cache lookup integrado em `route.ts` antes do classifier — AC3
3. `apps/web/src/lib/agent/cost-router.ts` existe com `isSingleConsultarDados` + 3 templates — AC4, AC5
4. Cost router bypass integrado em `route.ts` após classifier — AC6
5. `QUOTA_BY_PLAN` corrigido para `{free:50, pessoal:1500, familia:3000, pro:10000}` — AC7
6. `checkQuota` usa `>= Math.floor(limit * 1.1)` + mensagem PT-PT correcta — AC8
7. `incrementQuota` usa `getServiceDb()` — AC9
8. Headers `X-Quota-Reset` + `Retry-After` em 429 QUOTA_EXCEEDED — AC10
9. Trial=Família documentado em código — AC11
10. Mensagens PT-PT estritamente correctas — AC12
11. ≥14 testes novos todos passing — AC13-AC16
12. 5/5 quality gates GREEN — AC17
13. Runbook `docs/runbooks/upstash-setup.md` criado — T8

---

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-05-12 | 1.0 | Draft inicial — 17 ACs, 10 Tasks, 11 AUTO-DECISIONS D44-D54, 6 cross-confirms, 6 hallucinations detectadas e corrigidas | @sm (River) |
