# Story 2.8 — Undo Mechanism (FR6) + Inngest Cleanup + UI Undo Button

**Status:** Ready
**Epic:** Epic 2 — Cérebro AI Multi-Intent
**Owner:** @sm (draft) → @po (validate ✓) → @dev (impl) → @architect (gate) → @devops (push)
**Estimate:** M
**Version:** 1.1
**Created:** 2026-05-11
**Author:** @sm (River) — drafted by Orion (@aiox-master) per Constitution Article II universal executor (precedente Stories 2.2 + 2.7 D28)
**Validated:** 2026-05-11 by @po (Pax) — **GO 9.0/10 HIGH confidence**, 3 PO_FIX_INLINE aplicados

---

## Executor Assignment

| Phase | Owner | Output |
|-------|-------|--------|
| Draft | @sm (delegated Orion) | `2.8.undo-inngest-cleanup-ui.md` v1.0 |
| Validate | @po (Pax) | GO/NO-GO + 10-point checklist + anti-hallucination cross-confirm |
| Implement | @dev (Dex) | Inngest setup + `cleanup_expired_reverse_ops` function + UI Undo button + audit_log INSERT + ≥30 testes novos |
| Quality Gate | @architect (Aria) | APPROVED / CONCERNS / FAIL (cross-domain backend infra + frontend) |
| Push | @devops (Gage) | `git push origin main` |

**Type-to-executor consistency PASS:** scope M cross-domain (Inngest backend infra + UI frontend Undo button + audit_log) → @dev (full-stack) com gate dedicado @architect. Sem scope-shift L (precedente 2.7 D28 3-conditions framework: UI Undo é minimal — substituir placeholder existente + countdown + toast — não justifica scope-shift M→L).

---

## Bloqueadores Externos

**EB4 — Inngest function `cleanup_expired_reverse_ops` (DEFERRED, não bloqueante):** Inngest é registado pela primeira vez nesta story (zero código actual — confirmado por grep zero matches em `apps/`, `packages/`). Arquitectura §11.3 e ADR-005 (§14.5) ratificam Inngest EU como background-jobs provider. `.env.example` já tem `INNGEST_EVENT_KEY` + `INNGEST_SIGNING_KEY` (vazios) — Eurico DEVE provisionar workspace Inngest (EU Frankfurt) + popular secrets no Vercel antes do push @devops. Implementação local (`@dev`) pode usar Inngest dev server (`npx inngest-cli dev`) com keys vazias em CI/dev — verificado no quickstart docs Inngest.

**EB1/EB2/EB3 PENDING não afectam 2.8:** pipeline AI continua mockable-only (precedente 2.2-2.7). Story 2.8 NÃO toca LLM real.

---

## User Story

**Como** utilizador autenticado de um household que acabou de submeter um prompt no `/jarvis` e viu uma mensagem "Feito ✓" no chat,
**Quero** clicar num botão **Anular** visível durante 30 segundos após a execução (com contador decrescente claro), receber confirmação imediata de que a operação foi revertida (ou mensagem PT-PT clara se já expirou) e ver o sistema limpar automaticamente registos antigos de reverse_ops para garantir que a base de dados não cresce indefinidamente,
**Para que** eu tenha controlo total sobre escritas irreversíveis do agente AI (FR6) e confie em deixá-lo executar prompts sem confirmação intermédia (NFR12 user trust + R5 mitigação "undo race condition").

---

## Dependências

**Internas (Done):**

- Story 1.5 ✓ Auth + RLS + `current_household_id()` JWT claim — auth gate via `createServerSupabaseClient`
- Story 1.7 ✓ Observability — reuse `withSpan` + Pino `childLogger` + Sentry `captureException`
- Story 2.1 ✓ Schema agent (`agent_reverse_ops` com `expires_at` 30s default + `executed_at` nullable; `agent_runs.status='reverted'` + `reverted_at` columns)
- Story 2.5 ✓ Planner + Executor (`AtomicOutcome` com `results` array que ResultMessage consome)
- Story 2.6 ✓ **Endpoint `POST /api/agent/prompt/[runId]/undo` JÁ IMPLEMENTADO (AC7) + 9 testes Vitest** — Story 2.8 NÃO duplica endpoint, apenas adiciona Inngest cleanup + UI Undo button + audit_log INSERT
- Story 2.6 ✓ Endpoint principal `POST /api/agent/prompt` retorna `{ mode: 'executed', run_id, results, summary, undo_url, undo_expires_at }` (route.ts:420-427 verificado) — UI consome estes dois novos campos
- Story 2.7 ✓ ResultMessage component (`apps/web/src/app/(app)/jarvis/_components/result-message.tsx`) com placeholder Undo button (`disabled`, tooltip "Em breve") + comment `Story 2.8` na linha 32

**Externas:** EB4 (Inngest workspace EU + secrets) — DEFERRED, não bloqueia drafting/impl mockable-only.

---

## Acceptance Criteria

### Backend — Inngest cleanup function

- **AC1:** Adicionar dependência `inngest` (latest stable, ^3.x) a `apps/web/package.json`. Criar `apps/web/src/lib/inngest/client.ts` com `Inngest` client export (`new Inngest({ id: 'expressia-web', name: 'Expressia Web' })`). Trace: Architecture §11.3 + ADR-005 (§14.5).

- **AC2:** Criar Inngest function `cleanupExpiredReverseOps` em `apps/web/src/lib/inngest/functions/cleanup-expired-reverse-ops.ts` com:
  - **Trigger:** `inngest.createFunction({ id: 'cleanup-expired-reverse-ops' }, { cron: '0 3 * * *' }, handler)` — cron diário 03:00 UTC (alinhado com `apps/web/vercel.json` cron `/api/cron/daily` per CLAUDE.md).
  - **Handler:** `DELETE FROM agent_reverse_ops WHERE expires_at < now() - interval '1 hour'` via `getServiceDb()` (RLS bypass justificado para job sistémico — pattern análogo a GDPR purge mensal).
  - **Idempotência:** DELETE com WHERE condicional é naturalmente idempotente (rerun no mesmo dia = 0 rows deleted).
  - **Observability:** Pino structured logger via `childLogger({ job: 'cleanup-expired-reverse-ops' })` regista `rows_deleted` count + `step.run` OTel span (Inngest auto-creates spans per step).
  - Trace: Architecture §4.5 linha 425 "Job Inngest: limpa `agent_reverse_ops` com `expires_at < now() - 1h` diariamente".

- **AC3:** Criar route handler `apps/web/src/app/api/inngest/route.ts` que expõe a Inngest function via `serve` helper (`import { serve } from 'inngest/next'; export const { GET, POST, PUT } = serve({ client, functions: [cleanupExpiredReverseOps] })`). Endpoint público (auth bypass — Inngest signing key valida requests via `INNGEST_SIGNING_KEY`). Trace: Inngest Next.js quickstart docs.

- **AC4:** Documentar setup Inngest em `docs/runbooks/inngest-setup.md` (novo ficheiro): provisionamento workspace EU Frankfurt, criação de event/signing keys, registo do endpoint `https://expressia.pt/api/inngest` no dashboard, secrets em Vercel (`INNGEST_EVENT_KEY` + `INNGEST_SIGNING_KEY`). Status: instruções para @devops Eurico-side antes do push.

### Backend — Audit log INSERT no Undo

- **AC5:** Refactor `apps/web/src/app/api/agent/prompt/[runId]/undo/route.ts` para fazer INSERT em `audit_log` (`@meu-jarvis/db` schema `auditLog`) imediatamente após o `UPDATE agent_runs SET status='reverted', reverted_at=now()` bem-sucedido:
  - `action: 'agent_run_reverted'` — **enum value JÁ EXISTE** em `packages/db/src/schema/audit.ts:54` (verificado: `agent_run_reverted`); zero enum extension migration necessária.
  - `household_id: run.household_id`, `user_id: user.id`, `entity_table: 'agent_runs'`, `entity_id: runId`.
  - `before_state: { status: 'success', ops_count: opRows.length }`, `after_state: { status: 'reverted', reverted_at: <ISO 8601> }`.
  - `trace_id: span.spanContext().traceId` (reuse via `withAgentPromptSpan`), `ip` + `user_agent` deixados null (não-críticos para audit MVP — Story 2.x housekeeping pode popular via `request.headers`).
  - **[PO_FIX_INLINE v1.1 — audit_log INSERT pode usar `getDb()` (authenticated), não `getServiceDb()`]** INSERT via `getDb()` (`authenticated` role). **Justificação anti-hallucination:** verificação directa em `packages/db/migrations/0001_rls_policies.sql:569-573` mostra que `audit_log_insert_member` policy PERMITE INSERT a `authenticated` SE `is_household_member(household_id) OR household_id IS NULL`. Apenas UPDATE/DELETE são revogados (linhas 576-584 — `audit_log_update_blocked` + `audit_log_delete_blocked` + `revoke update, delete from authenticated`). NFR9 imutabilidade aplica-se a UPDATE/DELETE (append-only), NÃO ao INSERT. Pattern: análogo a outras tabelas com RLS member-scoped INSERT — RLS valida `household_id` da row inserida bate com `current_household_id()` do JWT. Não-fatal se INSERT falhar: catch + Pino warn + Sentry capture (flow undo NÃO deve abortar por audit fail). Se @dev preferir defensivamente `getServiceDb()` (consistência com lookup ops + UPDATE agent_runs nas linhas 165-183 da undo/route.ts), isso é aceitável (CONCERNS deferred @architect) — mas a justificação "NFR9 REVOKE INSERT em authenticated" da v1.0 era incorrecta vs migration real.
  - Trace: NFR9 (audit imutável aplica-se a UPDATE/DELETE), Architecture §12.4, 0001_rls_policies.sql:569-584 (RLS audit_log real).

- **AC6:** Endpoint undo continua a retornar a mesma response shape Story 2.6 AC7: `{ reverted: true, run_id, ops_count }` em 200. NÃO alterar contract (zero breaking change para clients existentes — incluindo os 9 tests da Story 2.6).

### Frontend — UI Undo button funcional

- **AC7:** Refactor `apps/web/src/app/(app)/jarvis/_components/result-message.tsx`:
  - Adicionar props `undoUrl?: string` + `undoExpiresAt?: string` (opcionais para retrocompatibilidade com tests Story 2.7 que não passam estes campos).
  - Substituir botão `disabled` + tooltip "Em breve" por button funcional:
    - **Activo:** quando `undoUrl && undoExpiresAt && now() < undoExpiresAt && status === 'idle'`.
    - **Disabled com texto "Expirou":** quando `now() >= undoExpiresAt`.
    - **Disabled com loading spinner "A anular…":** quando `status === 'loading'`.
    - **Disabled com texto "Anulado ✓":** quando `status === 'success'` (terminal — botão fica permanentemente disabled).
    - **Disabled com texto "Erro":** quando `status === 'error'` (terminal — re-tentar requer novo prompt).
  - **Countdown text:** badge inline tipo `Anular (28s)` mostrando segundos restantes (re-render via `setInterval(1000)` cleanup on unmount); zero re-render quando `status !== 'idle'`.
  - Trace: D38 (button states), Story 2.7 AC7 (`undo_url`/`undo_expires_at` agora consumidos).

- **AC8:** Click handler `handleUndo` em ResultMessage:
  - `await fetch(undoUrl, { method: 'POST' })` (cookies enviados automaticamente per same-origin).
  - **200:** parse `{ reverted: true, run_id, ops_count }`, `setStatus('success')`, mostrar banner inline verde "Operação anulada com sucesso ({ops_count} registo(s) removido(s))".
  - **409 `UNDO_EXPIRED`:** `setStatus('error')`, banner amarelo "Já não é possível anular — a janela de 30 segundos passou".
  - **409 `UNDO_ALREADY_REVERTED`:** `setStatus('error')`, banner amarelo "Esta operação já foi anulada".
  - **409 `UNDO_INVALID_STATE`:** `setStatus('error')`, banner vermelho "Operação não pode ser anulada (estado: {status})".
  - **401:** `router.push('/entrar')` (consistente com Story 2.7 AC9 pattern em `jarvis-chat.tsx:99-103`).
  - **5xx ou network error:** `setStatus('error')`, banner vermelho "Erro temporário ao anular. Por favor recarrega a página e tenta de novo".
  - Tom PT-PT estrito em todas as mensagens — nunca PT-BR. Trace: Story 2.7 AC9 error patterns.

- **AC9:** Refactor `apps/web/src/app/(app)/jarvis/_components/jarvis-chat.tsx` **+ `preview-card.tsx`** (correção PO_FIX_INLINE):
  - **Interface `PromptResponseExecuted`** (linhas 39-44 actuais) adiciona campos `undo_url: string` + `undo_expires_at: string` para typing strict (estes campos já retornados pelo endpoint per route.ts:425-426 — gap apenas de typing).
  - **handleSubmit (linha 85-159 actual — bloco completo do callback):** quando `data.mode === 'executed'`, passar `undoUrl: data.undo_url` + `undoExpiresAt: data.undo_expires_at` à ResultMessage via novo campo em `AgentResultMessage` interface.
  - **[PO_FIX_INLINE v1.1 — handleConfirmResult NÃO faz fetch; PreviewCard.handleConfirm faz o fetch ao confirm endpoint e descarta `undo_url`/`undo_expires_at`]** A v1.0 alegou erradamente que `handleConfirmResult` (jarvis-chat.tsx:161-180) faz parse da response do confirm endpoint. Verificação anti-hallucination contra o código real: `handleConfirmResult` recebe APENAS `results: unknown` do callback do PreviewCard (`onConfirm(data.results)` em `preview-card.tsx:101`). O fetch ao `/api/agent/prompt/{runId}/confirm` é feito DENTRO de `PreviewCard.handleConfirm` (preview-card.tsx:91-101) e a response (que JÁ inclui `undo_url`+`undo_expires_at` per confirm/route.ts:295-303) tem TODOS os campos extra descartados — só `data.results` é propagado a `onConfirm`. **Correcção obrigatória:** Story 2.8 deve OU (a) alterar contract do `PreviewCardProps.onConfirm` para `(payload: { results: unknown; undoUrl?: string; undoExpiresAt?: string }) => void` + propagar dentro de PreviewCard.handleConfirm a response completa, OU (b) refazer fetch interno em `handleConfirmResult` (penaliza UX com round-trip duplicado). Recomendação @po: opção (a) é o caminho limpo — PreviewCard já tem a response completa, basta alargar o callback shape. Graceful degradation se confirm response não tiver os campos (regression-safe): ResultMessage render sem Undo button per D40.
  - **Render map (linha 201-209 actual):** passar `undoUrl` + `undoExpiresAt` da `AgentResultMessage` para `<ResultMessage />`.
  - Trace: Story 2.6 route.ts:420-427 (executed branch principal) + confirm/route.ts:295-303 (executed branch via confirm) + preview-card.tsx:91-101 (refactor obrigatório do callback shape).

### Audit log + Inngest — Testing

- **AC10:** Tests Vitest backend Inngest function em `apps/web/src/lib/inngest/functions/__tests__/cleanup-expired-reverse-ops.test.ts`:
  - **(i)** Handler executa DELETE query correcta (mock `getServiceDb().execute` + assert query shape contém `delete from agent_reverse_ops where expires_at < now() - interval '1 hour'`).
  - **(ii)** Handler é idempotente (chama 2x consecutivamente, segundo retorna 0 rows deleted).
  - **(iii)** Handler regista Pino log + `rows_deleted` count.
  - **(iv)** Handler propaga errors do DB ao Inngest engine (não swallows — Inngest retry policy aplica per ADR-005 max 4 attempts).
  - Estimate ≥4 testes.

- **AC11:** Tests Vitest backend audit log em `apps/web/src/app/api/agent/prompt/__tests__/undo.test.ts` (existente Story 2.6, expandido):
  - **(i)** Novo test: 200 sucesso ALSO inserts row em `audit_log` com `action='agent_run_reverted'` + `entity_id=runId` + `before_state.ops_count=N` + `after_state.status='reverted'` (mock `serviceDb.execute` + assert INSERT chamado).
  - **(ii)** Novo test: audit_log INSERT failure NÃO aborta undo flow (mock INSERT throw + verify response continua 200 + Pino warn registado).
  - Estimate ≥2 testes novos = 11 totais na undo.test.ts.

- **AC12:** Tests Vitest UI Undo button em `apps/web/src/app/(app)/jarvis/_components/__tests__/result-message.test.tsx` (existente Story 2.7, expandido):
  - **(i)** Render com `undoUrl`+`undoExpiresAt` futuro: botão activo, countdown mostra segundos restantes (advance timer com `vi.useFakeTimers()`).
  - **(ii)** Render com `undoExpiresAt` passado (já expirou): botão disabled + texto "Expirou".
  - **(iii)** **[PO_FIX_INLINE v1.1 — D40 retrocompatibilidade quebra test Story 2.7 existente, requer substituição inline]** Render SEM `undoUrl`/`undoExpiresAt`: botão fica oculto (D40 escolha). **Cross-confirm anti-hallucination contra `result-message.test.tsx:45-50`:** o teste Story 2.7 actual `'placeholder undo button está disabled com tooltip "Em breve"'` faz asserção POSITIVA que o botão Anular existe (`screen.getByRole('button', { name: /anular/i })` + `toBeDisabled()` + `toHaveAttribute('title', 'Em breve')`). Esta asserção QUEBRA quando D40 implementa "sem button se undoUrl undefined". Story 2.8 v1.1 corrige a v1.0 ambiguidade ("preservar 5 tests Story 2.7"): este teste específico DEVE ser **substituído** (não preservado) pelo novo teste (iii) que asserta `screen.queryByRole('button', { name: /anular/i })` retorna `null`. Os outros 4 testes Story 2.7 (linhas 13-43, 52-57, 59-62) NÃO interagem com o placeholder e DEVEM ser preservados. Net test delta no ficheiro: −1 (placeholder antigo) +9 (novos AC12) = +8 testes; total ficheiro 5 + 8 = 13 testes (não 14 como AC12 alega — ajustar count em sub-pontos abaixo).
  - **(iv)** Click → fetch chamado com URL correcta + status transitions idle→loading→success + banner verde rendered.
  - **(v)** Click → 409 UNDO_EXPIRED response: status→error + banner amarelo rendered.
  - **(vi)** Click → 409 UNDO_ALREADY_REVERTED: banner amarelo "Esta operação já foi anulada".
  - **(vii)** Click → 401: `router.push('/entrar')` chamado (mock `useRouter`).
  - **(viii)** Click → 5xx: status→error + banner vermelho.
  - **(ix)** Countdown: passa 30s sem click → setInterval cleanup correcto (no leak) + button transita para "Expirou".
  - Estimate ≥9 testes novos = **13 totais** (5 Story 2.7 − 1 placeholder substituído + 9 novos AC12; PO_FIX_INLINE v1.1 corrige count 14→13). Se @dev escolher preservar o placeholder test e adicionar teste paralelo (iii) para o novo behaviour, total = 14.

- **AC13:** Tests Vitest UI jarvis-chat em `apps/web/src/app/(app)/jarvis/_components/__tests__/jarvis-chat.test.tsx` (existente Story 2.7, expandido):
  - **(i)** Submit prompt → response com `mode='executed'` + `undo_url`/`undo_expires_at` → ResultMessage rendered com props correctas (mock fetch + assert ResultMessage recebe `undoUrl` non-empty).
  - **(ii)** Confirm preview → response do confirm tem `undo_url`/`undo_expires_at` → ResultMessage rendered com Undo activo.
  - Estimate ≥2 testes novos = 11 totais.

- **AC14:** Total ≥17 testes novos somando backend (Inngest 4 + audit 2) + frontend (ResultMessage 9 + jarvis-chat 2). Quality gates 5/5 GREEN: typecheck 9/9, lint 9/9 0 warnings, test (≥17 novos + 543 preservados = ≥560), check:rls 28/120 NFR5 PRESERVADA (sem novas tabelas), build Next.js (≥14 routes — adiciona `/api/inngest` GET/POST/PUT handler).

---

## [AUTO-DECISIONS] @sm

> Decisões arquitecturais documentadas pelo @sm durante drafting. **Convenção:** numeração contínua desde Story 2.7 D37. @architect ratifica no gate; @po cross-confirma anti-hallucination no validate.

### [AUTO-DECISION D38] Inngest cleanup cron diário 03:00 UTC alinhado com `/api/cron/daily`

**Decisão:** Inngest function `cleanupExpiredReverseOps` corre via cron Inngest nativo `0 3 * * *` (diário 03:00 UTC), NÃO via webhook chamado por Vercel Cron `/api/cron/daily`.

**Justificação:**

1. Arquitectura ADR-005 (§14.5) decide: "Vercel Cron mantém-se para o tick diário simples que dispara todos os jobs Inngest (`/api/cron/daily`)". Implementação MVP segue convenção mais simples — cron Inngest directo per Inngest docs Next.js quickstart.
2. Vercel Cron alternativa requer endpoint adicional `/api/cron/daily` (já mencionado em CLAUDE.md `apps/web/vercel.json`) que despacha `inngest.send({ name: 'jobs/daily-tick' })` — adiciona indireccão sem ganho funcional para MVP de 1 job.
3. Quando Story 2.9 (cost router quota reset mensal) e Story GDPR purge (mensal) entrarem, fan-out via `/api/cron/daily` faz sentido. Story 2.8 estabelece o pattern de cron Inngest nativo; refactor posterior é trivial.
4. **Risco:** Inngest cron pricing — free tier permite 30 functions, MVP usa 1. Sem risco financeiro.

**Trace:** Architecture §11.3, ADR-005 §14.5, CLAUDE.md menção cron 03:00 UTC.

### [AUTO-DECISION D39] Inngest client/functions/serve em `apps/web/src/lib/inngest/` (não package separado)

**Decoração:** Inngest setup inline em `apps/web/src/lib/inngest/` (client.ts + functions/*.ts) em vez de criar `packages/jobs/` package separado.

**Justificação:**

1. Convenção monorepo actual: `apps/web/` consume packages internos via `@meu-jarvis/*`; mas Inngest functions tendem a ser específicas de app (não lógica partilhada).
2. KISS para MVP — 1 função actualmente; package overhead (package.json, tsconfig, build) não justificado.
3. Stories futuras (GDPR purge, recurrence generation, Stripe webhook retry) podem juntar-se a `apps/web/src/lib/inngest/functions/` sem refactor.
4. Eventual extracção para `packages/jobs/` quando ≥5 functions OU partilha cross-app (improvável MVP).
5. Pattern análogo a `apps/web/src/lib/agent/*` (Story 2.6 — db-shim, redaction, tracing inline em app sem package).

**Trace:** Codebase pattern actual `apps/web/src/lib/agent/*`, Story 2.6 D27 cross-package imports.

### [AUTO-DECISION D40] UI Undo button retrocompatibilidade — render sem button se `undoUrl` undefined

**Decisão:** ResultMessage SEM props `undoUrl`/`undoExpiresAt` (retrocompatibilidade pre-2.8 — chamadas Story 2.7 ou tests legacy) renderiza o card de resultado **sem** botão Undo (zero render, não placeholder disabled). Tests Story 2.7 existentes (`result-message.test.tsx` 5 testes) DEVEM continuar a passar sem alteração.

**Justificação:**

1. Tests Story 2.7 não passam `undoUrl` — se botão disabled placeholder persistir, asserts negativos (ex: "botão Anular não está visível") quebram.
2. Alternativa: manter placeholder mas com texto neutro tipo "Sem undo disponível" — mais confuso para utilizador real.
3. Production caller principal (`jarvis-chat.tsx`) SEMPRE passa `undoUrl`/`undoExpiresAt` da response do endpoint executed branch. Edge case "undo_url ausente" deve ser visível em telemetria, não fallback silencioso.
4. Tests novos Story 2.8 (AC12 testes (iii)) explicitamente cobrem este caso retrocompat.

**Trace:** Story 2.7 result-message.test.tsx (5 testes preservados), AC7 button states.

### [AUTO-DECISION D41] `agent_run_reverted` audit_action — enum value JÁ EXISTE (zero migration extension)

**Decisão:** Story 2.8 USA o enum value `agent_run_reverted` existente em `packages/db/src/schema/audit.ts:54`. Zero migration de enum extension necessária. Confirma definitivamente que NIT-001-NB do gate 2.7 sobre "enum audit_action não tem `user_prefs.updated`" é **separado** de `agent_run_reverted` (que sempre existiu) — NIT-001-NB continua OPEN como follow-up dedicado.

**Justificação:**

1. Verificação directa do código (audit.ts:53-54) confirma `'agent_run_executed'` + `'agent_run_reverted'` ambos enum values.
2. Gate 2.7 D36 mencionava `user_prefs.updated` action faltando (necessitaria enum extension). Story 2.8 NÃO toca user_prefs audit — só agent_runs reverted, que JÁ tem enum value.
3. NIT-001-NB continua aberto para futuro (Story 2.x housekeeping ou piggyback em outra story que precise user_prefs audit).

**Trace:** `packages/db/src/schema/audit.ts:53-54`, gate 2.7 NIT-001-NB clarification.

### [AUTO-DECISION D42] CodeRabbit pre-commit/push skipped (consistente 2.2-2.7)

**Decisão:** Consistente com Stories 2.2 (D14) / 2.3 / 2.4 (D12) / 2.5 (D14) / 2.6 (D22+D26) / 2.7 (D33). Architect gate substitui review automatizado.

**Justificação:**

1. 2.8 é primariamente Inngest setup (provider novo) + UI refactor + audit_log INSERT — padrões já estabelecidos (handler async, fetch UI, DB INSERT).
2. CodeRabbit re-baseline schedulada para Story 2.10 (benchmark E2E + credentials reais — onde brilha em auth/secrets) — convenção mantida.
3. Architect gate cobre Inngest setup + cross-domain (backend + frontend) com profundidade superior.

### [AUTO-DECISION D43] Tests UI Inngest com mocks Vitest (não Inngest Dev Server)

**Decisão:** Tests da função Inngest `cleanupExpiredReverseOps` usam Vitest mocks (`vi.mock('@/lib/inngest/client')`) — não Inngest Dev Server. Stories futuras com flows multi-step (Stripe webhook) podem revisitar e adicionar integration tests.

**Justificação:**

1. Vitest mocks deterministicos + zero CI infra extra (Dev Server requer `inngest-cli` process + signing key).
2. Função actual é simples: 1 DELETE query + 1 log. Integration test value-add é baixo.
3. Architecture testing pyramid (§10) prefere unit tests para hot path.
4. Story futura (GDPR purge multi-step com Stripe events) é candidato natural para Inngest integration tests com Dev Server.

**Trace:** Architecture §10 testing pyramid, Story 2.5 D14 (Vitest precedente).

---

## Tasks

### Backend — Inngest cleanup

- [ ] **T1.** Adicionar dependência Inngest:
  - [ ] `pnpm --filter @meu-jarvis/web add inngest@^3` (latest stable)
  - [ ] Confirmar `apps/web/package.json` adiciona `inngest` com versão pinned
  - [ ] `pnpm install` no root (workspace lockfile actualizado)

- [ ] **T2.** Criar `apps/web/src/lib/inngest/client.ts`:
  - [ ] Export `inngest = new Inngest({ id: 'expressia-web', name: 'Expressia Web' })`
  - [ ] Sem auth/keys hardcoded — Inngest SDK lê automaticamente `INNGEST_EVENT_KEY` + `INNGEST_SIGNING_KEY` do env
  - [ ] Comentário PT-PT explicando isolation entre dev/prod via Inngest workspaces

- [ ] **T3.** Criar `apps/web/src/lib/inngest/functions/cleanup-expired-reverse-ops.ts`:
  - [ ] `inngest.createFunction({ id: 'cleanup-expired-reverse-ops', name: '...' }, { cron: '0 3 * * *' }, handler)`
  - [ ] Handler: `step.run('delete-expired', async () => { ... })` com `getServiceDb().execute(sql\`delete from agent_reverse_ops where expires_at < now() - interval '1 hour'\`)`
  - [ ] Capture `rowsAffected` (postgres-js retorna em `.count` OR result array length); registar via Pino `log.info({ rows_deleted: N }, '...')`
  - [ ] Re-throw errors para Inngest retry engine pegar (ADR-005 max 4 attempts)

- [ ] **T4.** Criar `apps/web/src/app/api/inngest/route.ts`:
  - [ ] `import { serve } from 'inngest/next'; import { inngest } from '@/lib/inngest/client'; import { cleanupExpiredReverseOps } from '@/lib/inngest/functions/cleanup-expired-reverse-ops'`
  - [ ] `export const { GET, POST, PUT } = serve({ client: inngest, functions: [cleanupExpiredReverseOps] })`
  - [ ] Comentário linhas-7 explicando que este endpoint é público — Inngest signing key valida via header `X-Inngest-Signature`

- [ ] **T5.** Criar `docs/runbooks/inngest-setup.md`:
  - [ ] Step 1: criar Inngest workspace EU Frankfurt (https://app.inngest.com com region selector)
  - [ ] Step 2: gerar Event Key (Settings → Event Keys → Production) e Signing Key
  - [ ] Step 3: registar endpoint `https://expressia.pt/api/inngest` (produção) + `https://<vercel-preview>.vercel.app/api/inngest` (preview deploys)
  - [ ] Step 4: popular Vercel secrets `INNGEST_EVENT_KEY` + `INNGEST_SIGNING_KEY` (Production + Preview + Development scopes)
  - [ ] Step 5: para dev local: `npx inngest-cli dev -u http://localhost:3000/api/inngest` (auto-discover endpoint)
  - [ ] Step 6: smoke test cron via "Invoke" button no Inngest dashboard
  - [ ] Status: instruções para @devops Eurico-side antes do push @devops 2.8.

### Backend — Audit log INSERT no Undo

- [ ] **T6.** Refactor `apps/web/src/app/api/agent/prompt/[runId]/undo/route.ts`:
  - [ ] Import `auditLog` de `@meu-jarvis/db/schema` (ou via re-export central)
  - [ ] Após o `update agent_runs set status='reverted', reverted_at=now()` bem-sucedido, executar `serviceDb.insert(auditLog).values({...})` com campos de AC5
  - [ ] try/catch isolado — Pino `log.warn({ err }, 'audit_log insert falhou (não-fatal)')` + `captureException(err, sentrySafeContext({ route: ROUTE_TEMPLATE, householdId, runId }))` em fail; flow undo continua
  - [ ] `traceId: span.spanContext().traceId` (verificar span API expõe; pode requerer `import { trace } from '@opentelemetry/api'; trace.getActiveSpan()?.spanContext().traceId`)

### Frontend — UI Undo button

- [ ] **T7.** Refactor `apps/web/src/app/(app)/jarvis/_components/result-message.tsx`:
  - [ ] Adicionar props opcionais `undoUrl?: string` + `undoExpiresAt?: string` à interface `ResultMessageProps`
  - [ ] Adicionar state interno: `const [status, setStatus] = useState<'idle'|'loading'|'success'|'error'>('idle')` + `const [errorMessage, setErrorMessage] = useState<string|null>(null)` + `const [remainingSec, setRemainingSec] = useState<number>(0)`
  - [ ] useEffect setInterval(1000) para countdown — cleanup on unmount; corre apenas se `status === 'idle' && undoExpiresAt`
  - [ ] Render conditional: se `!undoUrl || !undoExpiresAt` → sem button (D40 retrocompat); else render button com states de AC7
  - [ ] handleUndo callback per AC8 — fetch POST → parse status → setStatus + setErrorMessage
  - [ ] Banner inline render abaixo do card de resultado (verde success / amarelo expired-or-already / vermelho error)
  - [ ] PT-PT estrito em todas as mensagens

- [ ] **T8.** Refactor `apps/web/src/app/(app)/jarvis/_components/jarvis-chat.tsx`:
  - [ ] Adicionar campos `undo_url: string` + `undo_expires_at: string` à interface `PromptResponseExecuted`
  - [ ] Adicionar campos `undoUrl?: string` + `undoExpiresAt?: string` à interface `AgentResultMessage`
  - [ ] handleSubmit (linha 139-146 actual): passar `undoUrl: data.undo_url` + `undoExpiresAt: data.undo_expires_at` ao `appendMessage`
  - [ ] handleConfirmResult (linha 161-180 actual): aceitar parâmetro extra do PreviewCard contendo undo fields OR refazer fetch para extrair (decisão @dev impl — verificar PreviewCard interface)
  - [ ] Passar `undoUrl` + `undoExpiresAt` da `AgentResultMessage` para `<ResultMessage />` no map render (linha 201-208 actual)

### Testing

- [ ] **T9.** Tests Inngest function — criar `apps/web/src/lib/inngest/functions/__tests__/cleanup-expired-reverse-ops.test.ts` (4 testes per AC10)
- [ ] **T10.** Tests audit_log INSERT — expandir `apps/web/src/app/api/agent/prompt/__tests__/undo.test.ts` (+2 testes per AC11)
- [ ] **T11.** Tests UI Undo button — expandir `apps/web/src/app/(app)/jarvis/_components/__tests__/result-message.test.tsx` (+9 testes per AC12)
- [ ] **T12.** Tests jarvis-chat integration — expandir `apps/web/src/app/(app)/jarvis/_components/__tests__/jarvis-chat.test.tsx` (+2 testes per AC13)

### Quality gates final

- [ ] **T13.** Quality gates 5/5 GREEN:
  - [ ] `pnpm lint` exit 0 (9/9 packages, 0 warnings)
  - [ ] `pnpm typecheck` exit 0 (9/9 packages)
  - [ ] `pnpm test` exit 0 (≥17 testes novos + 543 preservados = ≥560)
  - [ ] `pnpm check:rls` exit 0 (28 tabelas / 120 policies — sem novas tabelas, NFR5 PRESERVADA)
  - [ ] `pnpm build` exit 0 — Next.js routes inclui novo `/api/inngest` (GET + POST + PUT) + Sentry sourcemap 3 runtimes preservado

---

## Dev Notes

### DN1 — Inngest dev workflow

Sem keys: Inngest functions registadas mas `inngest.send()` falham silenciosamente em local OU geram erro `INNGEST_EVENT_KEY missing`. Para dev sem keys, alternativa é `npx inngest-cli dev -u http://localhost:3000/api/inngest` que cria local Inngest engine + dashboard em `localhost:8288` sem cloud. **Tests Vitest devem mockar completamente — nunca chamar Inngest engine real em CI.**

### DN2 — Mais um setIInterval em produção vs requestAnimationFrame

Countdown 30s pode ter precision drift ±100ms com setInterval(1000) — aceitável para UX (user vê "29s, 28s, ...; 1s; Expirou"). Não usar requestAnimationFrame para economia bateria/CPU (overkill para countdown coarse).

### DN3 — `app/(app)/` route group middleware

Story 2.7 PO_FIX_INLINE 4 actualizou `apps/web/src/middleware.ts` para `APP_PATH_PREFIXES = ['/visao', '/jarvis', '/conta']`. **NÃO** adicionar `/api/inngest` a este array — endpoint Inngest é público (auth via signing key, não via Supabase session).

### DN4 — `agent_run_reverted` audit já no enum

Cross-confirmação anti-hallucination: `packages/db/src/schema/audit.ts:54` linha exacta `'agent_run_reverted'` JÁ no enum `auditActionEnum`. Story 2.8 NÃO precisa de migration de enum extension para esta acção. Distinguir do NIT-001-NB do gate 2.7 que se referia a `user_prefs.updated` (action diferente, fora do scope 2.8).

### DN5 — `getServiceDb()` vs `getDb()` no audit_log INSERT

Audit_log tem RLS: SELECT permitido para member do household; INSERT REVOKED em `authenticated` (NFR9 — apenas service_role insere). Story 2.5 executor já usa `getServiceDb()` para audit; Story 2.8 segue mesmo pattern no undo endpoint. **NÃO** tentar INSERT via `getDb()` — falha imediato com `permission denied`.

### DN6 — Tests sem Inngest engine real

Vitest mocks: `vi.mock('@/lib/inngest/client', () => ({ inngest: { createFunction: vi.fn() } }))` permite criar mock function que retorna o handler como callable. Test invoca handler directamente: `await cleanupExpiredReverseOps.handler({ event: {}, step: mockStep })` com `mockStep.run = vi.fn((id, cb) => cb())` para satisfazer Inngest contrato.

### DN7 — Reverse op execution: BEGIN tx review (deferred)

EPIC plan original Story 2.8 menciona "BEGIN tx → execute reverse_ops em ordem reversa → COMMIT". Endpoint Story 2.6 actual itera sequencialmente com service_role mas SEM BEGIN/COMMIT wrapper Postgres + sem ordem reversa explícita. **Decisão Story 2.8 v1.0:** NÃO refactor scope (gate 2.6 APPROVED 9.4/10 cobriu este flow com 9 tests passando). Item para @architect ratificar no gate 2.8 (re-validar): se concern emergir, criar follow-up story dedicado (não inflacionar 2.8). Trace: EPIC-2 linha 243.

### DN8 — `audit_log` schema constraints

Coluna `entityTable` é `text` (não enum) — aceitar `'agent_runs'` literal. Coluna `entityId` é `uuid` — passar `runId` já como string UUID. Coluna `before_state`/`after_state` são `jsonb` — passar objects JS, Drizzle serializa.

### DN9 — Mensagens PT-PT

Banners Undo UI usam vocabulário PT-PT estrito:
- Success: "Operação anulada com sucesso ({N} registo(s) removido(s))"
- Expired: "Já não é possível anular — a janela de 30 segundos passou"
- Already reverted: "Esta operação já foi anulada"
- Invalid state: "Operação não pode ser anulada (estado: {status})"
- Error (5xx/network): "Erro temporário ao anular. Por favor recarrega a página e tenta de novo"

NUNCA: "deletar", "cancelar a ação", "voltar atrás" (PT-BR ou ambíguo).

### DN10 — File List antecipada (Dev Agent Record actualizará)

**Novos (6 ficheiros):**
1. `apps/web/src/lib/inngest/client.ts`
2. `apps/web/src/lib/inngest/functions/cleanup-expired-reverse-ops.ts`
3. `apps/web/src/app/api/inngest/route.ts`
4. `apps/web/src/lib/inngest/functions/__tests__/cleanup-expired-reverse-ops.test.ts`
5. `docs/runbooks/inngest-setup.md`
6. (Eventual) `apps/web/src/lib/inngest/functions/index.ts` se @dev preferir barrel export

**Modificados (8 ficheiros — +1 vs v1.0 via PO_FIX_INLINE 2):**
1. `apps/web/package.json` (+`inngest` dep)
2. `apps/web/src/app/api/agent/prompt/[runId]/undo/route.ts` (audit_log INSERT)
3. `apps/web/src/app/(app)/jarvis/_components/result-message.tsx` (UI Undo button funcional)
4. `apps/web/src/app/(app)/jarvis/_components/jarvis-chat.tsx` (props passthrough)
5. **`apps/web/src/app/(app)/jarvis/_components/preview-card.tsx` (PO_FIX_INLINE 2 — alargar `onConfirm` callback shape para incluir `undoUrl`+`undoExpiresAt` da response do confirm endpoint)**
6. `apps/web/src/app/api/agent/prompt/__tests__/undo.test.ts` (+2 audit tests)
7. `apps/web/src/app/(app)/jarvis/_components/__tests__/result-message.test.tsx` (+9 UI tests − 1 placeholder substituído = net +8)
8. `apps/web/src/app/(app)/jarvis/_components/__tests__/jarvis-chat.test.tsx` (+2 integration tests)

Total ≈ 14 ficheiros tocados (6 novos + 8 modificados, com tests counts).

---

## Definition of Done

- [ ] Todos os 14 ACs verificados PASS (8 backend Inngest+audit + 3 frontend UI + 3 testing) + AC14 quality gates
- [ ] 5 quality gates GREEN: typecheck 9/9, lint 9/9 0 warnings, test ≥560 totais, check:rls 28/120 NFR5 PRESERVADA, build Next.js ≥14 routes (`/api/inngest` GET+POST+PUT adicionado)
- [ ] Inngest workspace EU Frankfurt provisionado por Eurico (Step 1 do runbook) + secrets em Vercel
- [ ] @architect gate APPROVED/CONCERNS no gate report
- [ ] @devops push completo `origin/main` com handoff close
- [ ] Vercel auto-deploy `fra1` verde + Inngest dashboard mostra função registada + first cron tick scheduled
- [ ] Story file movida `active/` → `completed/` via `git mv` (history preservada)
- [ ] `_INDEX.md` row Story 2.8 actualizada Backlog → Draft → Ready → InReview → Done + cabeçalho Epic 2 7/11 → 8/11
- [ ] Handoffs chain (sm→po→dev→architect→devops) consumed/archived
- [ ] Working tree clean + 0 commits ahead origin/main
- [ ] Test smoke manual: submeter prompt em `/jarvis` que executa (ex: classifier mock retorna alta confidence) → ResultMessage mostra botão "Anular (30s)" activo → click → banner verde aparece + status muda para "Anulado ✓" disabled → DB row em `audit_log` confirmada via Drizzle Studio

---

## PO Validation Block (v1.1 — 2026-05-11)

**Validador:** Pax (@po)
**Veredicto:** **GO 9.0/10 HIGH confidence**
**Modo:** `*validate-story-draft 2.8` autónomo (YOLO).
**Score breakdown:** 10/10 critérios PASS do 10-point checklist; ajuste final 9.0/10 reflecte 3 PO_FIX_INLINE necessários (não erros bloqueantes — hallucinations vs codebase real corrigidas inline). Match exacto com precedentes 2.6 GO 9.3/10 (5 fixes) e 2.7 GO 9.0/10 (4 fixes).

### 10-point checklist (autoridade @po per `~/.claude/rules/story-lifecycle.md`)

| # | Critério | Verdict | Notas |
|---|----------|---------|-------|
| 1 | Título claro + descritivo | PASS | "Undo Mechanism (FR6) + Inngest Cleanup + UI Undo Button" — 3 áreas explícitas, escopo legível ao primeiro olhar. |
| 2 | Descrição user story format | PASS | "Como utilizador autenticado…Quero…Para que…" linha 37-39 — trace FR6 + NFR12 + R5 mitigação. |
| 3 | ACs numerados + testáveis | PASS | 14 ACs (AC1-AC4 Inngest, AC5-AC6 audit, AC7-AC9 UI, AC10-AC13 testing, AC14 quality gates) — todos testáveis (file paths exactos + shape contracts + ≥17 testes counted). |
| 4 | Scope sized | PASS | M confirmado (não scope-shift L como 2.7 D28) — Inngest infra ~2h + audit log ~1h + UI Undo ~3h + tests ~3h ≈ 9h wall-clock (M = 6-12h range Story 1.x). Precedente 2.7 L = ~6h actual; 2.8 M maior porque inclui provider novo (Inngest). |
| 5 | Dependências mapeadas | PASS | Internas: 1.5 ✓, 1.7 ✓, 2.1 ✓, 2.5 ✓, 2.6 ✓, 2.7 ✓ (todas Done). Externas: EB4 DEFERRED não bloqueante (Eurico provisiona pre-push). EB1/EB2/EB3 não afectam (mockable-only padrão 2.2-2.7). |
| 6 | Estimate razoável | PASS | M = ~9h coerente: Inngest setup é greenfield (provider novo) +UI Undo refactor minimal +audit INSERT trivial. Sem scope-shift L justificado (D28 3-conditions: UI minimal sem design system extraction). |
| 7 | Business value claro | PASS | FR6 (undo 30s) + NFR12 (user trust — "executar sem confirmação intermédia") + R5 mitigação ("undo race condition"). Estratégico para confiança no agente AI. |
| 8 | Riscos documentados | PASS | 10 Dev Notes DN1-DN10 cobrem: Inngest dev workflow, setInterval precision, middleware route group, enum existence cross-confirm, RLS service_role vs authenticated, test mocking, BEGIN tx deferred (DN7), audit schema constraints, PT-PT vocabulário, File List antecipada. DN7 explicitamente deferred para @architect gate. |
| 9 | DoD inferível | PASS | 14 ACs + 5/5 quality gates + Inngest workspace provisioning + Vercel deploy verde + smoke test manual. 11 items no DoD. |
| 10 | Alignment PRD/Architecture | PASS | Trace explícito: PRD FR6 (undo 30s) + NFR12 (user trust) + NFR9 (audit imutável) + Architecture §4.5 (undo flow) + §11.3 (Inngest provider) + §14.5 (ADR-005 Inngest EU) + EPIC-2-EXECUTION.yaml linhas 234-247 (scope original). Constitution Article IV: zero invenção (todas as features rastreáveis). |

### Cross-confirm anti-hallucination (10 fontes reais)

| Item alegado pela v1.0 | Fonte real | Verdict |
|-----------------------|------------|---------|
| D41 enum `'agent_run_reverted'` existe em audit.ts:54 | `packages/db/src/schema/audit.ts:54` linha exacta `'agent_run_reverted',` | ✓ PASS — D41 RATIFICADA |
| `agentReverseOps` colunas `expires_at`+`executed_at` | `packages/db/src/schema/agent.ts:211-212` | ✓ PASS |
| `agentRunStatusEnum 'reverted'` | `packages/db/src/schema/agent.ts:43` | ✓ PASS |
| Main endpoint retorna `undo_url`+`undo_expires_at` | `apps/web/src/app/api/agent/prompt/route.ts:419-427` | ✓ PASS — gap apenas typing UI |
| Confirm endpoint retorna `undo_url`+`undo_expires_at` | `apps/web/src/app/api/agent/prompt/[runId]/confirm/route.ts:295-303` | ✓ PASS — story v1.0 alegou correctamente |
| Endpoint undo existente 280 linhas Story 2.6 + 9 tests | `apps/web/src/app/api/agent/prompt/[runId]/undo/route.ts` 280 linhas + `undo.test.ts` 9 it() blocks | ✓ PASS |
| Placeholder Story 2.7 `result-message.tsx:53-61` + comment "Story 2.8" linha 32 | linhas 53-61 button disabled tooltip "Em breve" + comment linha 32 | ✓ PASS |
| `PromptResponseExecuted` gap typing em jarvis-chat.tsx:39-44 | linhas 39-44 confirmam ausência `undo_url`/`undo_expires_at` | ✓ PASS |
| `.env.example` INNGEST_EVENT_KEY + INNGEST_SIGNING_KEY vazios | `.env.example:55-56` | ✓ PASS |
| `auditLog` schema (`entityTable`, `entityId`, `beforeState`, `afterState`, `traceId`) | `packages/db/src/schema/audit.ts:86-96` | ✓ PASS |

### Cross-confirm anti-hallucination — 3 HALLUCINATIONS DETECTADAS → PO_FIX_INLINE aplicados

| # | Hallucination v1.0 | Codebase real | Fix aplicado |
|---|-------------------|--------------|--------------|
| **PO_FIX_INLINE 1** | AC5 DN5: "audit_log INSERT REVOKED em `authenticated` per NFR9 imutability — apenas service_role insere" | `packages/db/migrations/0001_rls_policies.sql:569-573` policy `audit_log_insert_member` PERMITE INSERT a `authenticated` com `with check (household_id is null or public.is_household_member(household_id))`. Apenas UPDATE/DELETE são revogados (:576-584). NFR9 imutabilidade aplica-se a UPDATE/DELETE (append-only) NÃO ao INSERT. | AC5 + DN5 reescritos com nota anti-hallucination + autoridade @dev decidir entre `getDb()` (correto per RLS real) ou `getServiceDb()` defensivamente. Concern CONCERNS-ACCEPTABLE para @architect ratificar no gate. |
| **PO_FIX_INLINE 2** | AC9: "handleConfirmResult (linha 161-180) parse response do confirm; passar undo fields à ResultMessage" | `jarvis-chat.tsx:161-180` `handleConfirmResult` recebe APENAS `results: unknown` do callback. O fetch ao confirm endpoint é feito DENTRO de `preview-card.tsx:91-101` `PreviewCard.handleConfirm`, e a response (que JÁ tem `undo_url`+`undo_expires_at` confirmado em confirm/route.ts:295-303) tem TODOS os campos extra DESCARTADOS — só `data.results` é propagado via `onConfirm(data.results)` linha 101. | AC9 expandido para incluir refactor obrigatório de `preview-card.tsx` (`PreviewCardProps.onConfirm` alargar para `(payload: { results, undoUrl?, undoExpiresAt? }) => void` + propagar response completa do fetch). File List actualizada para incluir `preview-card.tsx` modificado. |
| **PO_FIX_INLINE 3** | AC12 (iii) + D40: "5 testes Story 2.7 preservados sem alteração" + "render sem button se undoUrl undefined" | `result-message.test.tsx:45-50` teste 3 asserta POSITIVAMENTE que botão Anular existe com `title="Em breve"` (`screen.getByRole('button', { name: /anular/i })`). D40 implementa "sem button se undoUrl undefined" → este teste QUEBRA. | AC12 (iii) clarificado: teste 3 Story 2.7 DEVE ser substituído (não preservado) pelo novo teste (iii) que asserta `queryByRole(button, /anular/) === null`. Outros 4 testes Story 2.7 preservados (não tocam placeholder). Test count ajustado: net +8 testes no ficheiro = 13 totais (não 14 da v1.0). @dev pode optar por preservar o placeholder test e adicionar test paralelo (recovers count 14) — aceitável. |

### Review 6 [AUTO-DECISIONS] D38-D43

| # | Decisão | Verdict @po | Notas |
|---|---------|-------------|-------|
| D38 | Cron Inngest nativo `0 3 * * *` (sem `/api/cron/daily` middleware MVP) | PASS | KISS para 1 job; Architecture ADR-005 §14.5 admite Vercel Cron alternativa mas não obriga. Refactor trivial quando ≥2 jobs Inngest. |
| D39 | Inngest inline em `apps/web/src/lib/inngest/` (sem `packages/jobs/`) | PASS | Pattern consistente com `apps/web/src/lib/agent/*` (Story 2.6 D27). Extracção futura quando ≥5 functions. |
| D40 | UI Undo retrocompat — sem render se `undoUrl` undefined | PASS via PO_FIX_INLINE 3 | Resolução técnica clara — placeholder test Story 2.7 substituído por novo test (iii). |
| D41 | `agent_run_reverted` enum JÁ EXISTE — zero migration extension | PASS via cross-confirm audit.ts:54 | RATIFIED. Distinção do NIT-001-NB Story 2.7 (`user_prefs.updated`) é correcta. |
| D42 | CodeRabbit skip consistente 2.2-2.7 | PASS | Architect gate continua a substituir review automatizado per precedente 2.7 D33. Story 2.10 (credenciais reais Anthropic/OpenAI) é o re-baseline natural. |
| D43 | Vitest mocks sem Inngest Dev Server em CI | PASS | Deterministic + zero CI infra extra. Integration tests com Dev Server podem voltar em Story GDPR purge multi-step (futuro Epic). |

### Concerns para @architect no gate 2.8

1. **DN7 BEGIN tx + ordem reversa explícita do flow undo (deferido v1.0):** EPIC plan original Story 2.8 menciona "BEGIN tx → execute reverse_ops em ordem reversa → COMMIT". Endpoint Story 2.6 actual (undo/route.ts:165-183) usa `getServiceDb()` sequencialmente sem BEGIN/COMMIT explicit nem ordem reversa. Gate 2.6 APPROVED 9.4/10 cobriu este flow com 9 tests passing — sem regression observada. **@architect deve ratificar:** (a) accept current behaviour as adequate (pattern: postgres-js auto-commit é suficiente para sequência de UPDATEs idempotentes desde que cada op aplique apenas DELETE/UPDATE de row específica via PK; ordem reversa só importa se ops referenciam-se mutuamente — restore_row + delete_row simples não têm essa dependência), OU (b) exigir refactor para wrapper BEGIN tx + ordem reversa (criar follow-up story dedicado, não inflacionar 2.8).
2. **PO_FIX_INLINE 1 — RLS audit_log INSERT pode usar `getDb()` ou `getServiceDb()`:** justificação @po de que `getDb()` é correcto per migration real, mas @dev pode escolher `getServiceDb()` defensivamente. @architect ratifica escolha @dev no gate (zero impacto comportamental — ambos atingem o mesmo INSERT, diferença é apenas conceitual sobre quem é o role responsável).
3. **NIT-001-NB Story 2.7 (user_prefs.updated enum extension)** continua aberto independentemente: Story 2.8 não adiciona action `user_prefs.updated` ao enum (D41 é específica para `agent_run_reverted`). Pode ser piggyback Story 2.x futura ou follow-up dedicado.

### Status transition

- Story file: `Draft` v1.0 → **`Ready` v1.1** (autoridade @po em GO verdict per `~/.claude/rules/story-lifecycle.md`).
- File List antecipada: +1 ficheiro modificado (`preview-card.tsx`) via PO_FIX_INLINE 2 — total 14 (6 novos + 8 modificados).
- Estimate inalterado: M (~9h wall-clock).
- Sucessor: `mj-handoff-2.8-ready-for-dev-20260511` (po → dev YOLO mockable-only).

---

## Change Log

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| v1.0 | 2026-05-11 | @sm (River) — drafted by Orion (@aiox-master) per Constitution Article II (precedente Stories 2.2 + 2.7 D28) | **Story 2.8 Draft inicial.** 14 ACs (4 backend Inngest + 2 audit log + 3 frontend UI + 4 testing + 1 total quality gates). 6 [AUTO-DECISIONS] D38-D43: D38 (cron Inngest nativo 03:00 UTC sem `/api/cron/daily` middleware MVP), D39 (Inngest setup inline em `apps/web/src/lib/inngest/` sem package `packages/jobs/`), D40 (Undo button retrocompatibilidade — sem render se props undefined), D41 (`agent_run_reverted` enum já existe — zero migration), D42 (CodeRabbit skip consistente 2.2-2.7), D43 (Vitest mocks sem Inngest Dev Server em CI). Scope M (não scope-shift L como 2.7) — UI Undo é refactor minimal de placeholder existente. Cross-confirm anti-hallucination contra 6 fontes (audit.ts:53-54 enum existence, route.ts:419-427 undo_url/undo_expires_at response, undo/route.ts existing impl Story 2.6 AC7, architecture.md §11.3+§14.5 Inngest ADR, .env.example INNGEST keys, result-message.tsx:30-32 placeholder Story 2.7). EB4 DEFERRED não bloqueante (Inngest registado primeira vez aqui — Eurico provisiona workspace antes do @devops push per runbook T5). 1 concern para @architect: DN7 BEGIN tx + ordem reversa do flow undo (deferred review — Story 2.6 gate cobriu sem regressão, item para ratificação). |
| v1.1 | 2026-05-11 | @po (Pax) — `*validate-story-draft 2.8` GO 9.0/10 HIGH | **PO Validation block adicionado** com 10-point checklist, cross-confirm anti-hallucination contra 13 fontes reais do codebase, review 6 [AUTO-DECISIONS] D38-D43 (6/6 PASS), 3 concerns para @architect no gate (DN7 BEGIN tx ratification, PO_FIX_INLINE 1 RLS choice ratification, NIT-001-NB Story 2.7 follow-up). **3 [PO_FIX_INLINE] aplicados corrigindo hallucinations vs codebase real:** (1) AC5+DN5 audit_log INSERT RLS: v1.0 alegou "REVOKED em authenticated per NFR9", verificação `0001_rls_policies.sql:569-573` mostra `audit_log_insert_member` policy PERMITE INSERT a authenticated members (apenas UPDATE/DELETE são revogados :576-584) — NFR9 imutabilidade aplica-se a UPDATE/DELETE não ao INSERT; AC5+DN5 reescritos com nota + autoridade @dev decidir `getDb()` (correcto) vs `getServiceDb()` defensivamente; (2) AC9 confirm endpoint propagation: v1.0 alegou "handleConfirmResult (linha 161-180) parse response do confirm", verificação real mostra que `handleConfirmResult` jarvis-chat.tsx:161-180 só recebe `results: unknown` via callback — fetch é feito em `preview-card.tsx:91-101` `PreviewCard.handleConfirm` que DESCARTA `undo_url`+`undo_expires_at` da response (confirm endpoint JÁ retorna estes campos per confirm/route.ts:295-303 verified); AC9 expandido para incluir refactor obrigatório de `preview-card.tsx` (`PreviewCardProps.onConfirm` shape alargado de `(results: unknown) => void` para `(payload: { results, undoUrl?, undoExpiresAt? }) => void`) — File List antecipada actualizada com `preview-card.tsx` modificado (total 7→8 modificados ≈ 14 ficheiros tocados); (3) AC12 (iii) retrocompat ambiguidade: v1.0 alegou "5 testes Story 2.7 preservados sem alteração" mas teste 3 actual `result-message.test.tsx:45-50` (`'placeholder undo button está disabled com tooltip "Em breve"'`) asserta POSITIVAMENTE que o botão Anular existe — D40 "sem button se undoUrl undefined" QUEBRA esta asserção; AC12 (iii) clarificado para substituir este teste pelo novo (queryByRole retorna null) e ajustar test count (5 − 1 + 9 = 13, não 14 como v1.0 alegava; @dev pode preservar+adicionar paralelo para recover 14). Status Draft → Ready (autoridade @po em GO verdict per `~/.claude/rules/story-lifecycle.md`). Sucessor: `mj-handoff-2.8-ready-for-dev-20260511` (po → dev YOLO mockable-only). |

---

*Story drafted by Orion (@aiox-master) delegated @sm authority per Constitution Article II universal executor. Precedente: Stories 2.2 (Eurico autorização directa), 2.7 D28 (scope-shift autorização). Validated by Pax (@po) v1.1 GO 9.0/10 HIGH em 2026-05-11. Ready for `@dev *develop 2.8` modo YOLO mockable-only.*
