# Story 1.7 — Observabilidade: OTel + Sentry EU + Grafana Cloud EU

**Status:** Done
**Epic:** Epic 1 — Foundation & Multi-Tenant Core
**Owner:** @dev
**Estimate:** M
**Created:** 2026-05-04
**Updated:** 2026-05-08

---

## ✅ DESBLOQUEADO — Credenciais EU configuradas

> **Estado (2026-05-07 22:40):** Bloqueadores externos **B3** (Sentry EU) e **B4** (Grafana Cloud EU) resolvidos pela sessão devops 2026-05-07. **8 secrets** configurados em runtime — story pronta para implementação.
>
> **Sentry EU (Frankfurt):**
> - Org: `eurico-xw` · Project: `expressia-web` · Platform: Next.js
> - DSN domain: `o4510848200278016.ingest.de.sentry.io` (sufixo `.de.` = EU data residency confirmada)
> - Org Token: `expressia-ci-prod` (scopes `org:read`, `project:read`, `project:write`, `project:releases`)
>
> **Grafana Cloud EU (Ireland — eu-west-6):**
> - Stack: `expressia.grafana.net` · Plan: Free (10K series + 50GB logs + 50GB traces)
> - OTLP endpoint: `https://otlp-gateway-prod-eu-west-6.grafana.net/otlp`
> - Cloud Access Policy Token (scopes `metrics:write`, `logs:write`, `traces:write`, `profiles:write`, `stacks:read`)
>
> **Vercel Observability:** Activado em projecto `euricojsalves-4744s-projects/expressia` (Region `cdg1` Paris UE).
>
> **Secrets storage (8 total em ambos os cofres):** `SENTRY_DSN`, `SENTRY_ORG`, `SENTRY_PROJECT`, `SENTRY_AUTH_TOKEN`, `OTEL_EXPORTER_OTLP_ENDPOINT`, `OTEL_EXPORTER_OTLP_HEADERS`, `GRAFANA_API_TOKEN`, `GRAFANA_STACK_NAME` — em GitHub Actions (`gh secret list -R DaSilvaAlves/expressia`) e Vercel (`vercel env ls`, Production+Preview, Encrypted).
>
> **Custos:** 0 EUR/mês (free tier ambas plataformas).
>
> **Referência:** `docs/handoffs/mj-handoff-secrets-configured-20260507.yaml`

---

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["typecheck", "lint", "vitest"]
```

---

## User Story

**As a** developer/operator da Expressia,
**I want** telemetria OpenTelemetry configurada desde o primeiro endpoint, com Sentry para errors e Grafana Cloud para métricas/traces/logs,
**so that** a equipa pode observar latência, error rate e custo LLM desde o dia 1 — conforme NFR13 ("OTel em todos os services desde Epic 1, não afterthought").

---

## Dependências

- **DEPENDE de Story 1.1** (monorepo Next.js 15 funcional). ✅ Done.
- **DEPENDE de Story 1.6** (`/api/me` endpoint para AC5 instrumentar). ✅ Done.
- **Bloqueadores externos B3 + B4** ✅ resolvidos (ver secção "DESBLOQUEADO" acima).
- Epic 2 (Cérebro AI) vai adicionar métricas `agent.latency.p95`, `agent.cost.eur_per_household` — esta story prepara a instrumentação base.

---

## Acceptance Criteria

- [x] **AC1:** Package `packages/observability/` existe e exporta: `logger` (Pino com redaction de PII), `tracer` (OTel helpers), `captureException` (Sentry wrapper).
- [x] **AC2:** OTel via `@vercel/otel` registado em `apps/web/instrumentation.ts` (Next.js 15 instrumentation hook — estável e activo por default desde Next 15.0; `experimental.instrumentationHook` foi removido em Next 15.x e era exclusivo de Next 13/14) com OTLP exporter apontado a Grafana Cloud EU (`OTEL_EXPORTER_OTLP_ENDPOINT` + `OTEL_EXPORTER_OTLP_HEADERS`).
- [x] **AC3:** Sentry configurado em `apps/web/sentry.server.config.ts`, `apps/web/sentry.client.config.ts` e (Edge runtime) `apps/web/sentry.edge.config.ts`, com `withSentryConfig` no `next.config.ts`. DSN Sentry EU (`o4510848200278016.ingest.de.sentry.io`) + sourcemap upload no build via `SENTRY_AUTH_TOKEN` + PII redaction `beforeSend` (NFR12).
- [x] **AC4:** Vercel Speed Insights e Analytics activados em `apps/web/src/app/layout.tsx` (Vercel RUM — NFR4).
- [x] **AC5:** Endpoint `/api/me` (Story 1.6) emite span OTel com atributos: `http.method`, `http.route`, `http.status_code`, `user.id` (anonymized), `household.id`.
- [x] **AC6:** Logger Pino configurado com redaction de `email`, `password`, `nif`, `iban`, `prompt_text` (NFR12). Logs estruturados em JSON.
- [ ] **AC7:** Dashboard mínimo Grafana criado com 4 painéis: latência p95 do `/api/me`, error rate 5xx, contagem de RLS violations (zero esperado), DB query latency. _(Scaffold JSON `docs/dashboards/grafana-epic1.json` versionado com 4 painéis configurados; export final via UI Grafana após production deploy gerar dados reais — deferido conforme handoff.)_
- [x] **AC8:** `pnpm lint` e `pnpm typecheck` em `packages/observability/` e `apps/web/` passam sem erros.
- [ ] **AC9:** Alertas configurados no Grafana: error rate > 1% em 5 min e latência p95 > 200ms em 5 min para `/api/me` (baseado nos limiares NFR15 escalados para este endpoint). _(Queries documentadas em `docs/runbooks/observability-setup.md` §6; criação efectiva via UI Grafana após production deploy — deferido conforme handoff.)_
- [x] **AC10:** Documentação mínima em `docs/runbooks/observability-setup.md` com instruções para configurar Sentry EU + Grafana Cloud EU + Vercel Observability.

---

## Trace (Article IV — No Invention)

| AC | Rastreabilidade |
|----|----------------|
| AC1 | Architecture §9.1 (stack OTel + Sentry + Grafana), NFR13 |
| AC2 | Architecture §9.1 (`OTel SDK → Grafana Cloud (EU)`), Next.js instrumentation hook |
| AC3 | Architecture §9.1 (Sentry EU data residency + PII redaction), NFR12 |
| AC4 | Architecture §9.1 (Vercel Observability para Web Vitals), NFR4, NFR14 |
| AC5 | Architecture §9.3 (trace ID propagation, `request-id` header) |
| AC6 | Architecture §9.3 (Pino com redactor), NFR12 (`email`, `nif`, `prompt_text` redacted) |
| AC7 | Architecture §9.4 (dashboards essenciais: Agent Health, Platform), NFR14 |
| AC8 | PRD NFR17-19, CON5 |
| AC9 | PRD NFR15 (alertas: error rate > 1% em 5min, latência p95 > 10s em 5min) |
| AC10 | PRD NFR8 (secrets management), Architecture §12.3 |

---

## Tasks

- [x] **Task 1 — Criar package `packages/observability/` (AC1)**
  - [x] 1.1 Criar `packages/observability/package.json` com nome `@meu-jarvis/observability` (source-only, sem build step — consumido via `transpilePackages` em `apps/web/next.config.ts`, igual a `@meu-jarvis/db`)
  - [x] 1.2 Instalar deps: `pino`, `@vercel/otel`, `@opentelemetry/api` (peer dep), `@sentry/nextjs`
  - [x] 1.3 Criar `packages/observability/src/logger.ts` — Pino com PII redaction (paths em Dev Notes)
  - [x] 1.4 Criar `packages/observability/src/tracer.ts` — wrappers `getTracer(name)` + utility para anotar spans com atributos de domínio (`household.id`, `user.id` hashed)
  - [x] 1.5 Criar `packages/observability/src/sentry.ts` — `captureException` wrapper que adiciona contexto `household_id` + scrub de PII antes de delegar
  - [x] 1.6 Criar `packages/observability/src/index.ts` — barrel exports

- [x] **Task 2 — Configurar OTel em Next.js via `@vercel/otel` (AC2)**
  - [x] 2.1 Criar `apps/web/instrumentation.ts` que invoca `registerOTel({ serviceName: 'expressia-web' })` de `@vercel/otel`
  - [x] 2.2 ~~Adicionar `experimental.instrumentationHook = true`~~ — flag removido em Next 15.x, `instrumentation.ts` é estável e activo por default; sem-flag é equivalente. Decisão documentada em `next.config.ts` e `instrumentation.ts`.
  - [x] 2.3 Confirmar que `OTEL_EXPORTER_OTLP_ENDPOINT` e `OTEL_EXPORTER_OTLP_HEADERS` chegam ao runtime serverless via env (Vercel Production + Preview já configurados pelo @devops 2026-05-07 — 8 secrets em runtime)
  - [x] 2.4 NÃO recalcular nem decodificar `OTEL_EXPORTER_OTLP_HEADERS` — respeitado conforme handoff
  - [x] 2.5 Cobertura automática inclui HTTP, fetch, Next.js routing — `@vercel/otel` trata serverless cold-start

- [x] **Task 3 — Configurar Sentry EU (AC3)**
  - [x] 3.1 Criar `apps/web/sentry.server.config.ts` (Node runtime)
  - [x] 3.2 Criar `apps/web/sentry.client.config.ts` (browser)
  - [x] 3.3 Criar `apps/web/sentry.edge.config.ts` (Edge runtime — middleware Next.js corre em edge)
  - [x] 3.4 Wrap `apps/web/next.config.ts` com `withSentryConfig(nextConfig, { org: process.env.SENTRY_ORG, project: process.env.SENTRY_PROJECT, authToken: process.env.SENTRY_AUTH_TOKEN, silent: !process.env.CI })`
  - [x] 3.5 Configurar `beforeSend` hook para redactar `event.user.email`, `event.request.headers.authorization`, `event.request.headers.cookie`, body fields que contenham PII
  - [x] 3.6 Adicionar job step ao `.github/workflows/ci.yaml` com `secrets.SENTRY_AUTH_TOKEN` (+ `SENTRY_ORG`, `SENTRY_PROJECT`) para sourcemap upload no build
  - [ ] 3.7 Verificar pós-deploy: forçar erro 5xx + confirmar evento em Sentry com `data residency = European Union (Frankfurt)` _(deferido para pós-deploy production)_

- [x] **Task 4 — Vercel Observability (AC4)**
  - [x] 4.1 Instalar `@vercel/analytics` e `@vercel/speed-insights`
  - [x] 4.2 Adicionar `<Analytics />` e `<SpeedInsights />` em `apps/web/src/app/layout.tsx`
  - [ ] 4.3 Activar em Vercel Dashboard → Analytics (se não activado) _(deferido — handoff devops confirma Vercel Analytics activado em projecto, validar pós-deploy)_

- [x] **Task 5 — Instrumentar endpoint `/api/me` (AC5)**
  - [x] 5.1 Actualizar `apps/web/src/app/api/me/route.ts` (Story 1.6) para emitir span OTel
  - [x] 5.2 Adicionar atributos: `http.route = '/api/me'`, `user.id = hash(userId)`, `household.id`
  - [x] 5.3 Capturar erros 5xx via Sentry `captureException()`

- [x] **Task 6 — Logger Pino (AC6)**
  - [x] 6.1 Configurar `pino` com `redact` para PII fields (9 paths: email, password, nif, iban, prompt_text, *.email, *.password, req.headers.authorization, req.headers.cookie)
  - [x] 6.2 Adicionar `hashForCorrelation` (sha256) para debug correlation sem PII
  - [x] 6.3 Usar logger em `GET /api/me` para logar requests/responses estruturados

- [ ] **Task 7 — Dashboard Grafana mínimo (AC7)** _(deferido — full UI export requer dados reais pós-deploy)_
  - [x] 7.1 Scaffold dashboard JSON em `docs/dashboards/grafana-epic1.json` com 4 painéis configurados (queries documentadas em runbook §5)
  - [ ] 7.2 Painel 1: latência p95 de `/api/me` _(query no scaffold; criar via UI pós-deploy)_
  - [ ] 7.3 Painel 2: error rate 5xx em 5 minutos _(query no scaffold; criar via UI pós-deploy)_
  - [ ] 7.4 Painel 3: contagem de `rls.policy_violations_total` _(query no scaffold; criar via UI pós-deploy)_
  - [ ] 7.5 Painel 4: DB query latency p95 _(query no scaffold; criar via UI pós-deploy)_
  - [ ] 7.6 Exportar dashboard JSON final para `docs/dashboards/grafana-epic1.json` _(scaffold já versionado; substituir após criação UI)_

- [ ] **Task 8 — Alertas (AC9)** _(deferido — UI Grafana, queries documentadas)_
  - [ ] 8.1 Alerta: error rate > 1% em janela de 5 min → notify (queries em runbook §6)
  - [ ] 8.2 Alerta: latência p95 > 200ms em janela de 5 min → notify
  - [x] 8.3 Documentar alertas em `docs/runbooks/observability-setup.md` §6

- [x] **Task 9 — Documentação e verificação final (AC8, AC10)**
  - [x] 9.1 Criar `docs/runbooks/observability-setup.md` (referência ao handoff secrets preservada em §3 e §9)
  - [x] 9.2 Confirmar workspace `pnpm-workspace.yaml` cobre `packages/observability/` (já cobre via glob `packages/*`)
  - [x] 9.3 Adicionar `transpilePackages: ['@meu-jarvis/db', '@meu-jarvis/auth', '@meu-jarvis/observability']` em `apps/web/next.config.ts`
  - [x] 9.4 `pnpm lint` — exit 0
  - [x] 9.5 `pnpm typecheck` — exit 0
  - [x] 9.6 `pnpm test` — exit 0 (8 novos testes em `@meu-jarvis/observability` + 8 testes existentes `/api/me` + 94 testes RLS — todos passam)
  - [x] 9.7 `pnpm build` — exit 0 (Next.js build com Sentry plugin activo; warnings deprecation Sentry SDK v8 não-bloqueantes documentados em Dev Notes)

---

## File List

_(Preenchido por Dex (@dev) durante implementação 2026-05-08.)_

**Ficheiros criados:**
- `packages/observability/package.json` — `@meu-jarvis/observability` v0.1.0 source-only
- `packages/observability/tsconfig.json` — extends `tsconfig.base.json`
- `packages/observability/vitest.config.ts` — Vitest node env
- `packages/observability/src/logger.ts` — Pino + 9 PII redact paths + `hashForCorrelation`
- `packages/observability/src/tracer.ts` — OTel helpers (`getTracer`, `annotateSpan`, `recordSpanError`, `withSpan`, `DomainAttributes`)
- `packages/observability/src/sentry.ts` — `captureException` wrapper + `scrubExtra` defesa em profundidade
- `packages/observability/src/index.ts` — barrel exports
- `packages/observability/src/__tests__/logger.test.ts` — 8 testes para `hashForCorrelation` + `PII_REDACT_PATHS`
- `apps/web/instrumentation.ts` — `registerOTel({ serviceName: 'expressia-web' })`
- `apps/web/sentry.server.config.ts` — Sentry init Node runtime + `beforeSend` PII scrub
- `apps/web/sentry.client.config.ts` — Sentry init browser + replayIntegration mask
- `apps/web/sentry.edge.config.ts` — Sentry init Edge runtime
- `docs/runbooks/observability-setup.md` — 9 secções (stack, contas EU, secrets, codebase, dashboard, alertas, diagnóstico, custos, referências)
- `docs/dashboards/grafana-epic1.json` — scaffold dashboard 4 painéis (latência p95 /api/me, error rate 5xx, RLS violations, DB latency p95)

**Ficheiros actualizados:**
- `apps/web/next.config.ts` — wrap com `withSentryConfig` + `transpilePackages` extendido com `@meu-jarvis/observability` (NOTA: `experimental.instrumentationHook` removido em Next 15.x — comportamento equivalente)
- `apps/web/src/app/layout.tsx` — `<Analytics />` + `<SpeedInsights />`
- `apps/web/src/app/api/me/route.ts` — wrap em `withSpan` com 5 atributos OTel + `captureException` em erros 5xx + logger Pino estruturado
- `apps/web/package.json` — adicionado `@meu-jarvis/observability`, `@opentelemetry/api`, `@sentry/nextjs`, `@vercel/analytics`, `@vercel/otel`, `@vercel/speed-insights`
- `.github/workflows/ci.yaml` — env `SENTRY_ORG`/`SENTRY_PROJECT`/`SENTRY_AUTH_TOKEN` no job `build` para sourcemap upload
- `pnpm-lock.yaml` — auto-actualizado pelo `pnpm install`
- `docs/HANDOFF-INDEX.md` — handoff `mj-handoff-story-1.7-ready-for-dev-20260508` consumed + arquivado
- `docs/handoffs/archive/mj-handoff-story-1.7-ready-for-dev-20260508.yaml` — `consumed: true` + `consumed_at` + `consumed_by: dev (Dex)` + movido de `handoffs/` para `archive/`

---

## Dev Notes

### Stack de observabilidade (Architecture §9.1, ADR-004)

| Camada | Provider | Região | Razão |
|--------|----------|--------|-------|
| RUM / Web Vitals | Vercel Observability | UE (cdg1 Paris) | Built-in, zero overhead |
| Errors + Replay | Sentry (EU data residency Frankfurt) | UE | DX premium, PII redaction, sourcemap upload |
| Métricas + Traces + Logs | `@vercel/otel` → Grafana Cloud EU (Ireland) | UE | Open standard, auto-instrumentação serverless, free tier suficiente até ~5k households |

**Grafana Cloud free tier:** 10k series de métricas + 50GB logs + 50GB traces — suficiente para o MVP.

### Logger Pino com redaction (Architecture §9.3, NFR12)

```typescript
// packages/observability/src/logger.ts
import pino from 'pino'

export const logger = pino({
  level: process.env.LOG_LEVEL || 'info',
  redact: {
    paths: [
      'email',
      'password',
      'nif',
      'iban',
      'prompt_text',
      '*.email',
      '*.password',
      'req.headers.authorization',
      'req.headers.cookie',
    ],
    censor: '[REDACTED]',
  },
  serializers: {
    err: pino.stdSerializers.err,
  },
})
```

**NUNCA** logar: conteúdo de prompts, email, NIF, IBAN, número de cartão, tokens de sessão.

### OTel instrumentation.ts via `@vercel/otel` (Next.js 15)

**Decisão arquitectural:** usar `@vercel/otel` em vez de `@opentelemetry/sdk-node` cru. Razão: `@vercel/otel` é mantido pelo Vercel, trata serverless cold-start automaticamente, e lê `OTEL_EXPORTER_OTLP_ENDPOINT` + `OTEL_EXPORTER_OTLP_HEADERS` directamente sem boilerplate.

```typescript
// apps/web/instrumentation.ts
import { registerOTel } from '@vercel/otel'

export function register() {
  registerOTel({
    serviceName: 'expressia-web',
    // OTLP endpoint + headers vêm de env vars automaticamente:
    // OTEL_EXPORTER_OTLP_ENDPOINT=https://otlp-gateway-prod-eu-west-6.grafana.net/otlp
    // OTEL_EXPORTER_OTLP_HEADERS=Authorization=Basic%20<base64(InstanceID:Token)>
  })
}
```

```typescript
// apps/web/next.config.ts (excerto)
import { withSentryConfig } from '@sentry/nextjs'

const nextConfig = {
  experimental: {
    instrumentationHook: true, // OBRIGATÓRIO para instrumentation.ts ser invocado
  },
  transpilePackages: ['@meu-jarvis/db', '@meu-jarvis/observability'],
  // ... resto da config
}

export default withSentryConfig(nextConfig, {
  org: process.env.SENTRY_ORG,           // 'eurico-xw'
  project: process.env.SENTRY_PROJECT,   // 'expressia-web'
  authToken: process.env.SENTRY_AUTH_TOKEN,
  silent: !process.env.CI,
})
```

**⚠️ Pontos de atenção (do handoff devops 2026-05-07):**

| Ponto | Detalhe |
|-------|---------|
| Base64 já formatado | `OTEL_EXPORTER_OTLP_HEADERS` já tem `Authorization=Basic%20<base64>` calculado pela UI Grafana — **NÃO recalcular nem decodificar**. SDKs OTel decodificam o `%20` (URL-encoded space) automaticamente. |
| `instrumentationHook` | Sem `experimental.instrumentationHook = true` em `next.config.ts`, o ficheiro `instrumentation.ts` é ignorado em build serverless. |
| Sourcemap upload Sentry | Build em CI precisa de `secrets.SENTRY_AUTH_TOKEN` no workflow GitHub Actions — sem este, o plugin Sentry emite warning não-bloqueante mas sourcemaps não são uploaded e stack traces ficam minificadas no Sentry. |
| Vercel free plan log limit | 1024 MB log per function execution — evitar `logger.info` verbose em hot paths produção (manter em `logger.debug` quando aplicável). |
| Sentry OTLP nativo (descoberto) | Sentry expõe agora endpoint OTLP nativo em `o4510848200278016.ingest.de.sentry.io/api/4510848213581904/integration/otlp`. Permitiria emitir traces directos para Sentry. **Decisão: NÃO usar nesta story** — manter dual-emission (Sentry para errors via SDK, Grafana para tudo via `@vercel/otel`) conforme PRD original. Re-avaliar em Epic 2 se surgir duplicação. |
| Next 15 sem `instrumentationHook` flag | Next.js 15.0+ tornou `instrumentation.ts` estável e activo por default — o flag `experimental.instrumentationHook` foi removido. Manter o flag em `next.config.ts` causa typecheck error TS2353. Story AC2 actualizada nesta implementação. Sem impacto funcional — comportamento equivalente sem flag. |
| Sentry SDK v8 deprecation warning | `@sentry/nextjs` v8.45 emite warning a sugerir mover init de `sentry.{server,edge}.config.ts` para o `register()` do `instrumentation.ts`. AC3 desta story exige os 3 ficheiros separados; manter conforme spec, warnings são não-bloqueantes (build green). Migração para single-file init pode ser feita em story futura quando a API estabilizar. |

### Sentry `beforeSend` hook (NFR12)

```typescript
// apps/web/sentry.server.config.ts
import * as Sentry from '@sentry/nextjs'

Sentry.init({
  dsn: process.env.SENTRY_DSN,
  environment: process.env.NODE_ENV,
  beforeSend(event) {
    // Remover PII antes de enviar
    if (event.request?.headers) {
      delete event.request.headers['authorization']
      delete event.request.headers['cookie']
    }
    if (event.user?.email) {
      event.user.email = undefined // NUNCA logar email em Sentry
    }
    return event
  },
  // Replay com máscara de texto (não captura conteúdo de inputs)
  integrations: [Sentry.replayIntegration({ maskAllText: true, blockAllMedia: true })],
  replaysSessionSampleRate: 0,    // desactivado por default
  replaysOnErrorSampleRate: 0.1,  // apenas em erros
})
```

### Métricas-chave (Architecture §9.2)

| Métrica | Source | Limiar de alerta |
|---------|--------|-----------------|
| `http.server.duration` (p95 `/api/me`) | OTel auto-instrumentation | > 200ms em 5min |
| `http.server.response_count` (5xx rate) | OTel | > 1% em 5min |
| `rls.policy_violations_total` | Log de RLS errors → Grafana | > 0 → CRITICAL |
| `db.client.operation.duration` p95 | OTel Postgres instrumentation | > 300ms sustained |

### Variáveis de ambiente — 8 secrets em runtime (já configurados)

Todos os secrets estão em GitHub Actions (`gh secret list -R DaSilvaAlves/expressia`) **e** Vercel (`vercel env ls`, Production+Preview, Encrypted). NÃO recriar — usar os existentes.

```bash
# Sentry EU (Frankfurt)
SENTRY_DSN=https://<key>@o4510848200278016.ingest.de.sentry.io/4510848213581904
SENTRY_ORG=eurico-xw
SENTRY_PROJECT=expressia-web
SENTRY_AUTH_TOKEN=sntrys_<...>     # Organization Token expressia-ci-prod (sourcemap upload)

# Grafana Cloud EU (Ireland — eu-west-6)
OTEL_EXPORTER_OTLP_ENDPOINT=https://otlp-gateway-prod-eu-west-6.grafana.net/otlp
OTEL_EXPORTER_OTLP_HEADERS=Authorization=Basic%20<base64(InstanceID:Token)>
GRAFANA_API_TOKEN=glc_<...>        # Cloud Access Policy Token expressia-web
GRAFANA_STACK_NAME=expressia
```

**Sentry EU confirmado:** DSN tem `ingest.de.sentry.io` (sufixo `.de.` = Frankfurt EU). Org region é imutável após criação — já validado.

**Grafana EU confirmado:** stack `expressia.grafana.net` em `eu-west-6` (Ireland). Region também imutável após criação — já validado.

**Rotação de tokens:** se necessário, ver "Notas para o consumidor" em `docs/handoffs/mj-handoff-secrets-configured-20260507.yaml` (passos `gh secret set` + `vercel env rm/add` + revogar token antigo).

### Dashboard JSON (exportar de Grafana)

Após criar o dashboard via UI Grafana, exportar JSON via Dashboard → Share → Export. Guardar em `docs/dashboards/grafana-epic1.json` para versionamento e reprodutibilidade.

---

## Testing Notes

- **Unit:** `pnpm typecheck` em `packages/observability/`.
- **Integration:** Verificar que spans OTel chegam ao Grafana Cloud (verificar na UI de Explore → Tempo).
- **E2E:** Não aplicável nesta story.
- **Manual:**
  - Fazer 1 request a `/api/me` e verificar trace no Grafana.
  - Forçar um erro 500 e verificar que aparece no Sentry.
  - Verificar que o dashboard Grafana mostra dados.
  - Verificar que PII não aparece nos logs ou no Sentry.

---

## Definition of Done

- [x] `packages/observability/` exporta logger, tracer, captureException
- [x] OTel instrumentation activa em Next.js 15 (instrumentation.ts via `@vercel/otel`)
- [x] Sentry configurado com data residency EU e PII redaction (3 runtimes: server, client, edge)
- [x] Vercel Observability activo em layout.tsx (`<Analytics />` + `<SpeedInsights />`)
- [x] Endpoint `/api/me` emite spans OTel com 5 atributos (`http.method`, `http.route`, `http.status_code`, `user.id` hashed, `household.id`)
- [x] Logger Pino com redaction de PII (9 paths)
- [x] Dashboard Grafana scaffold versionado em `docs/dashboards/grafana-epic1.json` _(criação UI Grafana deferida pós-deploy)_
- [x] Alertas error rate + latência documentados em runbook §6 _(criação UI Grafana deferida pós-deploy)_
- [x] `docs/runbooks/observability-setup.md` criado (9 secções)
- [x] `pnpm lint` e `pnpm typecheck` passam (exit 0)
- [x] `pnpm test` exit 0 (110 testes — 8 novos observability + 8 existentes /api/me + 94 RLS db-test + 1 smoke)
- [x] `pnpm build` exit 0 (Sentry plugin activo, sourcemap upload condicional ao `SENTRY_AUTH_TOKEN`)
- [x] @architect gate APPROVED _(score 9.4/10 HIGH confidence — ver `docs/qa/gates/1.7-architect-gate.md`)_
- [x] PR ready para @devops merge _(branch `main`, commit feat `a16cdc9` + commit chore docs do gate; @devops *push 2026-05-08)_

---

## CodeRabbit Integration

> **CodeRabbit Integration**: Desactivado em `core-config.yaml`. Revisão manual pelo @architect.

---

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-05-04 | 1.0 | Story criada; BLOQUEADORES EXTERNOS documentados (Sentry EU, Grafana Cloud EU) | River (@sm) |
| 2026-05-08 | 1.1 | Refresh pós-handoff `mj-handoff-secrets-configured-20260507`. Bloqueadores B3+B4 marcados ✅ DESBLOQUEADO. AC1/AC2/AC3 actualizados com pacote `@vercel/otel` (auto-instrumentação Next.js) em vez de `@opentelemetry/sdk-node` cru. Tasks 1, 2, 3 reescritas com configs reais (`experimental.instrumentationHook`, `withSentryConfig` com `SENTRY_ORG`/`SENTRY_PROJECT`, sourcemap upload via `SENTRY_AUTH_TOKEN` em CI). Tasks 7-8 sem flag `[REQUER X]` (credenciais prontas). Dev Notes: pontos de atenção do handoff (Base64 não recalcular, log limit 1024 MB, Sentry OTLP nativo descoberto mas mantida dual-emission). Endpoints reais: `o4510848200278016.ingest.de.sentry.io` + `otlp-gateway-prod-eu-west-6.grafana.net/otlp`. Lista dos 8 secrets em runtime + rotação documentada. Story pronta para `@po *validate-story-draft 1.7`. | River (@sm) |
| 2026-05-08 | 1.2 | Validação `*validate-story-draft 1.7` por Pax (@po) — **GO** com score 9.5/10. 10 critérios validados: Title/Description/AC/Scope/Dependencies/Estimate/BusinessValue/Risks/DoD/Alignment ✅. Executor assignment válido (`@dev` ≠ `@architect`). Anti-hallucination ✅ (todos os pacotes/endpoints/features verificáveis). 2 minor fixes aplicados pelo @po para coerência interna: (1) File List acrescentada com `apps/web/sentry.edge.config.ts` (Task 3.3), `.github/workflows/ci.yaml` (Task 3.6, sourcemap upload) e deps reais em `apps/web/package.json`; (2) Stack table em Dev Notes corrigida `OTel SDK` → `@vercel/otel` para coerência com decisão arquitectural. Status transicionado **Draft → Ready** (regra `story-lifecycle.md` — autoridade @po em `*validate-story-draft` GO). Próximo passo: `@dev *develop 1.7`. | Pax (@po) |
| 2026-05-08 | 1.3 | Implementação `*develop 1.7` por Dex (@dev) em modo YOLO. Status **Ready → InProgress → Ready for Review**. Tasks 1-6 + 9 completas (núcleo da observabilidade); Tasks 7-8 (dashboard Grafana + alertas via UI) deferidas para pós-deploy production conforme handoff `mj-handoff-story-1.7-ready-for-dev-20260508` (precisam de dados reais nos painéis). Decisões arquitecturais documentadas em Dev Notes: (1) `experimental.instrumentationHook` removido em Next 15.x — story v1.2 e handoff devops baseavam-se em Next 13/14; comportamento equivalente sem flag, decisão registada em `next.config.ts` e `instrumentation.ts`. (2) Imports relativos `./logger` (sem extensão) em vez de `@/logger` no source de `@meu-jarvis/observability` — convenção para packages source-only consumidos via `transpilePackages` (TS path mappings não atravessam transitively + webpack não auto-strip `.js`). (3) Sentry SDK v8 emite warning a sugerir mover init para `instrumentation.ts` — AC3 exige 3 ficheiros separados, mantidos conforme spec, warnings não-bloqueantes. Quality gates: `pnpm lint` ✅ exit 0, `pnpm typecheck` ✅ exit 0, `pnpm test` ✅ 110 testes verde, `pnpm build` ✅ exit 0. AC1-AC6 + AC8 + AC10 ✅; AC7 + AC9 partial (scaffold + queries documentadas, criação UI deferida). DoD: 11/13 marks ✅, restantes 2 são `@architect` quality gate + `@devops` push. Próximo passo: `@architect` quality gate. | Dex (@dev) |
| 2026-05-08 | 1.4 | Quality gate por Aria (@architect) — **APPROVED** com score **9.4/10** Confidence: HIGH. Gate report completo em `docs/qa/gates/1.7-architect-gate.md` (10 áreas arquitecturais validadas: stack `@vercel/otel`, decisão autónoma sobre `instrumentationHook`, Sentry 3 runtimes EU, PII redaction 3 camadas, dual-emission Sentry+OTel, aderência §9, Tasks 7-8 PARTIAL aceito, Vercel RUM, package design, NFRs). Decisão autónoma do @dev sobre remoção de `experimental.instrumentationHook` validada com evidência externa autoritativa (Next.js 15 release notes oficiais + Sentry GH issue #13551 — em Next 15+ `instrumentation.ts` é estável e activo por default, manter o flag causa `TS2353`). Dois fixes documentais MINOR aplicados directamente pelo @architect no gate via commit chore: (DOC-001) runbook §7 contradizia §4 sobre `experimental.instrumentationHook` — corrigido para reflectir realidade Next 15; (DOC-002) `architecture.md` Tech Stack table line 121 + §9.1 line 728 não especificavam `@vercel/otel` como wrapper canónico — clarificado nas duas linhas. Tasks 7-8 PARTIAL aceitas (deferral pragmático: dashboard JSON está completo e versionado, criação UI sem dados reais produziria gráficos vazios). 3 OBS-* não-bloqueantes documentados para sessão pós-deploy (UI Grafana + smoke test 5xx + Vercel Analytics check). Status transicionado **Ready for Review → Done**. Push final pelo @devops Gage com `chore(docs)` commit a fechar a story. Story file movida `active/` → `completed/` conforme convenção (1.4, 1.5, 1.6). Próximo passo: handoff `mj-handoff-1.7-post-deploy-grafana-20260508` activado em sessão `@devops` futura quando Vercel deploy estiver verde. | Aria (@architect) + Gage (@devops) |
