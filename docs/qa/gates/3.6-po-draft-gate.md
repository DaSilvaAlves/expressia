# Story 3.6 — @po Draft Validation Gate v1.0

**Story:** [3.6 Sistema de tags globais (criar/listar/aplicar inline)](../../stories/active/3.6.tags-system.md)
**Validator:** @po (Pax)
**Phase:** SDC Phase 2 — Validate (post-draft, pre-architect-ratify)
**Date:** 2026-05-18
**Mode:** YOLO autónomo (Eurico delegated)
**Checklist:** AIOX 10-point story-draft validation (canónico — adaptado de `validate-next-story.md`)
**Input:** `docs/stories/active/3.6.tags-system.md` v1.0 (@sm River, 2026-05-18)

---

## TL;DR

**Verdict: NO-GO (5.5/10)** — Story 3.6 tem fundação sólida (DB + API confirmados existentes; PT-PT impecável; ACs bem decompostos; tracing FR12 explícito) MAS o draft v1.0 contém **2 invenções críticas** que contradizem o estado real do codebase e **1 risco subestimado** que altera o tamanho do scope. Requer @sm rework antes de @architect ratify-DPs.

| Categoria | Pontos | Severidade |
|-----------|--------|------------|
| Estrutura, ACs, tasks, NFRs, PT-PT | 7/10 PASS | — |
| Invenções tecnológicas (shadcn/Radix Dialog+Popover) | 2 CRITICAL FIX | BLOCKER |
| `GET /api/tasks` shape (R-3.6.1 subestimado) | 1 CRITICAL FIX | BLOCKER |

---

## 10-Point Validation Checklist

### Point 1 — Goal & Context Clarity

**Status:** ✅ PASS

**Evidência:**
- User Story explícita (linhas 46-50): "Como utilizador da Expressia... Quero criar tags globais... Para que possa organizar e localizar tarefas por contexto..."
- Valor de negócio claro: trace a **FR12** (PRD linha 53 — "Sistema deve permitir tags globais por household") + Epic 3 §6.3 (PRD linha 301 — "3.6 Sistema de tags globais (criar/listar/aplicar)")
- Persona enquadrada (@household-owner / @household-member) com isolação multi-tenant explícita

**Sem reservas.** Goal e contexto suficientes para @dev.

---

### Point 2 — Acceptance Criteria Coverage

**Status:** ⚠️ PARTIAL (com fix recomendado)

**Evidência:**
- 11 ACs bem decompostos: AC1 TagBadge · AC2 TagPicker · AC3 TagsManager · AC4 integração 3 vistas · AC5 filtro URL · AC6 `?with_counts` · AC7 RLS/audit · AC8 empty/loading/error · AC9 a11y · AC10 performance · AC11 PT-PT
- Cada AC tem "**ACn PASS:**" critério explícito mensurável
- Cobertura horizontal completa (CRUD + UI + integração + a11y + perf + i18n)

**Reservas (não bloqueantes, mas devem ser endereçadas em fix):**
- **AC4(a) "+N chip"** não especifica cor/styling do chip extra (ex: `+2` muted vs accent)
- **AC4(c) calendário 1 tag + dot**: comportamento quando há 3+ tags ambíguo — só 1 dot ou múltiplos dots?
- **AC8(c) loading TagPicker**: spinner "16px animate-spin" mas falta especificar onde aparece (inline no trigger? no popover header?)
- **AC10(d) "< 200ms"** TagsManager: incluir overhead do `task_count` subquery — esta meta é optimista se household tem milhares de task_tags. Considerar 300-500ms ou justificar via index `task_tags_tag_idx`.

**Score:** 7/10 ACs estão completamente self-contained; 4 ACs têm pequenas ambiguidades acima.

---

### Point 3 — Tasks/Subtasks Alignment

**Status:** ✅ PASS (com observação NIT)

**Evidência:**
- 9 Tasks (~25 subtasks) — T1 pré-condições → T9 quality gates
- Mapeamento AC↔Task explícito em headers (ex: "Task 2 — TagBadge + utilidades de cor (AC1, AC9d)")
- Sequência lógica: T1 verifica → T2/T3 fundação → T4/T5 componentes → T6/T7 integração → T8 testes → T9 gates
- Granularity adequada (subtasks ~30min-1h cada)

**NIT:** Task 6.5 "Confirmar join/N+1: se `GET /api/tasks` não retorna tags, adicionar JOIN..." está dentro de Task 6 (integração 3 vistas), mas é uma decisão arquitectural de query design que pode merecer Task dedicada (ou pelo menos elevação para `[ARCH-DECISION]` que precisa de @architect approval, não `[DEV-DECISION]`). Ver Point 9.

---

### Point 4 — Technical Implementation Guidance

**Status:** ❌ FAIL (factual drift bloqueante)

**Evidência negativa (anti-hallucination):**

#### CRITICAL FIX #1 — shadcn Dialog NÃO existe no projecto

A story refere **shadcn `<Dialog>`** em:
- AC3(a) linha 125: "Abre modal shadcn `<Dialog>`"
- AC3(h) linha 150: "modal full-screen em `< 768px` (padrão shadcn Dialog responsive)"
- T1.2 linha 316: "Confirmar que shadcn Dialog está disponível... Se não existir: `pnpm dlx shadcn@latest add dialog`"
- T5.1 linha 345: "Abre via `<Dialog>` shadcn"

**Realidade verificada (Grep):**
```
apps/web/package.json
- Zero entradas @radix-ui/*
- Zero entrada shadcn-ui ou class-variance-authority
- Zero entrada cmdk ou Sonner
```

```
Glob apps/web/src/**/dialog.tsx → 0 ficheiros
Glob apps/web/src/**/popover.tsx → 0 ficheiros
ls apps/web/src/components/ui/ → directory does NOT exist
```

```
EditTaskModal.tsx (Story 3.3 Done) — modal **hand-rolled** (useState open + `if (!open) return null` + useEffect Esc handler). Sem shadcn, sem Radix.
```

**Precedent crítico** (Story 3.5 architect ratify v1.2 — INDEX linha 197):
> "D-3.5.2 pre-impl ratify assumiu Radix instalado mas zero deps Radix em package.json — D-3.5.2 pragmatic correcto vs constraint zero new deps DP-3.5.1"

**Implicação:** O Story 3.6 v1.0 inventa uma stack (shadcn Dialog + Popover + AlertDialog) que NÃO existe no projecto. Adicionar `@radix-ui/react-dialog` + `@radix-ui/react-popover` + `@radix-ui/react-alert-dialog` + `class-variance-authority` + `cmdk` é:
1. **Decisão arquitectural não-trivial** (5+ novas deps, +~120kB no bundle JS) — pertence a @architect, não a @sm draft
2. **Altera o size estimate** de S (6-10h) para M (10-14h+) — instalação shadcn CLI + 3 componentes + Tailwind config + testes integração
3. **Contradiz o pattern Epic 3** já estabelecido — Story 3.3/3.4/3.5 todas hand-rolled (KanbanBoardClient, CalendarTaskCard, EditTaskModal)

#### CRITICAL FIX #2 — shadcn Popover NÃO existe no projecto

Mesma realidade que Dialog. Referido em:
- AC2(c) linha 101: "botão trigger `[+] Tag` ... que abre popover/dropdown"
- T1.3 linha 317: "Confirmar que Popover shadcn está disponível... Se não existir: `pnpm dlx shadcn@latest add popover`"
- T4.1 linha 335: "Botão trigger `[+] Tag` abre Popover shadcn"

**Mesma conclusão que Fix #1.**

#### CRITICAL FIX #3 — `GET /api/tasks` shape — R-3.6.1 é REALIDADE confirmada (não risco mitigado)

A story trata como Risk MEDIUM (linha 405) com mitigação "T1.1 verifica shape actual antes de iniciar Task 6. Se não inclui tags: adicionar JOIN `json_agg` ou batch."

**Realidade verificada (Read apps/web/src/lib/api-helpers/list-tasks.ts:32-50):**

```typescript
export interface TaskRow {
  id: string;
  household_id: string;
  // ... 17 campos ...
  created_at: string;
  updated_at: string;
}  // SEM campo tags[]
```

E linha 125 do mesmo helper:
```typescript
sql`id in (select task_id from public.task_tags where tag_id = ${filters.tag_id}::uuid)`
```

→ Backend Story 3.2 suporta **FILTRO por `tag_id`** (sub-query IN) MAS não retorna o array `tags` no response. **Confirmado:** integração TagBadge em cards (AC4a/b/c) requer alteração do `listTasksHelper` para adicionar JOIN com `json_agg` OU 2ª query batch — não é "verificar se existe", é **certeza de que NÃO existe**.

**Implicação:** Esta alteração toca:
- `listTasksHelper.ts` (assinatura `TaskRow` + query) — file partilhado entre route handler e RSC
- Tipos Zod em `api-schemas/tasks.ts` (adicionar `tags: TagSchema[]`)
- Possível performance regression (JOIN `LEFT JOIN task_tags + tags` com `json_agg GROUP BY tasks.id` — testar p95 em 1000 tasks com 5000 task_tags)
- **3 vistas RSC** consumem este helper — calendar pode usar variant separado (Story 3.5 fetch unscheduled+scheduled)

Esta NÃO é decisão `[DEV-DECISION]` ad-hoc — é **decisão de query design** que afecta `listTasksHelper` (Story 3.3 D-3.3.1 ratified) e precisa de @architect (data-engineer pattern). Subestimar isto como risco MEDIUM mantém o size em S falsamente.

---

### Point 5 — Reference Effectiveness

**Status:** ✅ PASS

**Evidência:**
- Dev Notes lista 7 referências canónicas com line numbers específicos (PRD §FR12, schema `tasks.ts:168-223`, RLS `0001:403-435`, API route paths exactos, pattern Story 3.5 Done, pattern toast Story 3.4 linhas 84-87+97+143-146+353-365)
- Bloqueadores Externos tabela (linhas 36-40) com line refs verificáveis
- Risks tabela (linhas 404-408) com mitigação concreta por risco

**Reservas:** Pattern shadcn refs (Dev Notes linha 423) está inválido — ver Critical Fix #1/#2. Após fix, esta linha deve ser removida.

---

### Point 6 — Self-Containment

**Status:** ⚠️ PARTIAL

**Evidência:**
- @dev tem User Story + 11 ACs + 9 Tasks + Dev Notes + NFR + Constitution + Risks no mesmo ficheiro
- Dependências Done verificáveis (Story 3.1, 3.2, 3.3, 3.4, 3.5 todas linkadas em /completed)

**Reservas:**
- Após Critical Fixes #1/#2: @dev precisará de decisão clara de **qual approach** substitui shadcn (hand-roll Dialog/Popover seguindo pattern EditTaskModal? Adicionar Radix? Adoptar Headless UI?). Sem esta decisão, story não é self-contained.
- Após Critical Fix #3: @dev precisará de query design ratificado (json_agg vs batch fetch). Hoje é "DEV-DECISION" demasiado vago para size S.

---

### Point 7 — Testing Guidance

**Status:** ✅ PASS

**Evidência:**
- Task 8 tem 5 suites Vitest+RTL explícitas com nº de tests por suite (4+6+4+3+3 = 20 tests target)
- Pattern testing estabelecido em Epic 3 (Stories 3.3/3.4/3.5 — todas com RTL `userEvent` + mock fetch)
- Baseline tracking documented (T1.4 — "target 435 após Story 3.5 — confirmar antes")
- Quality gates 5/5 explícitos em Task 9

**NIT:** falta menção a estratégia para evitar flaky tests (precedent Story 3.5 §CONCERN-1 LOW "flaky test inicial em api/conta/preferencias"). Recomendação: adicionar nota "se test flaky em primeiro run, isolar com `.only` antes de baseline."

---

### Point 8 — NFR Coverage

**Status:** ✅ PASS

**Evidência:**
- NFR5 RLS multi-tenant: ZERO new tables → `pnpm check:rls` mantém GREEN 120 policies (confirmado por mim: 0001 contém 4 policies `tags` + 4 policies `task_tags`)
- NFR9 perf: budgets explícitos AC10 (4 budgets) + `React.memo` AC10(c)
- NFR12 PII: nota "Tag names não logar em texto claro em spans OTel" (Dev Notes NFR table linha 474)
- NFR19 absolute imports: explícito linha 475 + Constitution VI
- CON3 PT-PT: 14 strings inventariadas em AC11 + grep target (AC11 PASS criterion)
- CON9 EUR: N/A (módulo Tarefas sem $)

**Sem reservas.**

---

### Point 9 — Constitution Compliance (Article IV — No Invention)

**Status:** ❌ FAIL

**Article IV — No Invention exige que cada decisão técnica rastreie a source documents.**

**Violações detectadas:**

1. **shadcn Dialog/Popover** (Fix #1/#2) — invenção pura. Zero referência no PRD, architecture.md, front-end-spec, ou package.json. Single source-of-truth para UI stack é `apps/web/package.json` (zero Radix). @sm assumiu — não verificou.

2. **`?with_counts=true` query param extension** (AC6) — não está no PRD nem em Story 3.2 (Done). É invenção de @sm para servir TagsManager (AC3f). Embora seja **decisão sensata** (backward compat, opt-in), formalmente é um endpoint extension que altera contrato API e pertence a @architect aprovação. Por outro lado, é additive e segue pattern Story 3.2 §AC3 — pode ser aceitável como **[DEV-DECISION ratificável]** se explicitamente flagged. Hoje está implícito.

3. **Palette 8 cores PT-friendly** (DP-3.6.1) — auto-decision sensata mas sem trace ao front-end-spec ou design tokens. Sem base documental, é invenção. Aceitável se documentar "DP-3.6.1 invenção @sm para scope S — Eurico pode override em design system futuro".

**Outros Articles:**
- I CLI First: PASS — Tags via API completa Story 3.2; UI é layer adicional
- II Agent Authority: PASS — `@dev` owner + `@architect` gate explícitos
- III Story-Driven: PASS — todo o trabalho rastreado a 3.6
- V Quality First: PASS — Task 9 explícita 5/5 gates
- VI Absolute Imports: PASS — Dev Notes especifica

**Score:** 4/6 articles PASS. Article IV violation é o que motiva NO-GO.

---

### Point 10 — Anti-Hallucination Cross-Confirms (PO due diligence)

**Status:** ❌ FAIL (3 findings confirmados via grep+read)

| # | Story claim | Realidade verificada | Severidade |
|---|-------------|---------------------|------------|
| 1 | "shadcn Dialog disponível ou installable via `pnpm dlx`" (T1.2) | Zero Radix em `package.json`. `components/ui/` directory does NOT exist. `pnpm dlx shadcn@latest add dialog` adiciona ~5 deps + Tailwind config + components/ui/ infra — non-trivial. | CRITICAL |
| 2 | "Popover shadcn disponível ou installable" (T1.3) | Idem #1. | CRITICAL |
| 3 | "T6.5 verificar se GET /api/tasks já retorna tags array" (linha 359) | **Verificado:** `TaskRow` interface (`list-tasks.ts:33-50`) NÃO inclui `tags`. JOIN obrigatório para AC4(a/b/c) — não é incerteza, é certeza. | CRITICAL |
| 4 | "Sonner NÃO instalado, confirmado Story 3.5" (AC8g) | Verificado: grep zero matches. PASS. | OK |
| 5 | "tags + task_tags tabelas + 8 RLS policies aplicadas Story 3.1" | Verificado: `schema/tasks.ts:174-224` + `0001_rls_policies.sql:403-435`. PASS. | OK |
| 6 | "6 endpoints tags completos Story 3.2" | Verificado: `apps/web/src/app/api/tags/route.ts` (GET+POST) + `[id]/route.ts` + `api/tasks/[id]/tags/` (route.ts + [tagId]/route.ts). PASS. | OK |
| 7 | "audit log tag.created em POST" (AC7c) | Verificado: `apps/web/src/app/api/tags/route.ts` chama `insertAuditLog` no POST handler (Story 3.2 imports). PASS. | OK |

**Score:** 4/7 claims PASS, 3/7 CRITICAL FAIL.

---

## Score Summary

| Point | Verdict | Score |
|-------|---------|-------|
| 1. Goal & Context | PASS | 1.0 |
| 2. AC Coverage | PARTIAL | 0.75 |
| 3. Tasks Alignment | PASS | 1.0 |
| 4. Technical Guidance | FAIL (3 CRITICAL) | 0.0 |
| 5. Reference Effectiveness | PASS | 1.0 |
| 6. Self-Containment | PARTIAL | 0.5 |
| 7. Testing Guidance | PASS | 1.0 |
| 8. NFR Coverage | PASS | 1.0 |
| 9. Constitution Compliance (Article IV) | FAIL | 0.0 |
| 10. Anti-Hallucination | FAIL | 0.25 |

**Raw score:** 6.5/10
**Adjusted score** (Article IV violation penalty − 1.0): **5.5/10**

**Decision threshold:**
- GO: ≥ 7.0
- NO-GO: 5.0 ≤ score < 7.0 → **THIS STORY**
- REJECT: < 5.0

---

## Verdict: **NO-GO**

**Score: 5.5/10 — MEDIUM confidence**

Story tem fundação sólida (DB+API+RLS verificados, ACs decompostos, PT-PT impecável, NFR cobertos, tasks bem sequenciadas) mas precisa de rework @sm em 3 áreas críticas antes de avançar para @architect ratify-DPs. Os fixes são **factíveis em 30-60 min** — não há nada inerentemente quebrado, apenas drift entre assumptions e codebase real.

---

## Required Fixes (Bloqueantes para GO)

### FIX-3.6.1 — Substituir referências shadcn Dialog/Popover/AlertDialog por approach real

**Locais a editar:**
- AC3(a) linha 125: substituir "Abre modal shadcn `<Dialog>`" → "Abre modal hand-rolled seguindo pattern `EditTaskModal.tsx` (useState open + `if (!open) return null` + useEffect Esc handler + portal opcional para z-index)"
- AC3(h) linha 150: substituir "padrão shadcn Dialog responsive" → "responsive via Tailwind `md:max-w-2xl` + mobile `inset-0` full-screen"
- T1.2 e T1.3 (linhas 316-317): substituir "Confirmar shadcn Dialog/Popover... pnpm dlx" → "**Decisão arquitectural pre-impl:** hand-roll Dialog + Popover seguindo `EditTaskModal` pattern (zero new deps) OU @architect ratify trade-off de adicionar Radix bundle. Recomendação @po: hand-roll para preservar Constitution VI scope S (precedent Story 3.5 D-3.5.2 ratified pragmatic)."
- T5.1 linha 345: "Abre via hand-rolled modal seguindo pattern EditTaskModal Story 3.3"
- T5.4 linha 348: substituir "<AlertDialog>" → "`window.confirm()` simples OU modal nested hand-rolled (decisão @dev, documentar como [DEV-DECISION])"
- T4.1 linha 335: substituir "abre Popover shadcn" → "abre dropdown hand-rolled com `useRef` + click-outside listener (pattern testado nos componentes Epic 3)"
- Dev Notes linha 423 (Pattern shadcn refs): REMOVE inteiramente OU substituir por "Pattern hand-rolled modal: ver `EditTaskModal.tsx:14-20` (Story 3.3). Pattern dropdown com keyboard: ver `KanbanBoardClient.tsx` colSelect."

**Risk R-3.6.2** linha 406: REMOVE (shadcn não existe; mitigação não aplica).

**Resultado esperado:** zero referências a "shadcn", "Radix", "@radix-ui" na story. Approach hand-rolled consistente com Epic 3 precedent.

---

### FIX-3.6.2 — Elevar R-3.6.1 a Task explícita + ratificar query design

**Razão:** `GET /api/tasks` NÃO retorna `tags` array (confirmado em `list-tasks.ts`). AC4(a/b/c) integração badges nas 3 vistas REQUER esta extensão. Não é "verificar — se não, adicionar" — é "confirmar shape + adicionar JOIN com decisão de design".

**Edits:**
- Adicionar **Task 6.0** (antes de T6.1): "**`listTasksHelper` extension** — adicionar `tags: { id, name, color }[]` ao `TaskRow`. Approach A (recomendado): `LEFT JOIN task_tags + tags + json_agg(...) FILTER (WHERE tags.id IS NOT NULL) GROUP BY tasks.id`. Approach B: 2ª query batch (`SELECT * FROM task_tags JOIN tags WHERE task_id IN (...)`) — pior latency mas evita GROUP BY. **[DEV-DECISION pre-impl D-3.6.1 ratificável]** — Eurico/@architect approve A vs B antes de T6.1. Tests p95 em fixture 1000 tasks × 5000 task_tags. Touch points: `list-tasks.ts:32-50` (TaskRow) + `api-schemas/tasks.ts` (Zod) + route.ts response."
- Adicionar **DP-3.6.6** (novo Decision Point): "Query design para JOIN tasks↔tags — Recomendação A `json_agg` (1 query, p95 < 100ms previsto, idiomatic Postgres) vs B batch 2 queries (mais simples mas N latency adicional). [AUTO-DECISION → A pendente Eurico/Aria confirm]."
- Atualizar Risks tabela: R-3.6.1 verdade reformulada de "probabilidade MEDIUM" → "**REALIDADE CONFIRMED**" + mitigação = "Task 6.0 explícita resolve pre-impl. @architect deve ratify DP-3.6.6 em fase ratify-DPs."
- Reavaliar **Estimate** total: +0.5h em T6.0 (query design + JOIN test) → total ~9h (ainda S mid-range — mantém-se em S, mas margem reduzida).

---

### FIX-3.6.3 — Formalizar AC6 `?with_counts=true` como extension declarada (anti-Article IV)

**Razão:** Article IV exige que cada feature rastreie a source. `?with_counts=true` foi inventado por @sm sem trace explícito.

**Edits:**
- AC6 linha 196: adicionar nota inicial "**[STORY-INVENT — auto-decision @sm justificável por scope S necessitar count por tag em TagsManager AC3f. Backward compat opt-in. @architect ratify formal em DP-3.6.X.]**"
- Adicionar **DP-3.6.7** (novo): "Endpoint extension `GET /api/tags?with_counts=true` — A: opt-in query param sem breaking change Story 3.2 (recomendado, atómico) vs B: novo endpoint `/api/tags/with-counts` (mais explícito mas duplica logic) vs C: sempre incluir `task_count` (breaking change Story 3.2 — REJECT). [AUTO-DECISION → A]"
- Adicionar trace em **Constitution Checks** Article IV: substituir "PASS — AC1-AC11 rastreiam FR12 / Epic 3 §3.6 / front-end-spec §656-708" → "PASS — AC1-AC11 rastreiam FR12 / Epic 3 §3.6 / front-end-spec §656-708; AC6 extension `?with_counts` documentada como [STORY-INVENT-DP-3.6.7] ratificável."

---

## Should-Fix (Recomendado mas não bloqueante)

### SF-3.6.1 — Esclarecer "+N chip" styling AC4(a)

Adicionar especificação visual: "chip `+N` usa `bg-muted text-muted-foreground text-xs rounded-full px-2 py-0.5` consistente com chip pattern Epic 3".

### SF-3.6.2 — Esclarecer comportamento múltiplos tags no calendário AC4(c)

"Se `tags.length > 1`: mostrar `<TagBadge>` da primeira + 1 dot `•` (não múltiplos — visual cluttered). Tooltip do badge inclui count restante (`+N`)".

### SF-3.6.3 — Mitigar flaky test risk pre-emptive

Adicionar a T8 nota: "Se test flaky em primeiro run (precedent Story 3.5 CONCERN-1), aplicar `.only` para isolar + investigar antes de batch run. Não aceitar baseline regressão."

### SF-3.6.4 — Performance budget realista AC10(d)

Revisitar "< 200ms TagsManager initial load" — com household real de 100+ tags e 5000+ task_tags, subquery `SELECT COUNT(*)` por tag pode aproximar-se de 300-500ms. Validar via EXPLAIN ANALYZE em fixture ou ajustar budget.

---

## Decision Points Review — DPs auto-resolved by @sm

| DP | @sm decision | @po review | Eurico override needed? |
|----|--------------|-----------|--------------------------|
| **DP-3.6.1** Palette 8 cores | A (8 + custom hex) | **APPROVE silent** — defensable scope S, custom hex preserva power user. Eurico pode revisitar em design system futuro (Epic 6+). | NO — auto-decision suficiente |
| **DP-3.6.2** Soft limit 5 tags | A (UI soft limit) | **APPROVE silent** — 5 é razoável; backend não enforça (correcto para backward compat). | NO |
| **DP-3.6.3** Autocomplete + criação inline | A | **APPROVE silent** — UX standard; precedent Linear/Notion. | NO |
| **DP-3.6.4** Modal Dialog vs página | A (modal) | **APPROVE silent CONDICIONAL FIX-3.6.1** — modal correcto para scope S, mas implementation precisa de hand-roll não shadcn. | NO (DP) / SIM (Fix #1 implementation) |
| **DP-3.6.5** Filtro URL param | A (`?tag_id=`) | **APPROVE silent** — consistente com backend Story 3.2 + 3.3 `?status=`/`?priority=` patterns. | NO |

**Novos DPs propostos via fixes:**
- **DP-3.6.6** (Fix #2) — Query design JOIN tasks↔tags: A `json_agg` vs B batch. [AUTO-DECISION → A pendente Aria ratify-DPs]
- **DP-3.6.7** (Fix #3) — Endpoint `?with_counts=true` declarado: A opt-in param. [AUTO-DECISION → A]

**Nenhum DP requer override Eurico antes de @dev — todos auto-decision sensatos.** Os 2 DPs novos pertencem a @architect na fase ratify-DPs.

---

## Anti-Hallucination Cross-Confirms (PO due diligence)

Verificações executadas durante esta validação (anti-pattern "blind trust @sm"):

| # | Claim verificada | Tool | Resultado |
|---|-------------------|------|-----------|
| 1 | `tags` schema + 4 RLS policies | Read `schema/tasks.ts:160-225` + Grep `0001_rls_policies.sql` | ✓ PASS (8 policies tags+task_tags em linhas 403-435) |
| 2 | API tags endpoints existentes | Bash `ls apps/web/src/app/api/tags/` | ✓ PASS (route.ts + [id]/ + tasks/[id]/tags/) |
| 3 | `GET /api/tasks` retorna `tags` array | Read `list-tasks.ts:32-50` (TaskRow) | ✗ FAIL — sem campo tags (FIX-3.6.2) |
| 4 | shadcn Dialog/Popover instalado | Glob `**/dialog.tsx` + `**/popover.tsx` + Grep package.json @radix | ✗ FAIL — zero matches (FIX-3.6.1) |
| 5 | EditTaskModal hand-rolled | Read `EditTaskModal.tsx:1-50` | ✓ PASS — useState+useEffect, sem Radix |
| 6 | Sonner NÃO instalado | Grep `sonner\|@radix\|AlertDialog` | ✓ PASS (zero matches) |
| 7 | FR12 PRD trace | Grep `prd.md` line 53 | ✓ PASS |
| 8 | Story 3.5 precedent zero Radix | Grep INDEX line 197 | ✓ PASS — confirma constraint estabelecido |

**Score:** 6/8 claims @sm validados. 2 CRITICAL discrepâncias confirmadas via codebase real (não memória / não assumptions).

---

## Next Steps

### Para @sm (REWORK v1.1 ~30-60min)

1. Aplicar FIX-3.6.1 (substituir shadcn → hand-roll across 7 locations)
2. Aplicar FIX-3.6.2 (adicionar Task 6.0 + DP-3.6.6 + atualizar R-3.6.1 + revisar estimate)
3. Aplicar FIX-3.6.3 (declarar AC6 como STORY-INVENT + DP-3.6.7)
4. (Opcional) Aplicar SF-3.6.1 a SF-3.6.4
5. Update Change Log: v1.0 → v1.1 com summary dos 3 fixes
6. Submeter para @po re-validate v1.1 (estimado 5-10min, score esperado 8.0-8.5/10)

### Para @po (após @sm v1.1)

Re-run validation com foco nos 3 fixes aplicados. Score target ≥ 7.0 para GO.

### Para @architect (após @po GO v1.1)

Phase 3 SDC — `*ratify-dps 3.6`:
- Confirm DP-3.6.4 hand-roll Dialog approach
- Ratify DP-3.6.6 query design (json_agg recomendado)
- Ratify DP-3.6.7 endpoint extension pattern
- Cross-confirm anti-hallucination (Aria pattern Story 3.4/3.5)

---

## Change Log (Gate)

| Versão | Data | Verdict | Score | Notas |
|--------|------|---------|-------|-------|
| 1.0 | 2026-05-18 | **NO-GO** | 5.5/10 | 3 CRITICAL FIXES required (shadcn invenção × 2 + R-3.6.1 subestimado). PT-PT impecável, ACs sólidos, NFR coberto. Constitution Article IV violado (invenção shadcn). Estimated rework: 30-60min @sm. |

---

## References

- Story file: `docs/stories/active/3.6.tags-system.md` v1.0
- Checklist canónico: `.aiox-core/development/tasks/validate-next-story.md`
- Precedent (zero Radix constraint): `docs/stories/completed/3.5.tasks-calendar-view.md` v1.4 + INDEX line 197
- Anti-hallucination source files:
  - `apps/web/package.json` (zero Radix)
  - `apps/web/src/lib/api-helpers/list-tasks.ts:32-50` (TaskRow shape)
  - `apps/web/src/app/(app)/tarefas/_components/EditTaskModal.tsx:1-50` (hand-roll precedent)
  - `packages/db/src/schema/tasks.ts:174-224` (tags + task_tags schema)
  - `packages/db/migrations/0001_rls_policies.sql:403-435` (8 RLS policies)
  - `apps/web/src/app/api/tags/route.ts:1-80` (API existente)

---

**Gate signed: @po (Pax) — 2026-05-18 — Phase 2 SDC validate-story-draft 3.6 v1.0 NO-GO 5.5/10 with 3 CRITICAL FIXES required.**
