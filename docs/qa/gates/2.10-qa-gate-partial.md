# QA Gate — Story 2.10 (Benchmark E2E Anthropic · Partial Gate · T1-T9 + AC11 mockable-only)

**Story:** 2.10 — Benchmark E2E Anthropic: integração real pipeline 3-estágios (200 prompts PT-PT)
**Reviewer:** Quinn (Guardian — @qa)
**Review date:** 2026-05-15
**Story file:** `docs/stories/active/2.10.benchmark-e2e-anthropic.story.md`
**Story version reviewed:** v0.4 (Ready for Review · T1-T9 done · T10 BLOCKED)
**Branch / commit:** `main` @ `050678a` (local, **NOT** pushed — exclusivo @devops)
**Predecessor handoff (consumed):** `docs/handoffs/archive/mj-handoff-2.10-ready-for-qa-20260515.yaml`
**Gate scope:** **PARTIAL** — cobre T1-T9 + AC11 mockable-only + DEV-DECISIONs D60-D65. **NÃO cobre** AC1/AC2 real, AC8 JSON output real, AC4 Eurico sign-off (todos dependem de T10, BLOCKED pending Eurico QA1 DPA UE Anthropic).
**Code/docs changes by QA:** **NONE** — gate-only role. Sem modificação a código (Article QA: review-only authority).

---

## Decisão

**`CONCERNS`** — score **8.7/10**, confidence **HIGH** (sobre o escopo parcial).

Verdict não é PASS apenas porque a story tem 2 sign-offs externos pendentes (Eurico QA1 + @po amostragem 30/200), ambos low-severity, ambos **não bloqueantes para push do parcial mockable-only**. Verdict não é FAIL porque os 11 ACs no escopo (AC1-AC7, AC9-AC13) estão satisfeitos com evidência directa de código + testes corridos. Story prossegue: `InReview` → **`Ready for Review (Partial Done · pending T10 complement gate)`**.

Próximo passo: `@devops *push 2.10 partial` (commit `050678a` local + este gate doc + handoff sucessor) com **prioridade medium**. Gate complementar pós-T10 fica para quando Eurico resolver QA1 e correr `RUN_BENCHMARK_REAL=true` + `RUN_BENCHMARK_E2E_REAL=true` manualmente.

---

## Sumário executivo

Story 2.10 entrega o **harness completo de benchmark mockable-only** que valida (em CI-safe mode) toda a coreógrafia do pipeline cognitivo Cérebro AI implementado em Stories 2.1-2.9: 200 fixtures PT-PT curados, runner classifier com hash SHA256 anti-PII, runner E2E com simulação determinística de `checkQuota → cache → Classifier → cost router → Planner → Executor`, 6 mock tools registadas no `toolRegistry` antes do pipeline, 27 testes Vitest (target ≥12 — superado 2.25×). T10 (execução real contra Anthropic + OpenAI keys) fica **explicitamente BLOCKED** com `runE2EReal` esqueletizada lançando erro descritivo apontando o runbook §8.4.

Os **4 Should-Fix da Pax** foram absorvidos verificavelmente: SF1 (EB1/EB2 → RESOLVED 2026-05-14) — confirmado via leitura `vercel-llm-keys-setup.md`; SF2 (NÃO criar runbook duplicado) — confirmado: nenhum `t10-api-keys-dpa-ue.md` no repo + `vercel-llm-keys-setup.md` tem §8.1-8.5; SF3 (`toolRegistry` populado antes do pipeline) — confirmado: `registerMockBenchmarkTools()` invocada em `run-benchmark-e2e.ts:148` + setup de cada test E2E; SF4 (baseline real ≥526) — confirmado via `pnpm test` reportado pelo @dev em T9 com breakdown package-a-package.

Os **6 DEV-DECISIONs D60-D65** foram cross-verified de ângulo qualidade (vs angle arquitectural do @architect Aria em 2.9): D60 (AC1/AC2 satisfeitos por código Story 2.2) — verificado via grep `MissingApiKeyError` em `packages/agent/src/providers/{anthropic,openai}.ts:67-71` (lança fail-fast quando key ausente e sem `clientOverride`); D61 (helper `fx()` + 200 IDs auto-gerados) — verificado via `grep -c "^  fx(" prompts-pt-pt.ts = 200`; D62 (`runE2EMock` sem Testcontainers) — trade-off aceitável e documentado, validado via test `runE2EReal throws T10 BLOCKED`; D63 (`vitest.benchmark.config.ts` separada) — verificado, permite gate parcial sem Docker (precedente cascata `/jarvis` onde Docker offline bloqueou db-test); D64 (`forceP95Ms` aos últimos 10%) — math correcta: `Math.floor(200*0.9)+1=181`; índice p95 sorted=190 cai dentro da janela 181-200; D65 (3 fixtures ajustados 0.65/0.6/0.65) — verificado via grep, fixtures #38/#164/#175 com confidence <0.70 estritamente.

A **re-execução directa pela @qa** dos benchmark tests (`pnpm --filter @meu-jarvis/db-test test:benchmark`) confirmou **27/27 PASS em 36ms** (test execution time) — números match exactos com claim do @dev em T9. Re-execução do typecheck do package db-test também confirma `tsc --noEmit` clean. `pnpm check:rls` re-executado confirma `✅ Todas as tabelas multi-tenant têm coverage completa` — NFR5 preservada (zero migrations novas nesta story).

**4 NITs non-blocking** identificados pela @qa: (NIT-010-NB) `runE2EMock` documenta trade-off D62 mas não há test específico que assert "Postgres NOT started in mock mode" — seria valor cosmético para Story 2.11+ quando `runE2EReal` for desbloqueada; (NIT-011-NB) `BENCHMARK_FIXTURES.length === 200` sanity check existe mas a aritmética `EXPECTED_DISTRIBUTION` (40+40+20+15+10+30+10+25+10=200) também merecia um test runtime — actualmente é guard-rail apenas em `benchmark-fixtures.test.ts` AC11(ii) com tolerância ±5; (NIT-012-NB) cost de re-rodar `runBenchmarkReal` 200 prompts em Anthropic Sonnet — estimado ~€0.04-0.08 (não bloqueante mas registar para custeio NFR20); (NIT-013-NB) `mock-benchmark-tools.ts` 6 tools têm `reverse: { kind: 'delete_row' }` mas zero teste valida a forma do reverse_op — `runE2EReal` (T10) precisará disto para FR6 undo path.

---

## Quality Checks (7 standard QA checks)

| # | Check | Verdict | Justificação |
|---|-------|---------|--------------|
| **1** | **AC Fulfillment** (escopo parcial) | **PASS** | 11/11 ACs no escopo satisfeitos: AC1/AC2 via D60 trace Story 2.2; AC3 via §8 do runbook existente; AC4 distribuição 40/40/20/15/10/30/10/25/10=200 verificada; AC5 runner mock+real esqueleto; AC6 E2E mock com 6 tools; AC7 cache modo degradado; AC9 FR4 preview via 3 fixtures <0.70 (D65); AC10 quota+cost router integrados no mock; AC11 27 testes vs target ≥12 (superado); AC12 `.env.example` linhas 31-32 confirmadas; AC13 quality gates 5/5 GREEN. Fora-escopo (gate complementar): AC1/AC2 real keys (T10), AC8 JSON real output, AC4 Eurico sign-off. |
| **2** | **Test Coverage** | **PASS** | 27 testes novos (target ≥12 — superado 2.25×): 6 fixtures (AC11 i-iv + sanity) + 12 runner (AC11 i-v + edge cases) + 9 E2E (AC11 i-iii + isSingleConsultarDados + preview + quota + tools). Re-executado pela @qa: 27/27 PASS em 36ms. Mockable-only é defensável para o escopo parcial — runE2EReal cobertura entrará no gate complementar pós-T10. |
| **3** | **Code Quality** | **PASS** | TypeScript strict `noUncheckedIndexedAccess` herdado da raiz — `tsc --noEmit` PASS (re-executado). Zero `any` em código novo (grep confirma). Imports absolutos para `@meu-jarvis/*` (Article VI / NFR19) — apenas `..` ao nível package interno (test→sibling-of-parent — aceitável, não viola NFR19 que proíbe `../../`). Error handling explícito em `runE2EReal` com mensagem PT-PT detalhada apontando runbook §8.4. Naming PT-PT (DN13 herdado): comments + JSDoc em PT-PT verificado. |
| **4** | **Security** | **PASS** | Zero `eval`/`new Function`/`exec`/`child_process` em código novo (grep confirma). `prompt_hash` via `createHash('sha256').update(prompt).digest('hex')` em `run-benchmark.ts:100` — NFR12 PII redaction respeitada (zero cleartext em logs/JSON). Mock tools usam `randomUUID()` nativo (sem prediction). Workspace deps `@meu-jarvis/*` resolvem via pnpm-workspace.yaml — zero dep externa nova excepto `openai ^4.73.0` + `zod ^3.23.8` (ambos aligned com `@meu-jarvis/agent` Story 2.2 — sem version drift). SQL injection N/A (mock E2E não toca DB; runE2EReal usará Drizzle parametrizadas via `executeDirectQuery` da Story 2.9 já APPROVED 9.5/10). |
| **5** | **Performance** | **CONCERN-LIGHT** | Overhead `forceP95Ms` aplicado aos últimos 10% (D64) é trade-off testável aceitável — alternativa de aplicar só ao último fixture falharia captura no índice p95=190 (sorted.length=200). Simulação determinística ~3ms/prompt em `runE2EMock` (D62) é razoável para CI (200 prompts × 3ms = 600ms wall — verificado: test E2E completo em 8ms agregado, indica simulação muito mais rápida que o claim pessimista — sem regressão). **Concern light:** `runE2EReal` real benchmark cost estimado ~€0.04-0.08 (200 prompts × Sonnet pricing — não bloqueia mas Eurico deve estar ciente pré-T10). NIT-012-NB registado. |
| **6** | **Documentation** | **PASS** | Runbook §8 DPA UE completo (5 subsecções 8.1-8.5) — verificado via grep: 8.1 Anthropic state + DPA contratual link, 8.2 OpenAI DPA UE link + `store:false` herdado de `32ac564`, 8.3 Mitigação D56, 8.4 Questão Aberta QA1 com 3 opções A/B/C + tabela decisão, 8.5 Cross-references Story 2.10 + commit. Dev Notes da story coerentes — pipeline 3-estágios redesenhado in-story (Article: self-contained). 6 DEV-DECISIONs D60-D65 documentadas com Razão + Alternativa rejeitada + Confidence + Scope-risk (formato match Story 2.9 D55-D57 APPROVED 9.5/10 pela Aria). Change Log v0.1→v0.4 completo. |
| **7** | **Constitutional Compliance** | **PASS** | **Article I (CLI First):** scripts CLI-invokable via `pnpm --filter @meu-jarvis/db-test benchmark` / `benchmark:e2e` / `test:benchmark` — verificado em package.json. **Article IV (No Invention):** D60 trace explícito a código Story 2.2 (zero código novo para AC1/AC2); todas as outras decisões traceadas a PRD/Architecture/Stories anteriores. **Article V (Quality First):** 5/5 gates GREEN reportados pelo @dev + re-verificados pela @qa em test:benchmark + check:rls + typecheck db-test. **Article VI (Absolute Imports):** verificado — zero `../../` em código novo. NFR5 RLS preservada (28/120 — re-verificado pela @qa). PT-PT exclusivo: vocabulário em comments/JSDoc/notes verificado (`telemóvel`/`frigorífico`/`automóvel`/`utilizar`/`eliminar` — PT-PT canonical). |

**6/7 PASS, 1/7 CONCERN-LIGHT, 0 FAIL.**

---

## DEV-DECISIONs D60-D65 — Ratification Table (angle: qualidade)

| ID | Decisão | Verdict @qa | Justificação @qa |
|----|---------|-------------|-------------------|
| **D60** | AC1/AC2 satisfeitos por código Story 2.2 — sem código novo | **RATIFIED** | Cross-verified via grep `MissingApiKeyError` em `packages/agent/src/providers/anthropic.ts:67-71` + `openai.ts:64-67` — fail-fast quando `apiKey === undefined` e `clientOverride === undefined`. Tests existem em `__tests__/providers/*.test.ts` (Story 2.2 Done APPROVED). Article IV (No Invention) respeitado: duplicar tests seria over-engineering. |
| **D61** | 200 fixtures num único ficheiro com helper `fx()` + ID auto-gerado | **RATIFIED** | Cross-verified: `grep -c "^  fx(" prompts-pt-pt.ts = 200` — exactamente 200 invocações. IDs sequenciais via `.map((f, idx) => ({id: idx+1, ...f}))` — estáveis e auditáveis num único diff. Manutenibilidade: refactor de distribuição visível em um só diff vs 9 ficheiros separados (pattern correcto para fixtures densos). |
| **D62** | `runE2EMock` sem Testcontainers — simulação determinística ~3ms/prompt | **RATIFIED** | Trade-off justificado: AC6 mock-mode valida **coreógrafia de decisões** (cost_router_bypass / preview_block / quota_block / executed) — não persistência real. Mock tools não tocam DB → start de Postgres em mock = overhead zero-benefit (~30s Docker pull). Verificado test `runE2EReal lança erro explícito T10 BLOCKED` — não há falsificação do estado BLOCKED. NIT-010-NB sugere test explícito "Postgres NOT started" para Story 2.11+. |
| **D63** | `vitest.benchmark.config.ts` separada — permite gate sem Docker | **RATIFIED** | Cross-verified: package.json tem `test:benchmark` script invocando config dedicada. Config exclui `globalSetup` (Testcontainers) → benchmark tests correm sem Docker (verificado pela @qa em local sem Docker — 27/27 PASS em 36ms). Pattern não cria fork de manutenção: config principal continua a correr RLS tests em CI via `rls-gate`. Precedente: cascata `/jarvis` Stories 2.7-2.9 todas com `db-test/rls` ENV-BLOCKED Docker offline mas test:benchmark é zero-Docker — fix arquitectural correcto. |
| **D64** | `forceP95Ms` aplica aos últimos 10% para captura no p95 | **RATIFIED** | Math correcta cross-verified: `Math.floor(200 * 0.9) + 1 = 181`. Percentile formula `Math.floor((95/100) * 200) = 190` → `sorted[190]`. IDs 181-200 (20 fixtures) garantem que o índice 190 fica dentro da janela alta latência após sort. Aplicar a 1 fixture só (idx 199) deixaria p95 em baseLatency — bug latente apanhado. Não introduz bias: aplicar 10% reflecte realisticamente "tail latency" do mundo real (não singleton outlier). Alternativa rejeitada de divergir da convenção numpy/scipy seria over-engineering. |
| **D65** | 3 fixtures ajustados 0.7→0.65/0.6 para validar FR4 | **RATIFIED** | Cross-verified via grep: fixtures #38 ('regista tarefa: passar a noite no quarto da minha filha', 0.65), #164 ('anula isso', 0.6), #175 ('Netflix 13,99 por mês e Spotify 6,99 por mês', 0.65) — exactamente 3 com confidence <0.70 estritamente. Não compromete representatividade: 3/200 = 1.5% — alinha com a expectativa MVP de prompts ambíguos low-confidence rare-but-non-zero. Razão técnica sólida: classifier contract `needs_confirmation: minConfidence < THRESHOLD` é strict less-than (`<`) — fixtures em 0.70 exactamente não accionam preview block, falhando o test AC9. Decisão de ajustar fixtures (não relax do contrato) é a correcta. |

**6/6 RATIFIED, 0 NEEDS_ADJUSTMENT, 0 REJECTED.**

(Match com Aria 2.9 que ratificou D55-D57 — patterns consistentes Stories 2.x.)

---

## Should-Fix Verification (4/4)

Cross-confirm anti-hallucination da Pax (4 SFs propostos no PO validation) vs estado real do repo a 2026-05-15:

| SF | Conteúdo | Verdict @qa | Evidência |
|----|----------|-------------|-----------|
| **SF1** | EB1/EB2 marcados RESOLVED 2026-05-14 (não PENDING) | **ABSORBED** | Story 2.10 v0.4 Bloqueadores Externos table — linha EB1 e EB2 ambas com **"RESOLVED 2026-05-14 (Vercel fra1 Production+Preview ...)"**. Validação E2E em produção `/jarvis` 2026-05-15 ~02:15 WET (commit `32ac564` meta-handoff confirma) corrobora EB2 (OpenAI live). EB3 (Upstash) mantém-se PENDING corretamente. |
| **SF2** | AC3/T3 estender `vercel-llm-keys-setup.md` §8 (NÃO criar runbook duplicado) | **ABSORBED** | Grep no repo confirma **ausência** de `docs/runbooks/t10-api-keys-dpa-ue.md`. `vercel-llm-keys-setup.md` tem §8 completa com 5 subsecções (8.1 Anthropic state, 8.2 OpenAI DPA UE, 8.3 Mitigação D56, 8.4 Questão Aberta QA1, 8.5 Cross-references). File List da story tem entry `"❌ docs/runbooks/t10-api-keys-dpa-ue.md — substituído por vercel-llm-keys-setup.md §8 (SF2 Pax)"` em "Não criados (intencional)". |
| **SF3** | AC6/T6 popula `toolRegistry` com 6 mock tools antes do pipeline E2E | **ABSORBED** | `__fixtures__/mock-benchmark-tools.ts` exporta 6 tools (`create_task`, `create_finance_variable`, `create_finance_recurrence`, `create_card`, `create_installment`, `cancel_last_run`) + `MOCK_BENCHMARK_TOOLS` const + `registerMockBenchmarkTools()` helper. `run-benchmark-e2e.ts:148` invoca `registerMockBenchmarkTools()` **antes** do pipeline mock E2E correr. `benchmark-e2e.test.ts:32` chama `registerMockBenchmarkTools()` no `beforeEach` de cada test. Names alinham com `TOOL_TO_INTENT_MAP` da Story 2.5 (Planner schemas). |
| **SF4** | AC13 baseline real `≥526 testes` confirmado pré-implementação | **ABSORBED** | Story v0.4 AC13 texto literal `"≥526 testes distribuídos como web 229 + agent 102 + classifier 79 + planner-executor 53 + tools 57 + db 6 (db-test ENV-BLOCKED Docker offline ...)"`. Re-cross-verified pelo @dev em T9 com numbers exactos. Aritmética: 229+102+79+53+57+6 = 526 — soma correcta. db-test/benchmark 27 novos (target ≥12) — total 553. |

**4/4 SFs ABSORBED.** Sem hallucinations residuais. PO validation final (Pax 8.5/10 GO Conditional) integralmente respeitado.

---

## Risks Identificados + NITs Non-Blocking

### Risks (low-severity, non-blocking para gate parcial)

| ID | Severity | Topic | Mitigação |
|----|----------|-------|-----------|
| **R1** | low | AC4 Eurico sign-off amostral 30/200 fixtures pendente | Não bloqueia gate parcial — fica para gate complementar pós-T10. Pax decidiu fluxo three-step (@dev draft → @po revê 30/200 → Eurico approves antes de @devops push). @po Pax ainda não executou a amostragem 30/200. |
| **R2** | low | T10 BLOCKED pending Eurico QA1 (DPA UE Anthropic) | 3 opções documentadas em runbook §8.4 (A: DPA contratual + risco aceite, B: bloquear MVP até EU endpoint, C: mover Planner para gpt-4o). Sem deadline imposto — Eurico decide quando confortável. |
| **R3** | low | `runE2EReal` esqueletizada — caminho real não testado | Test explícito `runE2EReal lança erro T10 BLOCKED` cobre estado actual. Quando QA1 desbloquear: implementação requer Testcontainers Postgres real + getProvider real + 200 prompts × Sonnet pricing ~€0.04-0.08. Gate complementar valida. |
| **R4** | low | Cache hit rate sempre 0% no benchmark (EB3 Upstash PENDING + modo degradado D57) | Não afecta correctness pipeline — cache é optimização (Story 2.9 AC1 modo degradado garantido). Quando EB3 resolver, benchmark real medirá cache_hit_rate verdadeiro. |

### NITs Non-Blocking (registados para follow-up Story 2.11+ ou Epic 3)

| ID | Severity | Descrição | Follow-up |
|----|----------|-----------|-----------|
| **NIT-010-NB** | LOW | `runE2EMock` documenta D62 trade-off (no Testcontainers) mas não há test explícito "Postgres NOT started in mock mode" | Story 2.11+ quando `runE2EReal` for desbloqueada: adicionar assertion negativa ou guard via env-var check. **Não bloqueia** — cosmético. |
| **NIT-011-NB** | LOW | `BENCHMARK_FIXTURES.length === 200` sanity check é runtime mas `EXPECTED_DISTRIBUTION` aritmética (40+40+...+10=200) é guard-rail apenas em test AC11(ii) com tolerância ±5 | Adicionar test directo `Object.values(EXPECTED_DISTRIBUTION).reduce((a,b)=>a+b,0) === EXPECTED_TOTAL` para evitar drift se distribuição mudar. **Não bloqueia** — drift catched indirectamente pela tolerância ±5. |
| **NIT-012-NB** | LOW | Custo estimado `runBenchmarkReal` 200 prompts Sonnet ~€0.04-0.08 (não documentado explicitamente) | Eurico deve estar ciente pre-T10 — registar nota no runbook §8.4 ou inline em T10 task. **Não bloqueia** — custo é one-time validation. |
| **NIT-013-NB** | LOW | `mock-benchmark-tools.ts` 6 tools têm `reverse: { kind: 'delete_row' }` mas zero teste valida a forma do reverse_op para `runE2EReal` | Quando T10 desbloquear: validar que `Executor.execute()` recebe reverse_op declarativo válido para FR6 undo path. **Não bloqueia** — runE2EMock não invoca reverse path. |

---

## Re-verificação directa pela @qa

Para garantir gate decision baseada em evidência (não em claims do @dev):

| Verificação | Comando executado pela @qa | Resultado |
|-------------|-----------------------------|-----------|
| Benchmark tests | `pnpm --filter @meu-jarvis/db-test test:benchmark` | 27/27 PASS em 36ms (3 test files: fixtures 6, runner 12, e2e 9) |
| Typecheck db-test | `pnpm --filter @meu-jarvis/db-test typecheck` | `tsc --noEmit` clean (0 errors) |
| RLS coverage | `pnpm check:rls` | `✅ Todas as tabelas multi-tenant têm coverage completa (SELECT/INSERT/UPDATE/DELETE)` |
| Fixture count | `grep -c "^  fx(" packages/db-test/src/benchmark/prompts-pt-pt.ts` | 200 (exact match D61) |
| D65 ambiguous fixtures | `grep -nE "0\.65\|0\.6," prompts-pt-pt.ts` | 3 matches em IDs #38/#164/#175 (exact match D65) |
| SF2 runbook | `grep "^## 8\|^### 8\." vercel-llm-keys-setup.md` | 5 subsecções 8.1-8.5 presentes |
| SF3 mock tools | `grep "registerMockBenchmarkTools\|toolRegistry.register"` | Helper invocado em run-benchmark-e2e.ts:148 + test setup |
| Security | `grep -rE "eval\(\|new Function\|exec\(\|child_process"` em `packages/db-test/src/benchmark/` | Zero matches — Article security OK |
| Absolute imports | `grep -rE "from '\.\./\.\./"` em `packages/db-test/src/benchmark/` | Zero matches — Article VI OK |

**Re-execução opcional do typecheck monorepo completo + lint 9/9 + test 526 baseline + build 9/9 skipped pela @qa** — numbers reportados pelo @dev em T9 são internamente consistentes (aritmética 229+102+79+53+57+6=526 verificada; 27 novos = 6+12+9 = 27 verificado), e re-execução não traria nova informação dado o gate parcial. Gate complementar pós-T10 re-executará tudo com keys reais.

---

## Status Final da Story

| Antes | Depois |
|-------|--------|
| `Ready for Review (T1-T9 done · T10 BLOCKED ...)` | **`Ready for Review (Partial Done · pending T10 complement gate)`** |

Nomenclatura "Partial Done" deixa **inequívoco** que T10 falta para fechamento completo. **Closure formal `Done`** acontece após gate complementar pós-T10 desbloqueio.

---

## Cobertura do gate complementar (futuro pós-T10)

Quando Eurico desbloquear T10:

| Critério | Acção @qa no gate complementar |
|----------|-------------------------------|
| AC1 real | Verificar `RUN_BENCHMARK_REAL=true` em local com `ANTHROPIC_API_KEY` real → Planner invoca AnthropicProvider sem mock |
| AC2 real | Idem com `OPENAI_API_KEY` real → Classifier invoca OpenAIProvider sem mock |
| AC8 JSON real | `docs/qa/benchmark-results-{timestamp}.json` commitado com `pass: true`, `accuracy_pct ≥90`, `latency_p95_ms <6000` |
| AC4 Eurico sign-off | @po Pax executa amostragem 30/200 + Eurico approve list final (entry no PR description ou story Change Log) |
| NIT-010/011/012/013 | Avaliar se entram no scope T10 ou ficam para Story 2.11+/Epic 3 |
| `runE2EReal` implementação | Verificar Testcontainers real + 6 mock tools registadas no toolRegistry + reverse_op shape correcto |

---

## Handoff

**Sucessor:** `docs/handoffs/mj-handoff-2.10-ready-for-devops-partial-20260515.yaml` (criado pela @qa em sequência a este gate CONCERNS-light verdict).

**Next agent:** `@devops` — `*push 2.10 partial` (priority **medium**).

**Condições push:**
- ✅ Commit `050678a` local pronto (verificado).
- ✅ Gate doc `docs/qa/gates/2.10-qa-gate-partial.md` criado.
- ✅ Story v0.4 → "Ready for Review (Partial Done · pending T10 complement gate)".
- ✅ Handoff sucessor criado, INDEX actualizado, handoff predecessor arquivado.
- ⏳ Após push: nada bloqueia — Eurico decide quando responder QA1 e correr T10 manualmente.

---

## Change Log

| Data | Versão | Descrição | Autor |
|------|--------|-----------|-------|
| 2026-05-15 | 1.0 | Partial gate **CONCERNS 8.7/10 HIGH confidence**. 11/11 ACs no escopo PASS. 6/6 DEV-DECISIONs D60-D65 RATIFIED (cross-verified D60 via grep MissingApiKeyError, D61 via 200 fx invocations, D63 via package.json scripts, D64 via math 181/190, D65 via grep 0.65/0.6 fixtures). 4/4 SFs Pax ABSORBED (SF1 EB1/EB2 RESOLVED, SF2 §8 não duplicado, SF3 registerMockBenchmarkTools antes pipeline, SF4 baseline 526 confirmado). 6/7 quality checks PASS + 1/7 CONCERN-LIGHT (performance — NIT-012 custo benchmark real). 4 NITs non-blocking registados (NIT-010 Postgres NOT started test / NIT-011 distribution aritmética / NIT-012 custo benchmark / NIT-013 reverse_op shape test). Re-verificação directa @qa: 27/27 test:benchmark PASS em 36ms + typecheck db-test clean + check:rls PRESERVADA. Status story v0.4 `Ready for Review` → `Ready for Review (Partial Done · pending T10 complement gate)`. T10 mantém-se BLOCKED pending Eurico QA1. Sem DOC-FIX. Sem código modificado pela @qa (review-only authority). | @qa (Quinn) |
