# Story 5.10 — Architect Gate (adversarial, independente)

- **Story:** 5.10 — Responsive sweep + branding auth + Lighthouse mobile ≥ 85 (story de fecho do Epic 5, 10/10)
- **Gate por:** Aria (@architect) — gate adversarial independente (verificação byte-a-byte + gates re-corridos + prova empírica das falhas pré-existentes)
- **Data:** 15/06/2026
- **Input:** Ready for Review v1.4-DEV (@dev Dex, 15/06/2026), 5/5 gates GREEN reportados, net +4 testes
- **Git baseline:** `98577c0` (origin/main, HEAD); nada commitado/pushed
- **Gate `@architect` justificado:** a landing+redirect raiz (AC6) toca routing/auth no RSC raiz (`app/page.tsx`) e a decisão de performance (DP-5.10.A) + a optimização RSC com `React.cache` sobre o caminho RLS (AC5) têm implicações de segurança/arquitectura.

---

## VEREDICTO: **PASS** — confiança **9,6/10** (Alta)

Story → **Done v1.5-ARCH-APPROVED**. Pronta para `@devops *push`.

A 5.10 fecha o Epic 5 com quatro eixos limpos: (1) **landing pública real na raiz** com session-check-antes-de-render (zero leak sessão↔landing, verificado no código e nos testes); (2) **optimização da double-query** da Visão via `React.cache` com a chave correcta `(userId, householdId)` encapsulando `withHousehold` — dedup **genuína** e RLS **preservada** (role `authenticated`, per-request, NUNCA `getServiceDb`); (3) **runbook Lighthouse** novo com medições reais (mobile `/`=99, `/entrar`=96) e AC ≥ 85 data-driven coerente com o baseline; (4) **sweep responsivo** das 15 telas com 2 fixes inline triviais (só Tailwind). As 5 falhas em `finance-flow.test.ts` foram **provadas empiricamente** como pré-existentes (Story 4.10) — NÃO regressão da 5.10. Os 5 gates foram **re-corridos por mim** e todos passam.

---

## Resultado dos 5 focos de verificação independente

| # | Foco | Resultado | Prova |
|---|------|-----------|-------|
| 1 | **AC6 — landing raiz `/` (sem leak)** | **MET** | `app/page.tsx` é RSC `async` com `getUser()` ANTES de qualquer render; `if (user) redirect('/visao')` precede o `return` da landing (linha 48 vs 50) → autenticado **nunca** vê a landing. Não-autenticado → wordmark "Expressia" (`font-serif`), claim PT-PT (tarefas/finanças/rotinas da família), CTA "Experimenta grátis"→`/registar` + "Entrar"→`/entrar` (`<Link>`). `export const dynamic = 'force-dynamic'`. Ambos os caminhos cobertos por testes (`page.test.tsx`: não-auth assert wordmark+claim+2 hrefs e `redirect` NÃO chamado; auth → `redirect('/visao')` 1× e landing não-renderizada via `NEXT_REDIRECT` throw). Touch targets `min-h-11` (=44px), `focus-visible:ring`. Copy PT-PT (sem PT-BR). |
| 2 | **AC5 — React.cache dedup genuína + RLS preservado** | **MET** | A análise do @dev está **correcta e provada**: `React.cache` sobre `getX(db,…)` NÃO deduplicaria (cada `withHousehold` abre `tx` distinto; `cache` deduplica por identidade de argumentos). Solução real: 6 wrappers `getXCached(userId, householdId) = cache((u,h) => withHousehold({userId:u,householdId:h}, tx => getX(tx,h)))`. **Chave estável** — `(userId, householdId)` são strings idênticas propagadas de `page.tsx` para `isVisaoEmpty` E para `WidgetGrid`→cada widget (mesma instância `React.cache` por request). **RLS preservado (a verificação mais importante):** `withHousehold` (db/client.ts:110) faz `set local role authenticated` + `set_config('request.jwt.claims',…)` numa transacção → RLS viva per-request; **zero `getServiceDb`** no caminho cacheado (grep: só comentários :14/:407 a documentar a ausência). `React.cache` é per-request no RSC (sem cross-request/cross-tenant leak). Funções base `getX` **inalteradas** — os 7 route handlers `/api/visao/*` mantêm contrato HTTP 1:1 (não tocados no diff). 1.ª rede app-enforced (`household_id` explícito em cada `getX`) intacta. |
| 3 | **5 falhas pré-existentes — PROVA EMPÍRICA** | **CONFIRMADO** | **Não-regressão provada por `git stash`.** Contra HEAD limpo (sem alterações 5.10): `finance-flow.test.ts` = **5 failed / 11 passed (16)** nas linhas 254, 314, 362, 406, 596. Com as alterações 5.10 (suite completa): **as MESMAS 5 falhas / mesmas linhas**. São Story 4.10 (`executeAtomic` finance tools via mock-DB; `expected false to be true`), fora do scope da 5.10 (frontend/landing/perf/runbook). **NÃO podem ser regressão** — pré-datam a story. `git stash pop` restaurou o working tree intacto. |
| 4 | **5 gates re-corridos independentemente** | **MET** | Ver tabela abaixo. Todos PASS. `/` no manifest como `ƒ /` (Dynamic) — confirma RSC dinâmico, não pré-render estático. `check:rls` EXIT 0, **28 tabelas INALTERADO**. |
| 5 | **AC3/AC4 — Lighthouse/runbook** | **MET** | `docs/runbooks/lighthouse-mobile.md` existe com as 6 secções (procedimento, métricas baseline, Fase 2 N/A, auditoria 15 telas, breakpoints, notas). AC numérico ≥ 85 é **coerente com o baseline documentado** (`/`=99, `/entrar`=96 — folga confortável → caminho A da DP-5.10.A=B). A limitação de `/visao` autenticada (não mensurável por CLI sem login — redirect→`/entrar`) está **documentada com transparência** (§1.3 + §2). Não penalizada — é honesta e a alternativa (E2E Playwright) está fora de scope por DP-5.10.C. |

**Verificações adversariais além do catálogo do @dev:**
- **`withHousehold` real verificado** (db/client.ts:110-129): `set local role authenticated` + `select set_config('request.jwt.claims', $claims, true)` PARAMETRIZADO com `{sub, household_id, role:authenticated}` — RLS genuinamente activa. A chave de cache `(userId, householdId)` mapeia 1:1 para os claims, sem possibilidade de servir dados de outro tenant.
- **Grep negativo de cores hardcoded** no diff completo (landing + queries + 2 fixes de nav): **ZERO** (`bg-white|bg-black|text-black|bg-neutral-N|ring-blue|text-white|#hex`). Confirma AC7.c.
- **2 fixes do sweep** (`MonthNavigation`/`WeekNavigation`: spacer `w-[150px]`/`w-[140px]` → `hidden sm:block`) são `aria-hidden` puramente decorativos — só utilitários Tailwind, sem impacto funcional nem de acessibilidade (ordem de leitura DOM intacta).
- **Caminho da Visão não regrediu**: `page.tsx` da Visão preserva o gate de onboarding + `resolveHouseholdId` + os 3 redirects de segurança (SEC-1) — a migração para wrappers cached é transparente para esse fluxo.

---

## Gates re-executados independentemente (não confiando nos números do @dev)

| Gate | Resultado | Exit |
|------|-----------|------|
| `pnpm lint` | **No ESLint warnings or errors** (`--max-warnings=0`) | 0 |
| `pnpm typecheck` | **10/10** (sem `any`) | 0 |
| `pnpm check:rls` | **28 tabelas** — coverage completa SELECT/INSERT/UPDATE/DELETE (INALTERADO) | 0 |
| `pnpm --filter @meu-jarvis/web test` | **1163 passed / 5 failed (1168 total)** — as 5 falhas = `finance-flow.test.ts` pré-existentes (idênticas a HEAD limpo) | 1* |
| `pnpm build` | **Compiled successfully** (46 páginas); landing `/` no manifest = `ƒ /` (Dynamic, 346 B / 209 kB) | 0 |
| `finance-flow.test.ts` @ HEAD limpo (`git stash`) | **5 failed / 11 passed (16)** — prova de não-regressão | 1* |

\* O exit 1 da suite web deve-se **exclusivamente** às 5 falhas pré-existentes de `finance-flow.test.ts` (Story 4.10), provadas idênticas contra HEAD limpo. **Zero regressões introduzidas pela 5.10.** Net da story = **+4 testes** (ficheiro novo `app/__tests__/page.test.tsx`, landing AC6/AC7) — excede o target AC8 ≥ +3.

---

## ACs MET / NOT-MET

| AC | Estado | Nota |
|----|--------|------|
| AC1 — auditoria responsiva 15 telas | **MET** | runbook §4: tabela 15 telas × 2 breakpoints; 2 FIX inline; 0 quebras não-documentadas; bottomTabBar SCOPE-OUT Epic 6 documentado. |
| AC2 — REMOVIDO (branding auth → Story 6.1) | **N/A** | marcador conservado; verificado byte-a-byte na 6.1 (Done, em prod, agora em `completed/`). |
| AC3 — Lighthouse baseline data-driven | **MET** | `/`=99, `/entrar`=96, A11y 95, FCP 0,8s; AC ≥ 85 adoptado (caminho A). |
| AC4 — runbook `lighthouse-mobile.md` | **MET** | 6 secções; formato consistente com runbooks existentes. |
| AC5 — `isVisaoEmpty` optimizado React.cache | **MET** | dedup genuína por `(userId,householdId)`; RLS preservado; route handlers 1:1. |
| AC6 — landing pública + redirect autenticado | **MET** | session-check-antes-de-render; 2 CTAs; sem leak; ambos os caminhos testados. |
| AC7 — A11y + PT-PT + dark mode sem leak | **MET** | grep negativo cores=0; touch 44px; foco visível; copy PT-PT. |
| AC8 — 5 gates GREEN + cobertura | **MET** | 5/5 gates re-corridos PASS; net +4; check:rls 28 tabelas; REQ-INLINE-1 OK. |

**8/8 ACs MET** (AC2 N/A por remoção justificada e verificada).

## DEV-DECISIONs ratificadas

- **D-5.10.1** (AC Lighthouse ≥ 85, baseline ≥ 85 → caminho A, sem Fase 2) — **RATIFICADA.** Coerente com DP-5.10.A=B e os scores reais medidos.
- **D-5.10.2** (AC5 via wrappers cacheados por `(userId,householdId)` encapsulando `withHousehold`, não `React.cache` cosmético) — **RATIFICADA.** É a leitura correcta de DP-5.10.D=A (dedup real per-request); segurança RLS verificada (role authenticated, per-request, 1.ª rede mantida).
- **D-5.10.3** (5 falhas `finance-flow.test.ts` não corrigidas, fora de scope) — **RATIFICADA.** Provadas pré-existentes; follow-up de QA dedicado recomendado.
- **AUTO-DECISION** (`font-serif` no wordmark vs registar Lora) — **RATIFICADA.** Não inventar fonte nova (Article IV); coerente com `(auth)/layout.tsx`/`OnboardingTour`/`visao`.

---

## Observações não-bloqueantes (housekeeping)

1. **OBS-1 — Discrepância benigna de contagem absoluta de testes.** O @dev reportou suite web = 1166 total; eu medi **1168 total** (1163 pass / 5 fail). A diferença (2 testes) **não afeta o veredicto**: o net da story é **+4** (ficheiro novo da landing, confirmado), zero regressões, e as 5 falhas são idênticas e pré-existentes em ambas as medições. A variação no baseline absoluto é cosmética (provavelmente momento de medição/colecção ligeiramente distinto) — não há nenhum teste em falta nem regressão escondida.
2. **OBS-2 — Follow-up de QA recomendado** para as 5 falhas de `finance-flow.test.ts` (Story 4.10, interacção mock-DB↔`executeAtomic` em apps/web). O package `@meu-jarvis/tools` passa isolado; a falha é da fixture mock no apps/web. Não bloqueia a 5.10 (caminho independente), mas é dívida técnica de teste que deve ser endereçada.
3. **OBS-3 — `/visao` autenticada não mensurada por Lighthouse CLI.** Documentado com transparência no runbook §1.3/§2. Para uma medição real da Visão com 7 widgets activos, usar DevTools com sessão (follow-up condicional, não trabalho desta story). Aceite como limitação honesta.

---

## Conclusão

A Story 5.10 está **arquitectonicamente sólida e segura**. A peça nova de routing/auth (landing raiz) não introduz leak; a optimização RSC (React.cache) está bem desenhada — a dedup é genuína e a RLS é preservada por construção. Os 5 gates passam de forma independente, as falhas pré-existentes estão provadas como não-regressão, e o Epic 5 fecha (10/10) sem dívida arquitectónica nova.

**PASS · 9,6/10 · Alta · Done v1.5-ARCH-APPROVED · pronta para `@devops *push`.**

— Aria, arquitetando o futuro 🏗️
