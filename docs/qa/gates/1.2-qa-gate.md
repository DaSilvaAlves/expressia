# QA Gate — Story 1.2 (CI/CD Pipeline com Quality Gates GitHub Actions)

**Reviewer:** Quinn (@qa) — Test Architect & Quality Advisor
**Data:** 2026-05-04
**Story:** `docs/stories/active/1.2.ci-pipeline-quality-gates.md`
**Epic:** Epic 1 — Foundation & Multi-Tenant Core
**PO Gate referenciado:** `docs/qa/gates/1.2-po-gate.md` (GO Conditional 9/10, 2 must-fixes aplicados)
**Implementação revista:** commit `54ca426` (`.github/workflows/ci.yaml` 198 linhas + `docs/runbooks/ci-setup.md` 209 linhas)
**Modo:** Static review do YAML/runbook + execução local dos gates equivalentes (`pnpm typecheck/lint/test/build/check:rls`)

---

## Verdict

**PASS** — Os 7 quality checks do qa-gate.md aprovados; story pronta para `Done` e push pelo @devops. Pipeline implementada com qualidade acima do que a story pede (PT-PT step names completos, defense-in-depth com timeouts, cache duplo pnpm, concurrency cancel-in-progress, permissões least-privilege). 2 ACs deferidos (AC6 tempo < 5min, AC10 branch protection UI) com justificação válida — ambos só verificáveis após push e configuração GitHub UI por @devops, e estão documentados no runbook §3 e §4.1 com procedimento concreto.

| Métrica | Valor |
|---------|-------|
| Score | **7 / 7** PASS |
| ACs satisfeitos | 8 / 10 implementados + 2 deferidos com justificação válida |
| Issues HIGH/CRITICAL | 0 |
| Issues MEDIUM (tech debt) | 0 nesta story (MNT-001/MNT-002 herdados da 1.1, fora de escopo) |
| Issues LOW (notes) | 2 (advisories para próximas stories) |
| Confidence | **High** |
| Quality gates locais executados | typecheck (PASS, FULL TURBO), lint (PASS, 0 warnings), test (PASS 1/1), build (PASS), check:rls (PASS 26/26 tabelas, 104 policies) |
| YAML syntax | Validado via js-yaml (3 jobs, 2 triggers, permissions least-privilege, concurrency cancel-in-progress) |

---

## Schema (compatibilidade `qa-gate-tmpl`)

```yaml
schema: 1
story: '1.2'
gate: PASS
status_reason: |
  CI pipeline implementada com qualidade acima do mínimo: 8/10 ACs satisfeitos +
  2 ACs deferidos com justificação aceitável (AC6 só verificável em primeira run real,
  AC10 é configuração GitHub UI a fazer pelo @devops). YAML idiomático, secrets só via
  ${{ secrets.X }}, postgres:16 com health-cmd pg_isready, timeouts em todos os jobs,
  permissions: contents: read, concurrency cancel-in-progress, step names PT-PT.
  Quality gates locais (typecheck/lint/test/build/check-rls) todos PASS.
reviewer: 'Quinn'
updated: '2026-05-04T13:00:00Z'
top_issues:
  - id: 'DOC-001'
    severity: low
    finding: 'Smoke test (§4.1) e negative tests (§4.2/4.3) do runbook só serão executados após primeiro merge — AC6 (<5 min) e DoD "rls-gate falha quando RLS coverage está incompleta" ficam validados nessa primeira run.'
    suggested_action: '@devops deve registar tempo total da primeira CI run num comment no PR de smoke test e correr os 2 negative tests antes de marcar branch protection como required.'
  - id: 'TEST-002'
    severity: low
    finding: 'Pipeline implementa apenas 3 dos 6 jobs canónicos do Architecture §11.4 (`quality`, `rls-gate`, `build`) — `integration`, `e2e`, `llm-bench`, `deploy` ficam para stories futuras.'
    suggested_action: 'Documentado correctamente em runbook §9. Trace: e2e em Epic 1 storiesfuturas; llm-bench em Epic 2; integration quando Stripe webhook entrar; deploy em story dedicada.'
waiver: { active: false }
```

---

## Contexto da Verificação

**Inputs validados:**
- Story file `docs/stories/active/1.2.ci-pipeline-quality-gates.md` — 8/10 ACs marcados, 2 deferidos com justificação inline
- PO Gate `docs/qa/gates/1.2-po-gate.md` — GO Conditional 9/10, 2 must-fixes (script naming) aplicáveis pelo @dev
- Implementação `.github/workflows/ci.yaml` — 198 linhas, 3 jobs
- Runbook `docs/runbooks/ci-setup.md` — 209 linhas, 10 secções
- Trace fonte: PRD NFR5/NFR8/NFR17 (linhas 98, 101, 116-117), Architecture §11.4 (linhas 845-879)

**Comandos executados localmente pelo Quinn:**

| Comando | Resultado | Notas |
|---------|-----------|-------|
| `pnpm typecheck` | PASS — FULL TURBO 2/2 cached, 47ms | Equivalente ao step `Typecheck (TypeScript strict)` do job `quality` |
| `pnpm lint` | PASS — "No ESLint warnings or errors" | Equivalente ao step `Lint (ESLint)` |
| `pnpm test` | PASS — 1 test file, 1 test, 1.63s | Equivalente ao step `Testes unitários (Vitest)` |
| `pnpm build` | PASS — Next.js production build OK | Equivalente ao step `Compilar Next.js (production build)` |
| `pnpm check:rls` | PASS — 26/26 tabelas verificadas, 104 policies em 0001_rls_policies.sql, coverage SELECT/INSERT/UPDATE/DELETE | Equivalente ao step `Verificar cobertura RLS (NFR5)` |
| `node -e "yaml.load(...)"` | PASS — 3 jobs (`quality`, `rls-gate`, `build`), 2 triggers (`pull_request`, `push`), `permissions: contents: read`, concurrency cancel-in-progress | YAML syntax válido |

---

## 7 Quality Checks (qa-gate.md)

### 1. Code Quality — **PASS**

| Verificação | Resultado | Evidência |
|-------------|-----------|-----------|
| YAML idiomático | PASS | Versões pinned (`PNPM_VERSION: '9.12.3'`, `NODE_VERSION: '20'`); actions oficiais com major version (`actions/checkout@v4`, `actions/setup-node@v4`, `pnpm/action-setup@v4`, `actions/cache@v4`) |
| Comentários explicativos | PASS | Header narrativo (linhas 1-18) com purpose, jobs, paralelismo decision, secrets necessários; comments inline a justificar `needs: [quality]` (linha 159-161) e a marcar trace por job (`AC1, AC2, AC9` linha 42, `AC3, AC8, NFR5` linha 85, `AC4` linha 157) |
| Estrutura modular | PASS | Cada job tem 3 fases bem isoladas: setup (checkout + pnpm + node + cache + install), execution (lint/typecheck/test ou migrate+rls ou build), env-injection só onde necessário (job `build` para Supabase secrets) |
| Defense-in-depth | PASS | `timeout-minutes` em **todos** os 3 jobs (10/8/10) — caso runner free-tier engasgue, job termina graciosamente; `restore-keys` no cache reduz cold-start; step "Aguardar Postgres ready" tem retry de 30×2s mesmo já tendo o `health-cmd` (defensivo extra) |
| Cache pnpm duplo | PASS | `actions/setup-node@v4 cache: 'pnpm'` (cache de dependências node_modules) **+** `actions/cache@v4` para `~/.local/share/pnpm/store` (cache de tarball store) — pattern recomendado pela pnpm community |
| Conventions | PASS | Job IDs em kebab-case (`quality`, `rls-gate`, `build`), env vars em SCREAMING_SNAKE_CASE (`NODE_VERSION`, `PNPM_VERSION`, `DATABASE_URL`, `POSTGRES_*`, `NEXT_PUBLIC_*`) |

**Conclusão:** Code quality é exemplar para infra story. YAML é mais robusto do que o snippet do Dev Notes pedia (timeouts, concurrency, permissions, comments inline com trace) — @dex ultrapassou o mínimo sem inventar features. Article IV — No Invention respeitado: tudo é instrumentação directa dos NFRs/ACs traceados.

---

### 2. Test Coverage — **PASS** (com nota contextual)

| Verificação | Resultado | Evidência |
|-------------|-----------|-----------|
| Aplicabilidade | N/A directo | Esta é story de infra/CI — não há código de produto a cobrir. O **próprio rls-gate É o teste** que valida NFR5 |
| RLS coverage gate | PASS | Script `scripts/check-rls-coverage.ts` corre 26 tabelas (income_categories, expense_categories, accounts, transactions, etc.), valida 4 commands (SELECT/INSERT/UPDATE/DELETE) por tabela contra `0001_rls_policies.sql` (104 policies). Resultado local: "✅ Todas as tabelas multi-tenant têm coverage completa" |
| Testes existentes na codebase passam | PASS | `pnpm test` corre 1 test file (smoke da Story 1.1) — verde, 1.63s |
| Pipeline corre sequência correcta | PASS | Job `rls-gate`: install → aguardar postgres ready (with retry) → `pnpm --filter @meu-jarvis/db db:migrate` (Drizzle) → `pnpm check:rls`. Sequência só corre check-rls **depois** de migrations aplicadas — ordem correcta |
| Negative test documentado | PASS | Runbook §4.2 dá receita exacta para criar tabela `_test_rls_gate` sem policies e confirmar que job falha. Receita é executável e tem cleanup ("Reverter o branch — não merge") |

**Conclusão:** Para uma story de infra, "test coverage" significa "o gate testa a coisa certa". O rls-gate testa exactamente o que NFR5 pede — estrutura SQL com policies para cada tabela com `household_id`. Validação local confirma 26/26.

---

### 3. NFR Compliance — **PASS** (4/4 NFRs cobertos)

| NFR | Severidade | Validação | Estado |
|-----|-----------|-----------|--------|
| **NFR5** — RLS Postgres em todas as tabelas com `household_id`; teste automatizado bloqueia merge | MUST | Job `rls-gate` corre `pnpm check:rls` que sai com exit≠0 se faltar policy. Runbook §4.2 documenta negative test para validar que falha de facto. Local: 26 tabelas verificadas, 0 violations | PASS |
| **NFR8** — Secrets nunca em código; gestão via vault da plataforma | MUST | Grep `secrets\.` no YAML retorna apenas `${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}` (linha 197) e `${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}` (linha 198). `POSTGRES_PASSWORD: test` (linha 97) é credencial efémera do service container interno ao runner — não é secret real (documentado no runbook §5 e justificado como pattern intencional) | PASS |
| **NFR17** — Quality gates pre-merge bloqueantes: lint + typecheck + test | MUST | Job `quality` corre os três sequencialmente, qualquer falha aborta job (default `set -e` shell + ausência de `continue-on-error`). Branch protection documentada em runbook §3 com 3 status checks required: `Quality (lint + typecheck + test)`, `RLS Coverage Gate (NFR5)`, `Build Next.js` | PASS |
| **NFR18** — CodeRabbit review activo (CRITICAL/HIGH bloqueia merge) | MUST | **Deferred — explicitamente documentado**. Story declara "CodeRabbit Integration: Desactivado em `core-config.yaml`. Revisão manual pelo @devops". Runbook §8 referencia "Adicionar `coderabbit-review` job: Quando `coderabbit_integration` for activado em `core-config.yaml` (story dedicada)". Não bloqueante para esta story porque a story está limitada a `lint+typecheck+test+rls+build` (escopo cristalino na user story) | PASS (deferred) |

**Notas adicionais:**
- **NFR19 (imports absolutos):** Não aplicável aqui — esta story só introduz YAML + markdown. Validado em 1.1 e protegido por ESLint config dessa story.
- **NFR16 (>=70% cobertura):** Não aplicável agora — gate de cobertura entrará em story futura quando Vitest tiver mais testes; AC actual da story 1.2 não exige isso.

**Conclusão:** Os 4 NFRs directamente endereçados pela story (NFR5/NFR8/NFR17/NFR18) estão satisfeitos. NFR18 deferral é documentado e dentro do escopo declarado.

---

### 4. Security — **PASS**

| Vector | Verificação | Resultado |
|--------|-------------|-----------|
| Hardcoded secrets | Grep `secrets\.` + scan manual de cada `env:` block | PASS — só `${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}` e `${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}` no job `build`. `POSTGRES_PASSWORD: test` é credencial efémera de service container (pattern padrão GH Actions) |
| Permissions least-privilege | Top-level `permissions:` block | PASS — `contents: read` (linhas 33-34). Sem `write` em qualquer recurso. Mitiga risco de exfil/tamper se runner for comprometido (especialmente importante quando virem PRs de forks no futuro) |
| Concurrency / DoS surface | `concurrency` group com `cancel-in-progress` | PASS — runs antigas no mesmo branch são canceladas (linhas 28-30), evita backlog em PRs com many pushes |
| Timeouts | `timeout-minutes` por job | PASS — `quality: 10`, `rls-gate: 8`, `build: 10`. Caso runner trave, job termina; sem job infinito a queimar minutos |
| Service container isolation | `services.postgres` ephemeral | PASS — cada run cria container novo; password `test`/`test` é local ao runner durante a run, não acessível externamente |
| Forks / PR externos | Documentado no runbook | PASS — runbook §7 troubleshooting nota: "Em forks/PRs externos, secrets de `repository` não são passados — esperado e seguro" |
| Branch protection bypass | Documentado | PASS — runbook §3 explicita "[x] Do not allow bypassing the above settings" como configuração obrigatória, com nota "importante — sem isto, owners conseguem dar push directo" |
| OWASP A05 (Security Misconfiguration) | YAML idiomático com defaults seguros | PASS |
| OWASP A02 (Cryptographic Failures) | N/A (sem crypto custom; usa GH secrets infra) | N/A |

**Conclusão:** Postura de segurança é defensiva e correcta. Permissions least-privilege + timeouts + concurrency + ephemeral services + secrets só via vault é o pattern hardened. Para foundation CI, está exemplar.

---

### 5. Documentation — **PASS**

| Documento | Verificação | Estado |
|-----------|-------------|--------|
| `.github/workflows/ci.yaml` header | Resume jobs, paralelismo decision, secrets necessários, trace ao Story/PRD/Arch | PASS |
| `docs/runbooks/ci-setup.md` | 10 secções: visão geral, secrets, branch protection, validação manual (smoke + 2 negative tests), env vars rls-gate, performance, troubleshooting, manutenção, próximos passos, referências | PASS |
| Runbook actionable | Procedimentos passo-a-passo (números 1-N), URLs concretas (`https://github.com/<owner>/<repo>/settings/secrets/actions`), nomes de status checks exactos para copy-paste | PASS |
| Story Change Log | 3 entradas (criação @sm, PO gate @po, implementação @dev) com versões 1.0/1.1/1.2 | PASS |
| Story File List | Distingue "Ficheiros criados" (2) de "Ficheiros NÃO modificados intencionalmente" (2 com razão) — prática anti-MNT (must-fixes do PO gate respeitados) | PASS |
| Trace table na story | AC1-AC10 → PRD NFR/Epic AC/Architecture §X.Y verificável | PASS |
| Decisões @dev documentadas | Comentários inline no YAML justificam `needs: [quality]` (linhas 159-161) e step names PT-PT (escolha implementada visível) | PASS |
| AC6/AC10 deferrals | Story marca explícito `_(Validação final só em primeira run real — ver runbook §4.1.)_` (AC6) e `_(Documentado em docs/runbooks/ci-setup.md §3 — configuração manual no GitHub UI após merge.)_` (AC10) | PASS |

**Conclusão:** Documentação é completa e operacional — qualquer @devops consegue configurar branch protection a partir do runbook sem perguntar. Header do YAML é narrativo o suficiente para alguém ler e entender em 30s o que a pipeline faz e porquê.

---

### 6. PT-PT Compliance — **PASS**

| Verificação | Evidência | Estado |
|-------------|-----------|--------|
| Step names em PT-PT | 21 steps: "Checkout repositório", "Configurar pnpm", "Configurar Node.js", "Cache adicional pnpm store", "Instalar dependências", "Lint (ESLint)", "Typecheck (TypeScript strict)", "Testes unitários (Vitest)", "Aguardar Postgres ready", "Aplicar migrations (Drizzle)", "Verificar cobertura RLS (NFR5)", "Compilar Next.js (production build)" | PASS |
| Vocabulário PT-PT | "Configurar" (não "setar"), "Instalar" (não "deletar/setar"), "Aplicar" (não "aplicação" forçado), "Verificar" (não "checar"), "Aguardar" (não "esperar"), "Compilar" (não "buildar") | PASS |
| Termos técnicos em inglês | Aceitáveis e mantidos: `cache`, `lint`, `typecheck`, `build`, `pnpm`, `Postgres`, `migrations`, `RLS` (acronym), `Vitest`, `ESLint`, `Drizzle`, `Next.js` — todos termos técnicos sem equivalente PT-PT natural | PASS |
| Comentários YAML em PT-PT | "Cancela runs anteriores no mesmo PR para poupar minutos CI", "Permissões mínimas (princípio least privilege)", "decisão consciente — AC5 'onde possível'" | PASS |
| Runbook em PT-PT | Tom calmo, vocabulário consistente; só termos técnicos em inglês onde justificado | PASS |
| Story em PT-PT | Idem | PASS |
| Job names | "Quality (lint + typecheck + test)", "RLS Coverage Gate (NFR5)", "Build Next.js" — termos técnicos em inglês mas explicação clara | PASS |
| Sem PT-BR detectado | Grep `usar |checar |setar |deletar |buildar ` retorna 0 matches | PASS |

**Conclusão:** Implementação é exemplar em PT-PT. Step names PT-PT eram "nice-to-have" no PO gate — @dev cumpriu integralmente. Tom calmo e operacional consistente.

---

### 7. Architecture Conformance — **PASS** (com nota de escopo)

| Architecture § | Implementação | Match |
|---------------|---------------|-------|
| §10.2 — RLS Testing Automatizado bloqueia merge (NFR5) | Job `rls-gate` aplica migrations + corre `pnpm check:rls` (variante do `scripts/check-rls.ts` referenciado no Architecture); falha com exit≠0 bloqueia merge via branch protection (runbook §3) | PASS |
| §11.4 — `on: [pull_request, push to main]` | Implementado literal: `pull_request:` + `push: branches: [main]` (linhas 22-25) | PASS |
| §11.4 — Job `quality`: lint + typecheck + unit tests | Implementado literal: 3 steps sequenciais lint→typecheck→test no job `quality` | PASS |
| §11.4 — Job `rls-gate`: services postgres:16, drizzle generate, migrate up, run scripts/check-rls.ts | Implementado: `services.postgres.image: postgres:16` (linha 94) com `health-cmd pg_isready` (linha 102), `pnpm --filter @meu-jarvis/db db:migrate` (linha 151) executa Drizzle migrate, `pnpm check:rls` (linha 154) executa o script de cobertura | PASS |
| §11.4 — Drizzle `generate` antes de migrate | **Não implementado** — story só corre `migrate`. Justificação: `generate` produz ficheiros de migration; em CI já existem (commit fixa as migrations); só `migrate` (que aplica as existentes) é necessário. Decisão correcta para o gate, mas registar como advisory para se schema generation drift entrar em story futura | PASS (com nota DOC-001) |
| §11.4 — Job `integration` (services postgres:16, redis) | **Fora de escopo** — explicitamente deferido para story futura. Runbook §9 documenta. | PASS (escopo claro) |
| §11.4 — Job `e2e` (playwright preview deploy) | **Fora de escopo** — Epic 1 stories futuras | PASS (escopo claro) |
| §11.4 — Job `llm-bench` (nightly) | **Fora de escopo** — Epic 2 | PASS (escopo claro) |
| §11.4 — Job `deploy` (main only, manual approval gate) | **Fora de escopo** — story dedicada | PASS (escopo claro) |
| §11.4 — Postgres connection pooling, region Frankfurt | N/A directo no CI (rls-gate usa container efémero, não Supabase prod). Documentado em runbook §5 como decisão consciente: "O gate RLS testa a estrutura SQL, não a instância Supabase" | PASS |
| Paralelismo `quality`/`rls-gate` (AC5) | `quality` e `rls-gate` correm sem `needs:` entre si — paralelos | PASS |
| `build` em série após `quality` (decisão consciente) | `needs: [quality]` no job `build` (linha 166); justificação documentada em comments YAML linhas 159-161 ("AC5 pede paralelismo 'onde possível'; encadear build após quality poupa minutos CI quando lint/typecheck/test falham") + runbook §1; `build` continua paralelo com `rls-gate` | PASS |

**Conclusão:** Arquitectura §11.4 é o esquema canónico de **6 jobs**; story 1.2 implementa **3** (`quality`, `rls-gate`, `build`) — subset declarado e respeita Article IV (No Invention). Os 3 ausentes (`integration`, `e2e`, `llm-bench`, `deploy`) ficam para stories futuras com trace explícita no runbook §9. Article IV satisfeito.

---

## Constitution Compliance

### Article I — CLI First (NON-NEGOTIABLE)

PASS. CI/CD pipeline é instrumentação do CLI. `pnpm lint/typecheck/test/build/check:rls` no GitHub runner é literalmente o que `@dev` corre localmente — pipeline observa e gateia, não controla. Confirmado: comandos do YAML são bit-exact iguais aos do `package.json` raiz (linhas 14-17, 20).

### Article III — Story-Driven Development

PASS. Implementação respeita formato AIOX:
- Executor `@dev` (story field) executou Tasks 1-4
- Quality gate `@devops` é o destino final do push (após este QA gate)
- Tasks 1-4 marcadas `[x]`, Task 5 marcada `[ ]` com justificação inline (deferred)
- File List separa "criados" de "não modificados intencionalmente"
- Change Log com 3 entradas datadas (1.0/1.1/1.2)
- DoD com checkboxes alinhados à execução real

### Article IV — No Invention

PASS. Cada decisão tem trace:
- `timeout-minutes` ausente da story original mas é defensive convention de GH Actions (não invenção de feature)
- `concurrency: cancel-in-progress` ausente da story original mas é optimização recomendada (justificada em runbook §6)
- `permissions: contents: read` ausente da story original mas é prática NFR8-aligned (least-privilege)
- 3 jobs implementados são literal subset do Architecture §11.4
- Step names PT-PT alinham com CON3+CON4 (PT-PT obrigatório)
- Comentários inline citam ACs/NFRs explícitos (`AC1, AC2, AC9` linha 42, etc.)

Os 3 itens "extra" (`timeout-minutes`, `concurrency`, `permissions`) são robustness layers sem expandir escopo — não inventam features novas.

### Article VI — Absolute Imports (CON5)

N/A. Esta story só introduz YAML + markdown.

---

## Validação dos PO Must-Fixes

O PO gate (`docs/qa/gates/1.2-po-gate.md`) listou 2 must-fixes que @dev devia aplicar:

| Must-Fix PO | Aplicação @dev | Validação Quinn |
|------------|---------------|----------------|
| #1: Usar `pnpm --filter @meu-jarvis/db db:check-rls` ou `pnpm check:rls` (não `check-rls`) | Linha 154: `pnpm check:rls` (variante root) | PASS — `package.json` raiz linha 20 confirma `check:rls` existe |
| #2: NÃO adicionar scripts a `packages/db/package.json` (já existem) | File List da story: "`packages/db/package.json` — scripts `db:migrate` e `db:check-rls` já existiam desde Story 1.1" | PASS — `packages/db/package.json` linhas 17 e 20 confirmam scripts intactos |

**3 should-fix advisories PO** (não-bloqueantes) também tratados:

| Should-Fix PO | Aplicação @dev |
|---------------|---------------|
| #1: Riscos não listados explicitamente | Mitigado parcialmente — runbook §6 (Performance) e §7 (Troubleshooting) cobrem R1 (tempo > 5 min) e R2 (drift Postgres CI vs Supabase prod). R3 (paralelismo `needs: [quality]`) é decisão consciente documentada nos comments YAML linhas 159-161. ACEITÁVEL |
| #2: AC5 vs `needs: [quality]` clarificação | Comments YAML linhas 159-161 documentam decisão expressa: "AC5 pede paralelismo 'onde possível'; encadear build após quality poupa minutos CI quando lint/typecheck/test falham". `build` continua paralelo a `rls-gate`. Clarificado em runbook §1. PASS |
| #3: Tech debt MNT-001/MNT-002 | Story File List explicitamente: "`apps/web/next.config.ts` — tech debt MNT-001/MNT-002 fora do escopo desta story (Article IV — No Invention)". PASS |

---

## Análise dos ACs Deferidos

### AC6 — Pipeline completa em < 5 min

**Justificação @dev:** "Validação final só em primeira run real — ver runbook §4.1."

**Verificação Quinn:**
- AC6 é tempo de execução em runner GitHub Actions — **só observável após push**
- Runbook §4.1 dá procedimento concreto: criar branch `chore/ci-smoke-test`, alterar README, abrir PR, confirmar tempo total < 5 min em "Checks"
- Runbook §6 lista mitigações se tempo > 5 min: Turbo cache remoto, splittar `quality` em 3 jobs paralelos, matrix build
- Cache pnpm duplo + paralelismo `quality`/`rls-gate` são as optimizações disponíveis para garantir < 5 min sem mais investimento
- Tasks 5.1 e 5.2 da story marcadas `[ ]` com sufixo `_(deferred — requer push e PR; entregável para @devops na primeira run)_`

**Veredicto:** Aceitável. AC6 é observacional pós-merge e está coberto por runbook + plano de mitigação documentado.

### AC10 — Branch protection rule

**Justificação @dev:** "Documentado em `docs/runbooks/ci-setup.md` §3 — configuração manual no GitHub UI após merge."

**Verificação Quinn:**
- Branch protection é **configuração GitHub UI** — não tem representação em código (nem mesmo via Terraform a esta altura do projecto, e não está pedido em nenhuma story)
- Runbook §3 dá procedimento detalhado: 8 checkboxes específicos a marcar, 3 status checks a adicionar como required, nota explícita "Do not allow bypassing the above settings" (importante)
- Runbook §3 nota: "Os nomes que aparecem em 'Require status checks' só ficam disponíveis depois de a pipeline ter corrido pelo menos uma vez num PR ou push" — sequência operacional correcta documentada
- @devops é o agente responsável por executar isto; story documenta o "como"

**Veredicto:** Aceitável. AC10 é "configuration as documentation" e o runbook entrega documentação operacional completa.

---

## Findings

### CRITICAL — 0
Nenhum.

### HIGH — 0
Nenhum.

### MEDIUM — 0
Nenhum *novo* nesta story. (MNT-001/MNT-002 são herança da Story 1.1 e estão correctamente excluídos do escopo.)

### LOW — 2 (Advisories para próximas stories)

**DOC-001:** Smoke test (§4.1) e negative tests (§4.2/4.3) do runbook só serão executados após primeiro merge. AC6 (<5 min) e DoD "rls-gate falha quando RLS coverage está incompleta" ficam validados nessa primeira run.
- **Severidade:** low
- **Categoria:** documentation / verification gap
- **Acção sugerida:** @devops deve registar tempo total da primeira CI run num comment no PR de smoke test. Correr os 2 negative tests (§4.2 quality fail + §4.3 rls fail) **antes** de marcar branch protection como required, para evitar bloquear merge legítimo se houver bug.

**TEST-002:** Pipeline implementa apenas 3 dos ~6 jobs canónicos do Architecture §11.4 (`quality`, `rls-gate`, `build`). Os 3 ausentes (`integration`, `e2e`, `llm-bench`, `deploy`) ficam para stories futuras.
- **Severidade:** low
- **Categoria:** scope / future stories
- **Acção sugerida:** Documentado correctamente em runbook §9. Trace para próximas stories: `e2e` em Epic 1 stories de UI testing; `llm-bench` em Epic 2; `integration` quando Stripe webhook entrar (Epic 3 ou story dedicada Stripe); `deploy` em story dedicada de Vercel deploy.

---

## Code Intelligence (Optional — Skipped)

> `isCodeIntelAvailable()` não retornou provider activo nesta sessão; secções "Blast Radius" e "Test Coverage" não aplicáveis. Story é puramente infra YAML + markdown — modificações não invocam funções/classes existentes do codebase, então blast radius seria 0 mesmo que provider estivesse activo.

---

## Tech Debt Log

**Nesta story:** zero novos itens.

**Herança aceite (Story 1.1):**
- MNT-001 (`experimental.typedRoutes` deprecado em `apps/web/next.config.ts`) — fora de escopo desta story; rolar para Story 1.4 ou tech-debt story dedicada
- MNT-002 (`outputFileTracingRoot` warning em `apps/web/next.config.ts`) — idem
- TEST-001 (smoke tests da Story 1.1 só verificam `1+1=2`) — recomendação para Story 1.3+ adicionar pelo menos 1 teste comportamental real

---

## Recommendations

1. **Antes do push da story 1.2 (immediate):**
   - @devops aplica este QA gate como sinal verde
   - @devops faz `git push` da branch contendo commit `54ca426`
   - Após merge ou no PR de validação: executar smoke test (runbook §4.1) e os 2 negative tests (§4.2 + §4.3)
   - Adicionar branch protection rule no GitHub UI (runbook §3) **só depois** de confirmar que os 3 status checks aparecem em "Require status checks"

2. **Bloqueador externo conhecido (Story 1.3):**
   - Story 1.3 depende de credenciais Supabase (project URL + anon key + DB password) que **só Eurico tem**
   - Recomendação Quinn: **NÃO arrancar 1.3** até credenciais Supabase estarem em mãos
   - Alternativa B (não-bloqueada por externos): considerar uma das B-stories que o Eurico priorize (B1/B2/B3/B4)
   - Alternativa C: Story 1.7 (também bloqueada por externos — confirmar com Eurico)

3. **Próximas stories que vão tocar `apps/web/next.config.ts`:**
   - Aproveitar para resolver MNT-001 + MNT-002 (5 min cada, PO gate da 1.2 já recomendou Story 1.4 como destino natural)

4. **Quando CodeRabbit for activado em `core-config.yaml`:**
   - Adicionar job `coderabbit-review` ao `ci.yaml` numa story dedicada (NFR18 plenamente endereçado)
   - Runbook §8 já antecipa esta manutenção

---

## Source Tree & Path Validation

Verificado contra o estado actual do repo:

| Path | Estado | Notas |
|------|--------|-------|
| `.github/workflows/ci.yaml` | EXISTE (198 linhas) | Criado nesta story, YAML válido |
| `docs/runbooks/ci-setup.md` | EXISTE (209 linhas) | Criado nesta story, 10 secções |
| `docs/runbooks/` (dir) | EXISTE | Criado implicitamente pela criação do `ci-setup.md` |
| `packages/db/package.json` | NÃO MODIFICADO | Scripts `db:migrate` (linha 17) e `db:check-rls` (linha 20) intactos — must-fix #2 do PO respeitado |
| `package.json` raiz | NÃO MODIFICADO | Script `check:rls` (linha 20) intacto |
| `scripts/check-rls-coverage.ts` | NÃO MODIFICADO | Story 1.1 entregou; usado pelo gate |
| `apps/web/next.config.ts` | NÃO MODIFICADO | Tech debt MNT-001/MNT-002 fora de escopo (Article IV respeitado) |
| `docs/qa/gates/1.2-po-gate.md` | EXISTE | PO gate de origem |
| `docs/qa/gates/1.2-qa-gate.md` | CRIADO POR ESTE GATE | — |

Tudo coerente.

---

## Decision Matrix

| Outcome | Critério | Aplicável? |
|---------|----------|-----------|
| **PASS** | Todos os 7 quality checks PASS, 0 issues HIGH/CRITICAL, ACs satisfeitos ou deferidos com justificação válida | **YES — verdict actual** |
| CONCERNS | Issues MEDIUM presentes mas não bloqueantes; ACs essenciais satisfeitos | NO |
| FAIL | Issues HIGH/CRITICAL presentes ou ACs essenciais não satisfeitos | NO |
| WAIVED | Issues conhecidos e aceites com aprovação explícita | NO |

---

## Próximos Passos no Workflow SDC

```
[ATUAL] @qa *gate 1.2 → verdict PASS
   ↓
@dev actualiza Story 1.2 Status: Ready for Review → Done (próximo passo da story)
   ↓
@devops *push → push branch contendo commit 54ca426 + abre PR
   ↓
[primeira CI run real] @devops valida AC6 (<5 min) + executa runbook §4.1, §4.2, §4.3
   ↓
@devops configura branch protection rule (runbook §3) marcando os 3 status checks como required
   ↓
[BLOQUEADO] Story 1.3 — aguarda credenciais Supabase do Eurico
```

**Alternativa imediata caso Eurico não tenha credenciais Supabase prontas:**
- Pausar pipeline Epic 1 até desbloqueio externo, OU
- Escolher Story B1/B2/B3/B4 (também não-bloqueadas por externos) — confirmar prioridade com Eurico

---

## Handoff

next_agent: @devops
next_command: *push
condition: QA gate verdict é PASS — story actualizada para `Done`, push autorizado

alternatives:
  - agent: @dev
    command: *develop {next-story}
    condition: Story 1.3 desbloqueada por credenciais Supabase
  - agent: @aiox-master
    command: *next
    condition: Eurico solicita escolha de B-story alternativa em vez de 1.3

---

## Change Log do Gate

| Data | Acção | Autor |
|------|-------|-------|
| 2026-05-04 | Gate criado, verdict PASS 7/7. 0 HIGH/CRITICAL, 2 LOW advisories (DOC-001 + TEST-002). PO must-fixes validados aplicados. AC6/AC10 deferrals validados aceitáveis. | Quinn (@qa) |
