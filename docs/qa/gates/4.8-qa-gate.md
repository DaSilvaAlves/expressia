# QA Gate — Story 4.8 (Vista "Cartões" — fatura on-the-fly + prestações)

**Gate type:** Phase 4 SDC — Quality Gate
**Story:** `docs/stories/active/4.8.finance-cards-view.story.md` (Ready for Review v1.2)
**Epic:** Epic 4 — Módulo Finanças
**Reviewer:** @qa (Quinn)
**Date:** 2026-05-22
**Nota de routing:** o `Owner` da story atribui o gate a `@architect`; o gate foi corrido por `@qa` por invocação explícita do utilizador (`*qa-gate` é comando exclusivo do @qa — `agent-authority`). Âmbito QA: 7-point quality gate + verificação independente de evidência.

---

## Veredicto

**PASS — 9,5/10 — HIGH confidence**

Story de observação reuse-heavy sobre as Stories 4.6/4.7, com uma peça nova de risco real — a aritmética do ciclo de facturação — correctamente isolada na função pura `calcStatementCycle`. 8/8 ACs PASS, 8/8 Tasks DONE, 21 testes novos, 716/716 testes web GREEN. Os 5/5 quality gates foram **re-verificados independentemente** pelo @qa (não aceites por reporte). O risco macro R-4.6 (fronteira do ciclo de fecho) está mitigado de forma sólida: convenção de fecho inclusivo (D-4.8.1) explícita no código e testada na fronteira exacta. Zero hallucinations — os 8 ficheiros da File List existem e estão no commit `204b585`. 1 observação LOW non-blocking (NIT-4.8.1 — `installments` fetched sem filtro `card_id` explícito; deviation funcional-equivalente da AC3, documentada nas Completion Notes).

Status recomendado: **Ready for Review v1.2 → Done v1.3**.

---

## 1. 7-Point Quality Gate

| # | Dimensão | Resultado |
|---|----------|-----------|
| 1 | **Requirements traceability** — 8 ACs | **PASS** — AC1 `FinanceViewTabs` (`href` activado) + rota `/financas/cartoes` · AC2 `card-statement-helpers.ts` (`calcStatementCycle` pura) · AC3 `list-card-statements.ts` (`getCardStatements`, 4 queries fixas) · AC4 página RSC + `CardStatementCard` · AC5 `CardInstallmentsList` (progresso X/N) · AC6 guard cartões sem ciclo · AC7 21 testes · AC8 5/5 gates. Todos mapeados a FR15/FR16, Epic §5 Story 4.8, DP7=A, R-4.6. |
| 2 | **Risk mitigation** — foco R-4.6 (fronteira do ciclo) | **PASS** — `calcStatementCycle` é função pura sem Drizzle/rede/timezone. Convenção D-4.8.1 (janela `[cycleStart, cycleEnd]` com ambos extremos inclusivos) explícita no código: `currentCycleEnd` = próxima ocorrência de `closingDay` a partir de hoje inclusive (`todayDay <= closingDay`); `sumWindow` usa `< start || > end` (inclusivo). `card-statement-helpers.test.ts` (10 testes) cobre a fronteira exacta, viragem de mês e `dueDay >/=/< closingDay`. R-4.8.5 (janela de fetch ±2 meses) verificada suficiente — `currentCycleStart` no pior caso ~1 mês antes de hoje, `nextCycleEnd` ~2 meses depois; a janela ±2 meses cobre folgadamente qualquer `closing_day ∈ [1,28]`. |
| 3 | **Test architecture** — 21 testes novos | **PASS** — 10 helper de ciclo (puros, zero mocks — `today` injectado, determinístico) + 6 `list-card-statements` (`db` mock via `DbShim`) + 5 `cartoes/page.tsx` (render, empty state, auth redirect, cartão sem ciclo). Suite total **716/716 GREEN** (87 ficheiros, 19,5s), zero regressão. |
| 4 | **NFR compliance** — NFR5/13/19 | **PASS** — NFR5: zero tabelas/colunas novas, `check:rls` re-corrido GREEN (15 tabelas cobertas). NFR13: `withSpan('finance.cards.render', ...)` + `captureException` em catch. NFR19: imports `@/...` absolutos. Multi-tenancy: página usa exclusivamente `getDb()` (RLS authenticated) — `getServiceDb()` ausente (grep confirmado), R-4.8.4 mitigado. |
| 5 | **Code quality** — sem `any`, sem N+1, SQL parametrizado | **PASS** — `getCardStatements` faz exactamente 4 queries via `Promise.all`, bucketing por ciclo em JS — sem N+1 independentemente do nº de cartões (D-4.8.3). SQL 100% via `sql` template parametrizado (`${windowStart}::date`, `${today}::date`) — zero interpolação de string. Tipos explícitos (`CardRow`/`TxRow`/`InstallmentRow`/`ProgressRow`). `lint` GREEN (0 warnings). Total por `kind` (`expense − income`, `transfer` excluído — D-4.8.6). |
| 6 | **Constitution** — 6/6 | **PASS** — ver §4. |
| 7 | **Backward compatibility** | **PASS** — zero migration, zero alteração de schema/RLS. `FinanceViewTabs.tsx` muda 1 `href` (placeholder → `<Link>`); `patrimonio` permanece desactivado para a Story 4.9. Rota nova `/financas/cartoes` aditiva. Zero breaking change. |

---

## 2. Verificação independente de evidência

Os 5 quality gates foram **re-corridos pelo @qa** — a story reportava "5/5 GREEN" e isso foi confirmado, não assumido.

| Gate | Reporte @dev | Re-verificação @qa | Status |
|------|--------------|--------------------|--------|
| `pnpm typecheck` | GREEN | **GREEN** — 9/9 tasks (FULL TURBO) | ✓ PASS |
| `pnpm lint` | GREEN 0 warnings | **GREEN** — 9/9 tasks, 0 warnings | ✓ PASS |
| `pnpm --filter @meu-jarvis/web test` | 716/716 | **GREEN** — 87 ficheiros, **716/716** testes | ✓ PASS |
| `pnpm build` | GREEN | **GREEN** — `✓ Compiled`; rota `/financas/cartoes` presente (1,02 kB · 209 kB First Load JS) | ✓ PASS |
| `pnpm check:rls` | GREEN | **GREEN** — 15 tabelas, `cards` coberta | ✓ PASS |

**Ficheiros da File List:** 8/8 presentes no working tree e no commit `204b585` (1271 inserções, 1 deleção).

**Carry-over conhecido (não-bloqueante, não atribuível à 4.8):** `pnpm test` na raiz falha em `@meu-jarvis/db-test` — Testcontainers exige Docker, ausente no ambiente local (carry-over das Stories 4.1-4.7). A Story 4.8 não altera RLS nem schema; o job CI `rls-gate` cobre essa camada contra Postgres efémero. Sem impacto no veredicto.

---

## 3. Verificação de Acceptance Criteria

| AC | Resultado | Evidência |
|----|-----------|-----------|
| AC1 — `FinanceViewTabs` + rota | **PASS** | `FinanceViewTabs.tsx` 1 linha alterada (`href` de `cartoes`); `cartoes/page.tsx` criada; build lista a rota. |
| AC2 — `calcStatementCycle` pura | **PASS** | Função pura `date-fns`, sem Drizzle/rede; `StatementCycle` exportada; `calcDueDate` aplica `dueDay > closingDay`. |
| AC3 — `getCardStatements` agregação | **PASS** | 4 queries fixas, `db`-injectável (`DbShim`), bucketing JS, totais por `kind`. |
| AC4 — página + `CardStatementCard` | **PASS** | RSC com auth → `redirect('/entrar')` → `resolveHouseholdId` → `getDb()` + `withSpan`; fatura corrente + próxima; empty states. |
| AC5 — prestações associadas | **PASS** | `CardInstallmentsList.tsx`; progresso `paidCount` = transacções com `transaction_date <= today`. |
| AC6 — cartões sem ciclo | **PASS** | Guard `c.closing_day !== null && c.due_day !== null` → `cycle: null`; nota renderizada, sem erro. |
| AC7 — ~20 testes Vitest | **PASS** | 21 testes novos (10+6+5); fronteira R-4.6 coberta; suite GREEN. |
| AC8 — 5/5 quality gates | **PASS** | Re-verificado independentemente — ver §2. |

**8 [AUTO-DECISIONS] (D-4.8.1 a D-4.8.7) ratificadas** — todas implementadas e rastreáveis no código (comentários de trace presentes nos dois helpers).

---

## 4. Constitution Check (6/6)

| Artigo | Resultado |
|--------|-----------|
| I — CLI First | **PASS** — vista de observação; criação de cartões deferida ao cérebro Jarvis (D-4.8.7). |
| II — Agent Authority | **PASS** — SDC respeitado; gate sem push (delegado a @devops). |
| III — Story-Driven | **PASS** — trabalho 100% a partir da Story 4.8; tasks/checkboxes actualizados. |
| IV — No Invention | **PASS** — 11 verificações anti-hallucination; zero feature inventada; helpers com trace explícito a AC/D/epic. |
| V — Quality First | **PASS** — 5/5 gates re-verificados; 21 testes novos; risco R-4.6 isolado e testado. |
| VI — Absolute Imports | **PASS** — imports `@/...` / `@meu-jarvis/...`; zero relativo. |

---

## 5. Issues

| ID | Severidade | Finding | Suggested action |
|----|-----------|---------|------------------|
| NIT-4.8.1 | **low** | A query 3 (`installments`) faz `select ... from public.installments` sem `where card_id in (...)` nem filtro de cartão arquivado — a AC3 redigia "WHERE card_id IN (<cartões>)". Funcionalmente equivalente: a RLS scopa por household e o mapeamento JS descarta prestações de cartões não listados (incl. arquivados). Efeito: ligeiro overfetch de prestações de cartões arquivados, depois descartadas. | Non-blocking. Documentado nas Completion Notes ("queries 2-4 não precisam de lista de `card_id` — RLS scopa"). Opcional: numa story futura, alinhar a query à redacção da AC ou actualizar a AC. Sem impacto em correcção nem em performance real (volume de prestações por household é baixo). |

Nenhum issue de severidade `medium` ou `high`. Nenhum bloqueador.

---

## Gate Status

Gate: PASS → docs/qa/gates/4.8-qa-gate.md

---

## Handoff

```yaml
next_agent: "@devops"
next_command: "*push"
condition: "QA gate PASS — publicar 204b585 + 43c13aa"
context: |
  Story 4.8 PASS. Após push, Epic 4 → 8/10 Done.
  Restam Story 4.9 (vista Património) + Story 4.10 (tools do cérebro).
  Working tree: docs/marketing/edit/ untracked (carry-over — decisão do @devops se entra no push ou fica de fora).
```

---

*QA gate corrido por Quinn (@qa AIOX) em 2026-05-22. Evidência verificada independentemente — 5/5 quality gates re-corridos, 8/8 ficheiros confirmados no commit `204b585`, núcleo de risco R-4.6 inspeccionado. Constitution Article V (Quality First).*
