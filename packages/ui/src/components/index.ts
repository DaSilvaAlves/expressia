/**
 * Barrel — componentes de `@meu-jarvis/ui` (Story 5.2).
 *
 * Componentes shared cross-package consumíveis via
 * `import { MoneyDisplay, DateDisplay } from '@meu-jarvis/ui'`.
 */
export * from './MoneyDisplay';
export * from './DateDisplay';
