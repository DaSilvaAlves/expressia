{
  "name": "@meu-jarvis/db-test",
  "version": "0.1.0",
  "private": true,
  "description": "Suite de testes RLS automatizada — meu-jarvis (Expressia). Vitest + Testcontainers Postgres 16 efémero. Trace: Story 1.4, NFR5.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"@meu-jarvis/db-test: lint herdado do typecheck — ver typecheck script\"",
    "build": "echo \"@meu-jarvis/db-test: package source-only (sem build step)\""
  },
  "dependencies": {
    "@meu-jarvis/db": "workspace:*",
    "@testcontainers/postgresql": "^10.16.0",
    "drizzle-orm": "^0.34.0",
    "postgres": "^3.4.9",
    "testcontainers": "^10.16.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  },
  "engines": {
    "node": ">=20"
  }
}
